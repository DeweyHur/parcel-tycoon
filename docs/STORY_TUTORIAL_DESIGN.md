# 상하차의 신 — 스토리·튜토리얼 모드 · 한국 달력 시나리오 기획서 v0.1

> 구현 상태(v1.4): 달력·이벤트·시나리오 재정의·스토리 비트 23개·대화창·달력 카드·계절 문자·창고장 노트·설정 토글까지 구현됨. 구현 메모는 이 문서 끝 부록 C.

`GAME_DESIGN.md`, `META_DESIGN.md`, `BALANCE_DESIGN.md`(v1.3 한 해 런) 위에 얹는다. 세 가지를 정한다.

1. **스토리 모드**: 전임 창고장이 3개월 동안 옆에서 잔소리하다 떠나는 대화형 튜토리얼. 강제 아님, 언제든 끌 수 있음.
2. **가이드 캐릭터**: 전임 창고장. 픽셀 아트 32×32 상반신, 표정 4종.
3. **나라별 달력 시나리오**: 지금처럼 "성수기·폭염·파업"이 따로 노는 대신, 한 나라의 1년 달력이 이벤트를 배치한다. 첫 세트는 한국. 미국·일본은 같은 틀에 데이터만 끼운다.

동기: 이 게임은 콘셉트가 친근하지 않다. 첫 화면에서 "계약, 배차비, 어음, 반송"이 한꺼번에 나오면 규칙을 배우기 전에 나간다. 규칙은 그대로 두고, **사람이 옆에서 한 번에 하나씩 말해 주는 것**으로 진입 장벽을 낮춘다. 동시에 시나리오가 달력을 따라가면 플레이어가 "다음 달엔 뭐가 오는지" 예측할 수 있고, 캐릭터가 그걸 미리 말해 줄 수 있다.

---

## 1. 원칙

- **규칙을 바꾸지 않는다.** 스토리 모드는 표준 1년 시나리오(`standard`, 한국 달력) 위에 대화 층을 얹은 것이다. 수치·난이도 보정 없음. 수습 난이도를 고르면 그 보정만 붙는다.
- **막지 않는다.** 대화는 한 번에 최대 3~4문장, 한 탭으로 넘어가고, 창을 덮지 않는다(하단 대화창). 턴 진행은 대화가 끝나야 하지만, `건너뛰기 ×`가 항상 보인다.
- **한 비트에 한 개념.** "창고 사용률"을 말하는 비트에서 "신뢰도"를 같이 말하지 않는다.
- **상황이 왔을 때 말한다.** 월초 브리핑을 빼면 대부분의 비트는 조건 트리거다(창고 76% 도달, 기한 1턴 남은 택배 발생, 첫 반송…). 아직 안 일어난 일을 미리 설명하지 않는다.
- **3개월 뒤엔 사라진다.** 6월 1턴에 창고장이 떠난다. 그 뒤로는 계절 예고 한 줄(월초 문자 메시지 톤)만 남고, 그것도 설정에서 끌 수 있다.
- **첫 런만 자동.** 프로필에 `story.seen`이 있으면 다음 런부터는 시작 시 "인수인계 다시 듣기" 토글이 꺼진 채로 나온다. 도움말에서 비트를 다시 읽을 수 있다.

---

## 2. 가이드 캐릭터 — 전임 창고장

플레이어는 동네 택배를 **인수한 새 사장**이다. 전임 창고장은 회사를 넘기고 석 달만 남아 있기로 한 사람. 회사를 물려주는 사람이라 "가르친다"가 아니라 "이 창고는 이렇게 굴러왔다"는 톤이 되고, 3개월 뒤 떠나는 것이 튜토리얼이 끝나는 자연스러운 이유가 된다.

### 2.1 시안 4종 (`character_concepts.png`)

| 시안 | 외형 | 말투 | 잘 맞는 점 | 걸리는 점 |
|---|---|---|---|---|
| **A. 박 반장** | 60대 · 흰머리 · 안경 · 작업조끼 · 머그 | 느긋한 반말. "그럴 수 있지." 실수하면 커피부터 권함 | 진입 장벽 낮추기라는 목적에 가장 맞음. 게임의 딱딱한 용어를 부드럽게 풀기 좋음 | 긴장감 있는 비트(반송·배상)에서 무게가 덜할 수 있음 → 걱정 표정으로 보완 |
| **B. 강 소장** | 50대 여성 · 쪽머리(새치) · 클립보드 | 존댓말, 숫자로 말함. 칭찬 짧고 지적 정확 | 게임 성격(타이트한 자금 운영)과 톤이 일치. "창고 76%예요. 다음 턴 입고 3칸. 계산 되죠?" | 친근함이 목적인데 첫인상이 차가울 수 있음 |
| **C. 노 기사** | 70대 · 빨간 트럭 캡 · 흰 콧수염 · 목수건 | 사투리 섞인 옛날 얘기. "추석 전주엔 차가 안 잡혀" | 한국 달력 이벤트(명절·장마·김장)를 **경험담**으로 말하기 가장 자연스러움 | 규칙을 정확히 설명하는 역할엔 약함. 문장이 길어질 위험 |
| **D. 여 실장** | 40대 · 포니테일 · 헤드셋 · 형광조끼 · 태블릿 | 짧은 문장, 현장 무전 톤 | 젊은 플레이어 친화, 대사가 짧아 모바일에 맞음 | "전임 창고장이 떠난다"는 서사가 40대엔 덜 어울림(이직? 승진?) |

### 2.2 추천

**A(박 반장)를 본체로, C(노 기사)의 경험담 톤을 계절 예고에 섞는다.** 즉 3개월 튜토리얼 비트는 박 반장이 규칙을 풀고, 6월 이후 월초 한 줄 예고는 "박 반장이 보낸 문자" 형식으로 노 기사식 경험담("추석 전주엔 차가 안 잡혀. 계약 미리 채워 둬")을 쓴다. 캐릭터는 하나, 톤은 두 층.

B는 **베테랑 난이도 전용 안내자**로 남겨 두면 좋다. 베테랑을 처음 고를 때 강 소장이 한 번 나와 "여기서부턴 숫자로 말합니다"라고 하는 식 — 나중 이야기.

### 2.3 스프라이트 사양

- 32×32 상반신, 투명 배경, 표정 4종(기본·웃음·걱정·놀람). 대화창에서 ×4(128px). 개별 PNG는 `sprite_A_<expr>.png`로 생성해 뒀다(생성 스크립트 `sprites.py`로 팔레트·소품 조정 가능).
- 표정 규칙: 규칙 설명 = 기본, 칭찬·정산 흑자 = 웃음, 위험 경고(창고 90%, 기한 1턴, 반송 예정) = 걱정, 사고 결과(반송·도난·파손) = 놀람.
- 픽셀 UI와 같은 배율(정수 배)로만 확대. 안티앨리어싱 금지.

---

## 3. 스토리 모드 구조

### 3.1 시작

타이틀에 **「인수인계」** 버튼(= 스토리 모드). 누르면 시나리오·회사·퍽 선택 없이 바로 시작한다: 표준 1년 · 한국 달력 · 동네 택배 · 퍽 없음 · 난이도만 묻는다(수습/정규, 기본 수습). 일반 「새 런」에는 준비 화면에 `창고장 안내` 토글이 있어 어떤 회사·시나리오에서도 켤 수 있다(비트는 회사 무관하게 동작해야 한다 — 3.4).

### 3.2 시간 배치

| 달력 | 개월차 | 창고장 | 다루는 것 |
|---|---:|---|---|
| 3월 | 1 | 매일 옆에 있음 | 입고 → 대기 → 차 꽉 채워 호출. 창고 사용률. 기한. 첫 정산(운영비 내역). 마켓과 계약 |
| 4월 | 2 | 자주 나옴 | 고객과 신뢰, 특수 품목의 등장(냉장 구역), 직접 배송, 이사철 보관 계약, 어음(후불 배차비) |
| 5월 | 3 | 가끔 나옴, 마지막 날 떠남 | 파손·배상·보험, 야외 적재와 비, **승리 조건**(1년 완주), 앞으로의 달력 예고, 작별 |
| 6월~2월 | 4~12 | 월초 문자 한 줄 | 다음 달 계절 이벤트 예고 (끌 수 있음) |

3개월 = 30턴 안에 조건 비트가 대부분 한 번은 일어나도록 트리거를 잡는다. 안 일어난 비트(예: 30턴 동안 반송 0)는 그냥 안 나온다 — 그 플레이어는 이미 알고 있는 것이다.

### 3.3 트리거 종류

| 종류 | 예 | 비고 |
|---|---|---|
| `month_start` | 매월 1턴, 입고 직후 | 브리핑. 4~5월은 짧게 |
| `turn` | 1개월차 1·2·3턴 | 첫 3턴만 고정 스크립트(첫 입고, 첫 대기, 첫 호출 유도) |
| `state` | 창고 ≥ 76% / ≥ 91%, 기한 1턴 남은 택배 존재, 첫 특수 택배 입고, 냉장 구역 가득 | 조건 충족 턴 시작에 1회 |
| `event` | 첫 호출 직후, 첫 정산, 첫 마켓 진입, 첫 계약 구매, 첫 반송, 첫 도난, 첫 파손, 첫 보관 제안, 첫 비 예보, 첫 부채 | 이벤트 큐 구독(도전과제 판정기와 같은 자리) |
| `farewell` | 6월 1턴 | 작별 + 달력 요약 카드 |
| `season_note` | 7월~2월 1턴 | 문자 메시지 UI, 1줄 |

같은 턴에 여러 비트가 걸리면 **하나만** 보여 주고 나머지는 다음 턴으로 미룬다(우선순위: event > state > month_start). 위험 경고류(`state`)는 미루지 않고 그 턴에 낸다.

### 3.4 회사 무관 조건

일반 런에서 안내를 켤 수 있으므로 비트는 "동네 택배의 시작 계약이 대량·냉장·프래자일"이라고 가정하지 않는다. 대사에서 계약 이름은 `{contract}`처럼 현재 보유 계약에서 뽑아 넣는다. 강철 창고처럼 냉장 구역이 0인 회사에서는 냉장 비트가 자동으로 빠진다(트리거 조건이 안 걸리므로).

---

## 4. 비트 목록 (3개월)

말투는 박 반장 기준. 각 비트는 최대 4문장, 필요하면 `강조` 대상(UI 요소 하이라이트)을 붙인다.

### 4.1 3월 — 창고

| # | 트리거 | 강조 | 내용 (요지) |
|---|---|---|---|
| 1 | 3월 1턴 시작 | — | 인사. "오늘부터 사장은 자네. 나는 석 달만 잔소리하고 빠질게." 3월은 이사철이라 일반 택배가 좀 많다 |
| 2 | 3월 1턴, 입고 직후 | 택배 목록 | 첫 택배. "저게 다 돈이야, 근데 저대로 두면 창고만 차." 기한 표시(⏳4턴) 가리킴 |
| 3 | 3월 1턴, 행동 선택 | `대기` 버튼 | **핵심 규칙**: 매 턴 차를 부르지 않는다. 차는 한 대에 얼마고, 꽉 채워 보낼수록 개당 싸진다. "오늘은 기다려" |
| 4 | 3월 2턴 | 창고 게이지 | 창고 사용률. 60%까지는 편하고 90% 넘으면 밖에 쌓인다(야외 적재는 5월에 자세히) |
| 5 | 3월 3턴 또는 일반 택배 부피 ≥ 탑차 용량 | 계약 버튼 | 호출 유도. 차량 게이지 설명: "6칸 차에 5칸 실렸으면 80%, 그게 손해 안 보는 선" |
| 6 | 첫 호출 직후 | 결과 카드 | 순익 = 수입 − 배차비. 적재 효율 ✓가 뜨면 신뢰 +1 → "신뢰는 4월에 얘기하자" |
| 7 | 기한 1턴 남은 택배 첫 발생 | 해당 택배 | (걱정) 기한 넘기면 보상 −25%, 이틀 더 두면 반송. "급한 건 급한 대로 보내" |
| 8 | 창고 ≥ 76% 첫 도달 | 창고 게이지 | (걱정) 주의 구간. 다음 2턴 입고 예정 확인하는 법 |
| 9 | 창고 ≥ 91% 첫 도달 | 창고 게이지 | (놀람) 위험 구간. 넘치면 밖으로 나가고 밖은 도둑이 든다 |
| 10 | 첫 정산 | 정산 화면 | 운영비 내역: 임대 + 계약 유지비 + 시설 + 인건비. "월세는 안 깎여. 배차비를 아끼는 게 자네 일" |
| 11 | 첫 마켓 진입 | 마켓 | 마켓에서만 계약을 산다. 계약 = 차를 몇 대 부를 권리. 상단 예측 줄(다음 달 물량) 보는 법 |
| 12 | 마켓, 계약 카드 처음 탭 | 카드 | 용량·대당 배차비·월 한도. 표준/프리미엄 차이는 "나중에 돈 남으면" |
| 13 | 첫 마켓 종료 | — | "4월은 고객이 특수 물건을 슬슬 맡기기 시작해. 냉장 구역 비워 둬" |

### 4.2 4월 — 고객과 돈

| # | 트리거 | 강조 | 내용 |
|---|---|---|---|
| 14 | 4월 1턴 | 고객 아이콘 | 고객: 택배마다 누가 보냈는지 보인다. 기한 안에 보내면 그 고객 신뢰↑, 신뢰가 오르면 물량과 단가↑. 폐기하면 −3 |
| 15 | 첫 특수 택배 입고(❄/⚠/🛃 중 먼저 오는 것) | 해당 택배 | 속성 설명은 **온 것만**. ❄면 "냉장 구역에 자동으로 들어가, 밖에 나가면 하루 만에 상해" / ⚠면 "아무 차나 되는데 깨질 확률이 있어" / 🛃면 "이틀은 통관 대기, 손 못 대" |
| 16 | 특수 택배 있는데 맞는 계약 없음 | 대기 → 직접 배송 | 직접 배송: 대기 턴에 하나씩 내가 나른다. 비싸지만 급할 때 |
| 17 | 첫 보관 제안(이사센터) | 제안 카드 | 이사철. 자리만 빌려주고 돈 받는 계약. "공간을 파는 거야. 창고 넉넉할 때만" |
| 18 | 첫 어음/후불 표시 또는 자금 예상 잔액 음수 | HUD 자금 | 월중엔 돈 때문에 막히는 일 없음. 배차비는 월말에 한꺼번에. 위 숫자는 **월말 예상 잔액**. 빨간색이면 단기 차입 → 이자 |
| 19 | 4월 정산 | 정산 | 고객별 수입 줄. "한 고객한테만 기대면 그 고객 끊길 때 같이 끊겨" |
| 20 | 4월 마켓 | 상시 카드 | 배차 추가·창고 확장은 항상 있다. 확장은 갈수록 비싸니 미루지 말 것 |

### 4.3 5월 — 손실, 승리, 작별

| # | 트리거 | 강조 | 내용 |
|---|---|---|---|
| 21 | 5월 1턴 | — | 가정의 달, 선물 상자(⚠ 파손주의)가 는다. 보험 얘기 |
| 22 | 첫 파손 위험 있는 호출(⚠을 능력 없는 차에) | 호출 모달 확률 | 확률과 배상액이 미리 보인다. "25%면 네 번에 한 번 물어주는 거야" |
| 23 | 첫 사고(반송·도난·파손·부패 중 첫 것) | 결과 | (놀람) 배상. 보험이 있으면 얼마 돌려받는지. 없으면 마켓에서 보험사 고르는 법 |
| 24 | 첫 비 예보 | 날씨 칩 | 야외 적재가 젖으면 보상 −20%. 대기 턴에 적재 화면으로 안팎 바꾸기 |
| 25 | 5월 5턴 즈음 | — | **승리 조건**: "이 회사는 1년 단위로 돌아. 내년 2월 설 물량까지 넘기면 자네가 진짜 사장이야. 점수는 그다음 얘기" |
| 26 | 5월 정산 | — | 3개월 성적표: 처리 개수, 반송, 적재 효율 횟수, 자금. 한 줄 평(흑자면 웃음, 적자면 걱정 + "6월부터 물량이 뛰어") |
| 27 | 6월 1턴 (`farewell`) | 달력 카드 | 작별. **1년 달력 카드**를 건넨다(5장 표의 플레이어용 버전: 월별 아이콘 + 한 줄). "달력은 사무실 벽에 붙여 뒀어. 급하면 문자해" |

### 4.4 6월 이후 — 문자 한 줄

`season_note` 형식. 문자 메시지 UI(작은 말풍선, 스프라이트 없이 아이콘), 탭하면 사라짐.

| 달력 | 문자 |
|---|---|
| 6월 | "장마 시작이야. 밖에 둔 건 다 젖어. 안에 못 넣을 거면 먼저 보내" |
| 7월 | "폭염. 냉장 물건 밖에 두면 그 턴에 끝이야. 냉장 구역 넓힐 거면 지금" |
| 8월 | "휴가철이라 일반은 좀 줄고 신선이 많아. 냉장 차 한 대 더 잡아 둬" |
| 9월 | "추석 전주엔 선물세트가 쏟아지고, 연휴엔 차가 안 잡혀. 연휴 전에 창고 비워" |
| 10월 | "가을 이사철. 이사센터가 자리 빌려 달라고 올 거야. 창고 남으면 받아" |
| 11월 | "김장철에 농산물 나오고, 월말엔 쇼핑 행사로 일반이 폭주해. 대량 배차 넉넉히" |
| 12월 | "연말이야. 파손주의·냉동·큰 물건이 다 늘어. 올해 제일 바쁜 달. 보험 확인" |
| 1월 | "새해 첫 달은 잠잠해. 숨 돌리면서 2월 설 준비" |
| 2월 | "설. 추석이랑 같은데 더 짧고 세. 이거 넘기면 끝이야. 잘했어" |

---

## 5. 한국 달력 시나리오

### 5.1 왜 나라별인가

지금 시나리오는 "성수기·폭염·파업·항만"처럼 **한 가지 압박을 3~6개월 내내** 거는 방식이다. 1년 런에서는 이게 지루하고(같은 압박 12개월), 또 예측이 안 된다(왜 갑자기 성수기인지). 달력을 따르면 압박이 **달마다 다른 얼굴로** 오고, 플레이어는 "11월이 온다"는 것만으로 준비할 수 있다. 캐릭터가 예고하기도 쉽다.

기존 이벤트 시나리오는 없애지 않고 **달력의 특정 달을 잘라낸 단기 모드**로 재정의한다(5.4).

### 5.2 한국 1년 (3월 시작, 2월 끝)

수치는 현재 `D.SEASON_MODS`의 값(1월 −15%, 7·8월 신선·냉동↑, 9·10월 농산↑, 11월 +30%, 12월 +40%)을 이어받고 이벤트를 추가한 것이다. 배수는 봇으로 재조정할 전제.

| 달력 | 이벤트 | 입고 | 품목 이동 | 날씨 가중 | 특수 규칙 |
|---|---|---:|---|---|---|
| 3월 | 봄 이사철 · 신학기 | ×1.05 | 일반↑, 대형 +3%p | 맑음 | 이사센터 보관 제안 확률 ×1.5 |
| 4월 | 이사철 마무리 | ×1.0 | — | 비 10% | 보관 제안 ×1.5 |
| 5월 | 가정의 달 | ×1.05 | ⚠ 파손 +8%p(선물) | 맑음 | 5~7턴 "선물 주간": ⚠ 보상 +10 |
| 6월 | 장마 시작 | ×1.0 | — | 비 35% | 젖음 페널티 활성 |
| 7월 | 장마 · 폭염 | ×0.95 | ❄ +8%p, ❆ +4%p | 비 30%, 폭염 20% | 폭염 경보 턴 월 1회 예고 |
| 8월 | 폭염 · 휴가 | ×0.9 | ❄ +10%p, 일반 −10%p | 폭염 30% | — |
| 9월 | **추석** | ×1.25 | ❄ 신선·🌾 농산·⚠ 파손 +5%p씩(선물세트), 대형 +3%p | 맑음 | 연휴 전 3턴 "명절 폭주"(입고 ×1.6, 기한 −1), 연휴 2턴 **업체 휴무**(호출 불가, 입고 없음, 그다음 턴 합쳐서 입고) |
| 10월 | 가을 이사철 · 수확 | ×1.05 | 🌾 농산 +8%p | 맑음 | 보관 제안 ×1.5, 보관료 ×1.2 |
| 11월 | 김장철 · **쇼핑 행사** | ×1.3 | 🌾 +6%p(전반), 일반 +10%p(후반) | 비 15% | 7~10턴 "행사 주간": 일반 입고 ×1.5, 대량 배차비 −10%(업체도 대목) |
| 12월 | 연말 · 크리스마스 | ×1.4 | ⚠ +8%p, ❆ +6%p, 대형 +5%p | 폭설 15% | 폭설 = 자연 냉장(기존 규칙). 반송 유예 −1 |
| 1월 | 신정 · 비수기 | ×0.85 | — | 폭설 20% | 운영비 인플레 이번 달 미적용(숨 돌릴 틈) |
| 2월 | **설** | ×1.3 | 9월과 동일 구성, ❆ +3%p | 폭설 10% | 명절 폭주 3턴 + 휴무 2턴. 마지막 달 |

명절의 실제 날짜는 해마다 다르므로 게임 안에서는 **9월·2월의 5~6턴을 연휴**로 고정한다(폭주 2~4턴, 휴무 5~6턴, 7턴에 밀린 입고). 달력 표시에 "🎑 추석 연휴 5~6턴"이 처음부터 보인다.

### 5.3 데이터 구조

```js
M.CALENDARS.kr = {
  name: '한국', startMonth: 3,
  months: {
    9: { arrivalsMult: 1.25, typeShift: { fresh: 5, produce: 5, fragile: 5, large: 3 },
         weather: { clear: 70, rain: 15, heat: 15 },
         events: [ { id: 'holiday_rush', turns: [2,4], arrivalsMult: 1.6, deadlineDelta: -1 },
                   { id: 'holiday_off',  turns: [5,6], noCalls: true, noArrivals: true, carryOver: true } ],
         label: '추석', icon: '🎑', note: 'season.note.kr.9' },
    ...
  }
}
```

- 시나리오는 `calendar: 'kr'`을 가리키고, `rules`로 달력 값을 다시 덮을 수 있다(기존 `rules.season` 오버라이드 유지).
- `events[].turns`는 달 안의 턴 범위. `noCalls`는 파업 시나리오의 규칙을 그대로 재사용한다(파업 = 전 업체 휴무).
- `carryOver`는 태풍의 "다음 턴에 합쳐서 입고"와 같은 코드 경로.
- 미국·일본은 `M.CALENDARS.us / .jp`로 같은 모양. 예: us는 `startMonth 1`, 9월 레이버데이 세일, 11월 블랙프라이데이·사이버먼데이(×1.6), 12월 크리스마스 + 1월 리턴 시즌(반품 = 🛃와 비슷한 "회수" 택배, 나중에), 6~8월 이사철. jp는 4월 시작(신년도), 8월 오봉, 12월 오세이보·연말연시, 5월 골든위크 휴무.

### 5.4 기존 시나리오와의 관계

| 기존 | 재정의 |
|---|---|
| 표준(1년) | `calendar: 'kr'` 그대로. 스토리 모드의 바탕 |
| 튜토리얼 6개월 | 스토리 모드로 대체. 시나리오 목록에서는 제거(안내 토글이 대신) |
| 성수기 | **11~12월 2개월 컷**. 달력 11·12월 값을 그대로 쓰고 시작 자금·계약을 4개월차 수준으로 세팅 |
| 폭염 | **7~8월 2개월 컷** + 폭염 가중 ×2 |
| 파업 | 달력 안의 명절 휴무로 흡수. 단독 시나리오는 "무작위 업체 파업"으로 남겨도 됨(변형 규칙) |
| 항만 · 자금난 · 이사철 · 대형 계약 · 감사 | 달력 위에 `rules`만 얹는 변형. 길이는 전부 1년 |
| 데일리 | 1년은 길다 → **달력의 무작위 한 달 컷**(오늘은 "9월 추석 주간", 내일은 "12월 연말") + 변형 2개 |
| 무한 | 달력이 2년째로 이어짐. 매년 배수 ×1.15 |

---

## 6. 대사 샘플 (박 반장)

비트 1~3, 7, 10, 25, 27만 전문으로 적는다. 나머지는 4장의 요지에서 같은 길이로.

**#1 (3월 1턴)**
> 어서 와. 오늘부터 여기 사장은 자네야.
> 나는 석 달만 옆에서 잔소리하고 빠질게. 듣기 싫으면 저 ×를 눌러.
> 3월은 이사철이라 일반 택배가 좀 많아. 시작하기엔 나쁘지 않은 달이지.

**#2 (첫 입고)**
> 저게 오늘 들어온 거야. 저게 다 돈이지.
> 근데 저대로 두면 창고만 차. 옆에 ⏳ 보이지? 나흘 안에 보내야 해.

**#3 (첫 행동)**
> 여기서 제일 중요한 얘기 하나만 할게.
> 차는 매일 부르는 게 아니야. 한 대에 얼마씩 나가거든.
> 꽉 채워서 보낼수록 한 개당 싸져. 그러니까 오늘은 그냥 **기다려**.

**#7 (기한 1턴, 걱정)**
> 저거 내일까지야. 넘기면 보상이 4분의 1 깎이고, 이틀 더 두면 반송돼.
> 반송은 돈도 못 받고 물어줘. 급한 건 급한 대로 보내.

**#10 (첫 정산)**
> 자, 첫 달 정산. 운영비 내역 봐 봐 — 월세, 계약 유지비, 시설비, 인건비.
> 월세는 안 깎여. 자네가 아낄 수 있는 건 배차비뿐이야.
> 그래서 차를 꽉 채우라는 거야. 남았지? 잘했어.

**#25 (승리 조건)**
> 이 동네 택배는 1년 단위로 돌아. 3월에 시작해서 내년 2월 설까지.
> 설 물량까지 넘기면 그때부터 자네가 진짜 사장이야. 점수니 뭐니는 그다음 얘기고.
> 그 전에 돈이 마르거나 창고가 터지면… 뭐, 다시 하면 되지.

**#27 (작별, 6월 1턴)**
> 석 달 됐네. 이제 나 없어도 돌아가.
> 달력은 사무실 벽에 붙여 뒀어. 9월 추석, 11월 행사, 12월 연말 — 이 셋만 조심해.
> 급하면 문자해. 커피는 자네 몫이야.
> *(1년 달력 카드 표시)*

---

## 7. UI

- **대화창**: 하단 고정, 높이 약 140px(폰 세로 기준). 왼쪽 스프라이트 ×4, 이름(노랑), 본문 최대 4줄, 오른쪽 위 `건너뛰기 ×`, 오른쪽 아래 `▶ 다음`. 목업 `dialog_mockup.png`.
- **강조**: 비트에 `highlight` 셀렉터가 있으면 그 UI 요소에 픽셀 테두리 깜빡임. 대화창이 그 요소를 가리면 대화창을 위로 올린다.
- **문자 메시지**(6월 이후): HUD 아래 작은 말풍선. 아이콘 📱 + 1줄. 3초 뒤 자동 축소, 탭하면 전문.
- **달력 카드**: 12칸 그리드, 각 칸 아이콘 + 한 단어(🎑 추석, 🛒 행사, 🎄 연말…). 작별 때 한 번, 이후 HUD 달력 아이콘으로 언제든.
- **설정**: 「창고장 안내」 켜기/끄기, 「계절 문자」 켜기/끄기, 「본 안내 다시 듣기」(프로필 `story.seen` 초기화).
- 도움말에 「창고장 노트」 탭: 본 비트 전문을 순서대로.

---

## 8. 구현 노트

- `www/js/story.js`: `BEATS[]` = `{ id, trigger: {type, cond}, once: true, speaker: 'park', expr, text: 'story.park.1', highlight, months: [1,2,3] }`. 게임 이벤트 큐를 구독(도전과제 판정기와 같은 훅). 조건은 `Game` 상태 조회 함수로만(규칙 엔진 수정 없음).
- 문자열은 전부 locales(`story.*`). 영어판은 톤을 맞춰 따로 쓴다(직역 금지 — 박 반장의 반말은 영어에선 짧은 문장·구어체로).
- 프로필: `story: { seen: [beatId], enabled, notesEnabled }`. 비트 `once`는 프로필 기준이 아니라 **런 기준** — 새 런에서 안내를 켜면 다시 나온다. 프로필 `seen`은 첫 런 자동 켜짐 여부와 도움말 탭용.
- 달력: `M.CALENDARS`, `Game`의 `_extraArrivals`/`_typeRatio`/`_genWeather`가 `D.SEASON_MODS` 대신 `calendar.months[cal]`을 읽는다. `events[]`는 `_startTurn`에서 턴 범위 검사. 명절 휴무는 파업 코드(`rules.strike`)를 전 업체로.
- 스프라이트: `www/img/story/park_{neutral,smile,worry,shock}.png` 32×32. `image-rendering: pixelated`.
- 순서: (1) 달력 데이터 + 기존 SEASON_MODS 대체 + 명절 이벤트, 봇으로 1년 승률 재확인 → (2) story.js + 대화창 + 3월 비트 13개 → (3) 4·5월 비트, 문자, 달력 카드, 설정 → (4) 영어 대사, 시나리오 목록 재정의(5.4).

---

## 부록. 열어 둔 질문

1. 캐릭터 최종: A 단독 / A+C 톤 혼합 / 다른 조합. 이름도 임시(박 반장·강 소장·노 기사·여 실장).
2. 스토리 모드 기본 난이도를 수습으로 둘지 정규로 둘지. 수습이면 "본편은 더 어렵다"는 안내가 작별 비트에 필요.
3. 명절 휴무 2턴이 1년 런에서 두 번(9월·2월) 오는 게 충분히 다른 느낌인지 — 설은 휴무를 3턴으로 늘려 차별화할 수도.
4. 11월 쇼핑 행사 기간에 배차비를 내릴지(업체도 대목이라 싸게) 올릴지(차가 귀해서 비싸게). 현실은 후자에 가깝지만 게임에선 전자가 "행사 = 기회"로 읽힌다.
5. 미국·일본 달력을 언제 넣을지. 틀만 먼저 만들고 데이터는 한국 달력이 봇 검증을 통과한 뒤.

## 부록 C. 구현 메모 (v1.4)

- **달력** `M.CALENDARS.kr.months[cal]` = `{ arrivalsMult, typeShift, weather(계절 가중치에 곱), heatAlerts, storageMult, storageFeeMult, returnGraceDelta, noInflation, icon, events[] }`. `D.SEASON_MODS` 삭제. `Game.calendar()/calMonth()/seasonMods()/monthEvents()/eventsAt()/isOffTurn()/isRushTurn()/calendarMonths()/returnGraceFor()/tableMonth()`.
- **이벤트** `_makeSchedule`: 태풍 이동 뒤에 이벤트 적용. `arrivalsMult`(그 턴 스펙 수 × 배수만큼 추가 생성, `typeShift` 적용된 비율로), `deadlineDelta`·`rewardDelta`는 스펙에 실려 `_spawnParcel`에서 반영, `noArrivals`는 휴무 턴 스펙을 휴무 뒤 첫 턴 앞에 붙임. `noCalls`는 `canCall`·`callCarrier`에서 거부(`err.holidayOff`), 직접 배송은 가능. `feeMult{carrier}`는 `truckFee`. `upcoming()`에 `off/events` 플래그, 계약 버튼은 휴무 표시.
- **시나리오** 전부 달력 위. 짧은 것은 `startMonth` + `monthOffset`(입고·품목·등급·가격 표를 뒤 개월차로): 성수기 11월/+3, 폭염 7월/+4, 블프 11월/+2, 데일리 = `dailyConfig().startMonth` 무작위 달/+3. 나머지는 12개월(승리 조건 스케일: 자금난 2,500, 이사철 12건, 감사 초과 12개). `half` 시나리오 삭제(`first_clear` 보상에서도 제거). 봇(40시드): 표준 23 · 성수기 83 · 폭염 90 · 블프 63 · 데일리 100 — 컷 시나리오는 아직 후함, 플레이 후 조정.
- **스토리** `www/js/story.js`: `BEATS[]`(id·months·kind·when·pages[{expr,hl,k}]·calendar), `Story.check(g, ctx)` → 비트 하나(seen 기록, notes 저장), `Story.sms(g)`(월초 문자, `cal.<id>.<cal>.sms`), `done/active`. 스프라이트 4표정은 base64 PNG 인라인(32px, 약 2KB). `turn` 비트는 호출 턴에도 나온다(kind call). 상태는 `game.story = { seen, notes, off }` → 세이브에 포함. 프로필 `story.seen`은 첫 런 자동 켜짐 판정용.
- **UI** `#story` 대화창(하단, 강조 대상이 화면 아래쪽이면 상단으로), `.story-hl` 깜빡임, `#sms` 말풍선(9초), `showCalendar()`(HUD 달 숫자 탭), 타이틀 「인수인계」(`showStoryStart`: 표준·동네 택배·무보험·퍽 없음·난이도 수습/정규·준비 마켓 없음), 퍽 화면 「창고장 안내」 토글(`prep.story`, 첫 런 기본 켜짐), 메뉴 토글 2개(안내·계절 문자 `opts.sms`), 도움말 → 「창고장 노트」. 훅: `startPlay`(start) · `afterTurn`(turn/call, `pendingCall`) · `showSummary`(summary) · `showMarket` 첫 렌더(market) · 마켓 닫기(turn/start + 문자).
- **문구** ui 키 `story.*`, `cal.*`, `err.holidayOff`, `log.holidayOff/calEvent`, `hud.off`, `fmt.calOnly`, `menu.story/sms`, `help.notes*`, `sms.from`, `prep.story*`, `title.story*`. 옛 `season.note.N`은 `cal.kr.N.note`로.
- **테스트** `test/unit.js` 55개(달력 3 + 스토리 4 추가), `test/story-shot.js`(영어, 1개월차) · `test/story-run.js`(한국어, 5개월차까지 UI로 진행, 비트·문자·노트 스크린샷) — 둘 다 `npm run serve` 필요.
- **연출(v1.4.1)** 타자 효과(글자마다 `SFX.blip`, 문장 부호에서 쉼, 탭하면 전부 표시 → 다시 탭하면 다음), 말할 때 입 벌림 프레임(`<expr>_talk`) 교대, 표정 6종(기본·웃음·걱정·놀람·생각·큰웃음), 다 보이면 ▼ 커서. **강제 클릭 게이트**: 페이지에 `gate`가 있으면 대화가 닫힌 뒤 `#story-gate` 오버레이가 hl 대상만 통과시키고(좌표 판정 → `target.click()`), 「여기를 눌러」 손가락 라벨 + 다른 곳 누르면 흔들림. intro→대기, callReady→가장 찬 계약, noContract→대기. 스프라이트 생성은 `tools/sprites.py`.
- **남은 것** 강 소장(베테랑 안내자), 미국·일본 달력 데이터, 컷 시나리오 봇 승률 조정, 영어 대사 톤 다듬기, 12개월 시나리오 승리 조건 수치 검증.

## 부록 D. 운송센터 · 배차 소모품 · 조연 (v1.5)

유저 방향: 업체를 "파손 전문" 같은 기능 이름이 아니라 **특색 있는 운송센터**로; 상위 등급은 같은 센터의 업그레이드가 아니라 **다른 센터와 신규 계약**(새 고객이 나타나듯); 배차 충전은 **가득 채우기만**이라 다 쓰지 않고 충전하면 손해; 마켓은 현재 계약이 맨 위 + 계약 상세; 나머지 캐릭터는 센터 담당자 + 스토리 조연.

- **데이터** `D.FAMILIES`(계열 9: 기본 스탯·rep) + `D.TIERS`(등급 공통 보정: 배차 +0/2/4/6, 용량 +0/1/2/3, 배차비 ×1/0.9/0.8/0.7, 계약가 ×1/2.2/4/7) + `D.CENTERS`(센터 27: family·tier·extraCaps·sizeMax·delay·simul) → 로드 시 `D.CARRIERS[센터 id]`로 합성(`id/family/tier/grade/refill`). 헬퍼 `D.centersOf(fam)`, `D.centerFor(fam, tier)`, `D.familyOf(id)`. 계약 `c.carrier`는 센터 id, `c.grade`는 tier 이름(유지비·배지). 신뢰도·특성 문구는 계열(`TRUST_PERKS[family]`), xp는 센터별. 회사 시작 계약·보장·가중치·이벤트 배차비는 계열 이름으로 쓰고 `famVal()`로 센터에 적용. 옛 세이브의 계열 id는 `fromJSON`에서 tier에 맞는 센터로 이관.
- **복합 능력** 푸른길(대량 2)·메가허브(대량 3, 동시 2대) ⚠ 안전 / 콜드체인(냉장 2)·극지(냉장 3, 크기 7) ❆ / 영하 30(냉동 2) ❄ / 완충 마스터즈(파손 2) 크기 7 / 백자(파손 3) ❄ / 월드게이트(통관 2) ⚠ / 코끼리(대형 2) ❄ / 제트(항공 1) 크기 4 / 고속철 즉시 입금 / 대양 지연 1턴.
- **배차 = 소모품** `_startMonth`의 월초 리셋 삭제. 마켓에 계약마다 `kind: 'refill'` 상시 카드(`refillPrice(c)` = 센터 `refill` × 물가 × 인플레), `Game.refill(id)`는 `calls = maxCalls`, 남은 배차는 버려짐(`log.refillWasted`). 충전은 월 구매 한도(5)에 안 들어간다. 배차 추가·업그레이드 모드 삭제(`buy(idx, slot)`만). 계열 풀 상향: 대량 7·냉장 4·냉동 4·파손 4.
- **마켓** 보유 계열은 더 높은 tier 센터만 등장(`switchFrom` = 갈아탈 계약 id, 확인 모달 `slot.switchAsk`: 잔여 배차 소멸·신뢰 0부터). 현재 계약 칸이 맨 위: 카드마다 「상세」(`showContractDetail`: 담당자 얼굴·계열·능력·차량·배차비·배차·충전가·신뢰 트랙·강화·실적) + 「충전 Nc」(남은 배차가 있으면 손해 경고 확인). 카드 설명에 `mk.tierVs`(계열 표준 대비)·담당자.
- **조연** `Story.REPS`(여 실장=대량, 노 기사=대형·철도, 강 소장=냉장·냉동, 이름 없는 담당자=그 외; neutral/talk/smile). 계약 체결 직후 `Story.greet(g, c, from)` → 담당자 인사(`rep.greet.<family>[.<tier>]`, 갈아타기면 `rep.switch` 먼저). 스토리 조연: firstCall 2페이지 담당자, rain 1페이지 노 기사, loss 2페이지 강 소장. 페이지에 `speaker`가 있으면 대화창 이름·스프라이트가 바뀐다(`Story.sprite(speaker, expr, talk)`).
- **봇** `marketBot`: 충전(0대 또는 75% 이하) → 한도 강화 → 갈아타기 → 빈 슬롯 새 계약 → 적재. 힌트 계약은 switchFrom 슬롯으로. 결과(30시드): 표준 0%(평균 6.4개월, 자금 6천 남기고 스트레스로 죽음 — 봇이 용량·창고를 못 늘림), 성수기 70·폭염 100·블프 83. **사람 플레이테스트로 재조정 필요**: 배차 풀·충전가·tier 등장 확률.
- **남은 것** 항공·해상·통관 담당자 얼굴, 도감 센터 탭에 담당자, 강 소장 베테랑 안내, 스토리에서 첫 충전 유도 비트(게이트).

## 부록 E. 팝업 안 게이트 · 조건부 비 비트 (v1.5.1)

유저 지적: "급한 순으로 눌러"라고 말하는데 그 버튼은 호출 팝업 안에 있어서 안 보인다 / 창고가 안 찼는데 "밖에 둘 물품을 고르라"는 비 비트가 헷갈린다.

- **팝업 비트** `kind: 'modal'` + `modal: 'call' | 'wait'`. 팝업이 (다시) 그려질 때마다 `storyCheck({ kind: 'modal', modal, sel/picked, elig, outdoor })`를 부르므로, 선택 수가 바뀌면 다음 비트로 이어진다. 게이트 오버레이(z 59)가 모달(z 50) 위에 있으니 팝업 안 버튼도 그대로 강제 클릭 대상이 된다. `closeModal()`은 대상이 사라진 게이트를 정리한다.
- **호출 체인** `callReady`(계약 카드 게이트) → `callModal`(sel 0: `#pick-urgent` 게이트) → `callGo`(sel > 0: 목록 설명 → 하단 호출 버튼 `#modal .foot .btn.primary` 게이트). '급한 순 자동 선택' 설명은 callReady.2에서 callModal.1로 옮겼다.
- **대기 체인** `noContract`(대기 버튼 게이트) → `waitSelf`(picked 0: 첫 택배 `#modal .zone .parcel` 게이트) → `waitGo`(picked > 0: 대기 버튼 게이트). noContract 를 본 뒤에만.
- **비 비트는 조건부** `rain`: `outdoorVolume() > 0` 이고 비(현재 또는 예보)일 때만 — 마당에 나가 있는 게 없으면 나오지 않는다. 문구도 "지금 마당에 {outdoor}칸 나가 있잖아"로. 2페이지가 대기 버튼 게이트 → `rainReorder`(대기 팝업의 `#wm-reorder` 게이트). 3개월 안에 조건이 안 걸리면 그냥 안 나온다(창고장 노트에도 없음).
- **테스트** unit 56개(팝업·비 조건 1개 추가). Playwright `passGate()`는 게이트가 이어지는 동안 반복해서 대상을 누르고 그 사이 비트를 읽는다(계약 → 자동선택 → 호출 3단 게이트 확인).

## 부록 F. 온보딩·정보 위계 1차 (v1.6, b5f7ff3)

유저가 받은 외부 피드백: **"글이 너무 많고 정보가 산발적이라 무슨 게임인지 모르겠다"**가 가장 많았다.
실측(5개월차 1턴, 375px): 한 화면에 숫자 91개·텍스트 726자·서로 다른 정보 구역 7개,
그중 대기 미리보기 줄과 입고 예정 줄이 잘려 있었다. 원인을 셋으로 나눠 각각 손봤다.

**① 정체성 — 게임이 자기가 뭔지 말한 적이 없다**
- 타이틀 부제 `title.sub`를 장르명("턴제 물류 경영 로그라이크")에서 동사로: "쌓아뒀다가, 트럭을 꽉 채워 보낸다". 그 아래 `title.goal` 한 줄로 승리 조건.
- `showHowTo(next, back)` — 첫 실행 3장 카드(택배가 쌓인다 / 트럭을 꽉 채워 보낸다 / 1년을 버틴다). 픽셀 아트는 CSS 박스(`.how .art`)로 그려 에셋 없음. 프로필 `howSeen`으로 한 번만 자동, 이후 「게임 방법」에서 다시 보기. 인수인계·새 런 양쪽 시작 경로가 `withHowTo(start)`를 거친다. 문구 `how.*`.
- **계약 카드에 개당 단가**. 이 게임의 핵심 긴장(호출 1회 정액 → 꽉 채울수록 개당 싸짐)이 UI 숫자로 드러난 적이 한 번도 없었다. `loadPreview(g, c, elig)`가 호출 팝업의 '급한 순 자동 선택'과 같은 규칙(`autoPick(c, sorted, 1)`)으로 한 대 적재를 계산해 `개당 Nc`를 띄운다. 80% 이상이면 금색.
- `story.intro.1`에 승리 조건을 올림. 기존에는 3개월차 비트 `win`(#25)에서야 나와서, 플레이어가 목표를 모르는 채 3개월을 배우는 구조였다.

**② 결정 — 화면이 상태만 보여주고 계산은 플레이어에게 넘겼다**
매 턴의 결정은 하나(부를까 기다릴까, 부르면 어느 차)이므로 화면이 위에서 아래로 (1) 지금 급한 게 있나 (2) 지금 부르면 얼마나 차고 개당 얼마인가 (3) 기다리면 어떻게 되나 순으로 답하게 재배치.
- 계약 카드 = 적재 게이지(`.contract .lg`) + `N/M칸 · 개당 Nc` + 특수 표기(×N, ⏱N, 정기배차). 신뢰도 바(`■■■ 10/15`)는 카드에서 빼고 「상세」로 (`showContractDetail`에 이미 트랙이 있다).
- 대기 버튼의 다음 턴 미리보기를 `--dim` → `--ink`로 올리고 위험 항목을 `.wrisk`로 강조. 계절 문자 말풍선(`#sms`)이 `bottom: 8px`라 액션 영역을 덮던 것을 `#actions` 높이만큼 띄움.
- 코치 줄 적재율이 100%를 넘어 "214%까지 찼어"로 나오던 것 수정(`Math.min(100, …)`).

**③ 글 — 같은 정보가 여러 줄로 흩어져 있었다**
- **택배 줄 묶기**: `groupKeyOf(p)` = 종류|속성|야외|**상태 문구**. 상태까지 키에 넣으므로 묶여도 잃는 정보가 없다. `GROUP_MIN 2`, 누르면 `openGroups`에 들어가 `.gchild`로 펼쳐진다. 호출/대기 팝업(`selectable`)에서는 묶지 않는다. 실측 16개 → 6줄.
- HUD 자금 분해(`#hud-due`)는 기본 접기, `#hud-cash-box` 탭으로 펼침(`hudDueOpen`, `#hud-more` ▾/▴).
- `#upcoming`: `white-space: nowrap; overflow-x: auto`로 오른쪽이 잘리던 것을 `flex-wrap`으로, 앞 2턴만(`up.slice(0, 2)`, 기획서 4장 원칙), 월말 정산 칩 중복 제거.
- `#scene` 36% → 29%(짧은 화면 30% → 25%). 화면을 제일 많이 먹는 영역이 전달하는 정보가 0이었다.

**검증** Playwright(390×800, ko)로 타이틀→3장 카드→인수인계 게이트→8턴 진행까지 콘솔 에러 0, 단위 60개 통과. 스크린샷 `Claude outputs/onboarding-after.png`.

**남은 것**
- 3D 뷰를 정보로 쓰기(급한 택배 깜빡임 등) — 이번엔 높이만 줄였다.
- 용어: `배차`, `×2`, `⏱N`는 여전히 설명 없이 나온다(상세에만 있음).
- 마켓 화면은 이번 패스에서 손대지 않았다 — 5개월차 마켓도 텍스트 밀도가 높다.
- 이 변경들에 대한 사람 플레이테스트.

## 부록 G. 마켓 화면 정보 위계 (v1.6.1, ee63114)

부록 F에서 플레이 화면만 손봤고 마켓은 남겨 뒀었다. 4개월차 마켓 실측(375px):
**숫자 151개 · 텍스트 1,409자 · 2.3화면 스크롤** → **86개 · 833자 · 2.0화면**.

**현재 계약 (4장)**
- 제목의 `배차 2/7대`를 본문(`mk.contractLine`)에서 또 쓰고 있었다. 본문은 용량·신뢰도만, 그리고 버튼과 같은 줄(`.card .crow`)로 내려 카드당 3줄 → 2줄.
- `mk.refillWaste`("아직 N대 남음 — 지금 충전하면 N대 손해")를 카드에서 제거. 배차가 남아 있으면 거의 항상 떠서 주황색이 의미를 잃었다. 충전을 실제로 누르면 나오는 `askConfirm`에 이미 같은 문구가 있다.
- `mk.currentContracts` 헤더가 두 줄짜리 규칙 설명이었다 → "현재 계약 — 배차는 여기서만 충전".

**마켓 계약 카드**
- 살지 말지는 **"월에 몇 칸을, 칸당 얼마에"**로 정해지는데, 그 수치가 산문 사이에 흩어져 있었고 `프리미엄 — 배차 +1·용량 +1·배차비 -10%`와 `계절 표준 대비: 14→15칸 · 2→4대 · 83→75c/대`가 같은 말을 두 번 했다.
- `offerSpec(it)`(아직 안 산 계약의 실효 수치) + `mk.capLine` → `컨테이너 15칸 × 월 4대 = 60칸 · 대당 75c (칸당 5c)` 한 줄. 플레이 화면의 `개당 Nc`와 같은 언어다.
- 산문 desc·`mk.tierVs`·담당자·신뢰 트랙은 새 `showOfferDetail(it, back)`(「상세」 버튼)로. 카드 6줄 → 3줄.
- 12장이 구분 없이 늘어서던 것을 종류별 헤더(`.mkhead`, 계약/강화/시설/고객/보험)로 나눔. `kind` 라벨을 카드 제목에서 뺐다.

**그 밖**
- 다음 달 예상 물량은 합계 한 줄(`mk.forecastSum`)로 접고 고객별 내역은 `fcOpen` 토글로.
- 계약 카드의 `×N`, `⏱N` → `동시 N대`(`hud.simulN`), `입금 N턴 뒤`(`call.payLater`).

**버그 — 확인 모달의 '취소'가 원래 화면까지 닫았다**
`askConfirm(msg, onYes, yesLabel)`의 취소가 언제나 `closeModal`이라, **마켓에서 배차 충전을 취소하면 마켓 모달이 닫히고 `phase === 'market'`인 채 플레이 화면으로 떨어졌다.** 보험 가입 취소, 도감 기록 초기화 취소, 메뉴의 런 포기 취소도 같은 증상. `askConfirm(..., onNo)`를 받아 호출부가 돌아갈 화면(`render` / `showInsurance(back)` / `showCodex('stats', back)` / `showMenu`)을 넘기게 했다.

**검증** Playwright로 예상 물량 토글 · 마켓 계약 상세 · 현재 계약 상세 · 충전 확인(취소 후 마켓 유지) · 카드 탭 구매까지 에러 0, 플레이 8턴 에러 0, 단위 60개 통과. 재현용 세이브는 `test/sim.js`의 봇으로 4개월차 마켓까지 진행해 만들었다.

**남은 것** 3D 뷰를 정보로 쓰기(부록 F에서 높이만 줄임), 정산 화면, 사람 플레이테스트.

## 부록 H. 정산 화면 · 3D 뷰 (v1.6.2, 7b21054)

부록 F·G에서 플레이·마켓을 손봤고, 남아 있던 두 화면.

### 정산

실측(4개월차, 375px): 1.0화면 · 숫자 43개 · 15줄이 전부 같은 무게. 길이가 문제가 아니라 **결론이 없는 것**이 문제였다.

- **이번 달 순익이 화면 어디에도 없었다.** `+2395 / -477 / -522 / -42`를 늘어놓고 덧셈은 플레이어에게 맡겼고, `현재 자금 1903c`는 변화량을 말해 주지 않는다. `_startMonth`에서 `monthStats.cashStart`를 찍고 `summary.net = cash - cashStart`(차입·상환 블록 **전**에 계산 → 순수 영업 결과)를 넣어, 화면 맨 위에 `.sumnet` 한 덩어리로: **이번 달 순익 +1354c** / `549c → 1903c`. 적자면 빨강. `winCash` 목표도 여기로.
- 수입 / 지출 / 사고 / 이번 달·상태 / 고객 으로 섹션 분리(`.sumsec`).
- 사고가 하나도 없으면 `이번 달 페널티 +0` · `반송/도난/파손 0/0/0` · `손해배상 -0` · `부패 폐기 0개` 네 줄 대신 `✔ 사고 없음` 한 줄. 좋은 소식에 네 줄을 쓸 이유가 없고, `-0` 표기도 같이 사라진다. 사고가 있으면 0이 아닌 줄만 나온다.
- 중복이던 `현재 자금` 줄은 머리말의 자금 흐름에 흡수.

검증: 흑자(4개월차 +1354)·적자(1개월차 −179, 반송 2·페널티 9·배상 275) 두 경우 모두 봇 세이브로 렌더 확인.

### 3D 뷰

부록 F에서 높이만 29%로 줄이고 "정보로 쓰기"는 남겨 뒀던 것.

- 기존에는 `userData.overdue`(기한 초과·부패 임박)만 빨갛게 맥동했다. **이미 늦은 것만 알려 주는 표시는 결정에 쓸 수 없다.** `userData.urgent`(초과 아님 · 통관 대기 아님 · `deadline <= 1`)를 추가해 호박색 맥동.
- `userData.exposed`(마당에 나가 있고 비·눈이 현재 또는 예보) → 차가운 파란 맥동. 젖으면 보상 −20%인데 3D에서는 구분이 안 됐다.
- **시도했다 버린 것**: 마당 바닥에 경고 테두리(`_syncYard`). 실제 화면 높이(29%)에서는 카메라가 마당 바닥을 잘라 내서 보이지 않았다 — 70%로 늘려야 겨우 보인다. 화면에 남는 건 상자뿐이므로 표시를 상자로 옮겼다. **이 뷰에 바닥·구역 단위 표시를 더 얹는 것은 이 카메라·높이에서는 의미가 없다**는 결론.

검증: 정상 상태(1개월차 4턴)에서는 아무것도 깜빡이지 않고, 마당 노출 + 초과 + 기한 1턴을 강제한 상태에서 세 색이 모두 보이는 것을 프레임 두 장으로 확인. 에러 0, 단위 61개, sim 60시드 통과.

### 남은 것
사람 플레이테스트(유저가 진행). 도움말 본문(`help.body`)은 아직 차량·계약 언어로 갱신되지 않은 문단이 남아 있다.

## 부록 I. 튜토리얼 유도 · 결정론 대본 (v1.7)

유저 관찰: **"튜토리얼에서 많이 막히고, 사람들이 새 런부터 시작해 버린다."** 둘을 같이 고친다.

### ① 인수인계로 유도 (막지 않는다)

- 타이틀 「새 런 시작」에 인수인계를 한 번도 안 본 프로필(`Profile.story.seen` 없음)이면 `title.newHint`("인수인계 먼저 권장")가 붙고, 눌렀을 때 `suggestStory()` 모달이 먼저 뜬다 — 기본 버튼이 「인수인계 시작」, 보조가 「그래도 새 런」. 잠그지 않는 이유는 v1.5.1과 같다(첫 번만 게이트, 그 뒤엔 신호로).
- `Profile.story`에 `done` 추가: 작별 비트(`farewell`)가 나오면 기록된다. `seen`(인수인계를 시작했다)과 `done`(끝까지 봤다)을 나눠 뒀다 — 지금 유도 조건은 `seen`이고, 더 강하게 밀고 싶으면 `done`으로 바꾸면 된다.

### ② 1~3개월차 물류 대본 (`www/js/tutorial.js`)

무작위 물류 위에서는 **"지금 차를 두 대 불러 봐", "배차가 떨어졌으니 마켓에서 충전해"를 말할 수가 없다.** 그래서 「인수인계」로 시작한 런(`cfg.scripted`)의 1~3개월차는 입고·날씨·마켓 매물을 고정한다. 4개월차부터는 평소대로 무작위. 일반 런의 「창고장 안내」 토글은 대본을 쓰지 않는다(회사·시나리오가 제각각이므로).

- `TUTORIAL.SEED` 로 시드까지 고정 → 파손·도난·통관 지연 굴림도 매번 같다.
- 훅: `Game.script(m)` 하나로 `_makeSchedule`(입고) · `_genWeather`(날씨) · `_genMarketItems`(매물) · `_startTurn`의 보관 제안을 갈아끼운다. **규칙 엔진은 손대지 않았다** — 대본은 "무엇이 오는가"만 정하고 계산은 그대로다.
- 난이도(수습/정규)는 그대로 얹힌다: 물류는 같고 배차비·운영비만 달라진다.

대본이 만드는 자리:

| 달 | 턴 | 무엇이 오는가 | 가르치는 것 |
|---|---|---|---|
| 1개월차 | 1~3턴 | 일반 2·2·3칸 = **정확히 7칸** | 첫 차는 +1칸(동네 택배) → 100% 적재 |
| | 4~6턴 | 3·1·2칸 = **정확히 6칸** | 두 번째 차도 딱 맞게 |
| | 8턴 | 한 턴에 8칸 → 재고 **정확히 12칸** | **차 두 대가 자동으로 붙는 순간**(신뢰 1단계로 동시 2대가 열린 직후) |
| 2개월차 | 2·4·7·9턴 | ⚠ 파손 4개 vs 시작 배차 2대 | 배차 소진 → 월말 마켓 충전 |
| | 5턴 | 이사 보관 제안(고정) | 공간을 파는 계약 |
| 3개월차 | 3~4턴 | 한 번에 24칸 초과 + 같은 턴 비 | 야외 적재·젖음·적재 정리 |

마켓 매물도 고정이라 "배차를 늘리는 세 가지"를 순서대로 꺼낼 수 있다.

| 마켓 | 매물 | 안내 비트 |
|---|---|---|
| 1개월차 | 충전(상시) · 한도 강화 `limit1` · 창고 확장 | `marketRefill`(정액이라 다 쓰고 채워라) → `marketLimit`(그릇을 키운다) |
| 2개월차 | 충전 · `limit2` · 냉장 시설 · **⚠ 상위 센터 갈아타기** | `marketSwitch`(배차·용량↑, 잔여 배차 소멸·신뢰 0부터) |
| 3개월차 | 충전 · `cap1` · 확장 · 대량 상위 센터 | — |

1개월차 마켓에 **계약 카드를 일부러 안 넣었다**: 바로 직전에 신뢰 1단계로 배운 "동시 2대"가 갈아타기로 날아가면 배운 게 거짓말이 된다. 갈아타기는 2개월차부터.

### ③ 새 비트 3종

- `trucks2` (호출 팝업): 차가 두 대 붙은 이유와 배차·배차비가 두 배로 나가는 것. ctx에 `trucks`·`vol`을 넘기도록 호출 팝업의 `storyCheck`를 고쳤다.
- `callsOut` (턴): 보낼 건 있는데 배차가 0 → "배차는 월초에 안 채워진다. 마켓에서 충전한다."
- `marketRefill` / `marketLimit` / `marketSwitch` (마켓): 마켓은 이제 렌더마다 `storyCheck`를 부른다(호출 팝업과 같은 방식) — 충전을 누르면 다음 안내로 이어진다. 짚을 수 있도록 마켓 카드에 `#mk-card-<kind>-<id>`, 충전 버튼에 `#mk-refill-<슬롯>` id를 붙였다. 충전 게이트는 **배차가 0일 때만** 건다(2대 남은 걸 억지로 충전시키면 방금 가르친 "다 쓰고 채워라"와 모순).

### 검증

- `test/unit.js` 66개(대본 5개 추가): 시드·입고·날씨 고정, 4개월차는 무작위, 일반 런은 대본 없음, 대본 마켓 구성, `callsOut` 비트.
- `test/tutorial-run.js`(신규, node): 대본 3개월을 봇으로 굴려 턴별 창고·배차·자금과 "2대 동시 호출 / 배차 0 / 야외 적재"가 실제로 오는지 찍는다. 수습 +358 → +711 → +761c, 반송 0~2(안내를 무시하고 충전을 안 했을 때만).
- `test/tutorial-ui.js`(신규, Playwright 390×800 ko): 타이틀 → 새 런 권유 모달 → 인수인계 → 3개월차 완주. 콘솔 에러 0, `intro·callReady·trucks2·marketRefill·marketLimit·marketSwitch·callsOut` 모두 화면에서 확인. 1개월차 8턴에 "차량 2대 · 적재 12/12칸"이 정확히 재현됐다.

### 남은 것

- 사람 플레이테스트(유저 진행). 특히 3개월차 3~4턴의 창고 초과가 겁주는 정도인지 배우는 정도인지.
- 대본 개월차에 `story.off`(안내 끄기)로 들어간 플레이어는 대본만 남는다 — 그대로 둘지, 안내를 끄면 무작위로 되돌릴지.

## 부록 J. 시간 단위 — 하루 1턴 · 일요일 휴무 (v1.7.1)

유저 지적 세 갈래. ① 온보딩 카드 문장이 구어체·비문이고 조사가 틀렸다("밖은 젖고", "기다리는 턴이", "창고가 터지면", "한길 물류**은** … 나가거든"). ② UI의 '턴'이 몰입을 깬다 — 박 반장은 이미 "오늘·이틀 더"로 말하는데 화면만 턴이다. ③ (1턴 = 평일 2일로 바꿔 본 뒤) **기한이 8일에서 6일로 2씩 줄어드니 더 헷갈린다.**

**① 문구·조사**
- `how.1.d` / `how.2.d` / `how.3.d` / `story.intro.3` 수정. "한길 물류는 한 대에 38c씩 **비용이 들어**" — 주어(업체)와 서술어(나가다)가 어긋나던 문장을 고쳤다.
- i18n 에 **조사 자동 선택**: 문구에 `{name}은/는`처럼 써 두면 값의 받침으로 고른다(`은/는`·`이/가`·`을/를`·`과/와`·`으로/로`). 숫자는 읽는 소리 기준(2·4·5·9만 받침 없음), 끝의 HTML 태그·괄호는 떼고 판단. 계약·센터 이름이 데이터로 바뀌므로 박아 두면 언제든 틀린다.

**②③ 시간 단위 — 하루가 한 턴**

거쳐 간 안과 버린 이유:

| 안 | 버린 이유 |
|---|---|
| 월 20·30영업일로 확장 | 봇 실측 월 호출 7.4 / 대기 2.6 — 턴만 늘리면 **결정 밀도가 1/3로** 떨어진다. 물량을 같이 늘리면 창고·배차·자금을 전부 3배로 다시 잡아야 한다 |
| 1턴 = 평일 2일 (달력 날짜 표기) | 달력은 맞아떨어지는데 **기한이 2씩 줄어** 읽기가 더 어려워졌다 |
| 1턴 = 하루 + 진짜 날짜(월 24영업일) | 런이 288턴 |

채택: **1턴 = 하루(영업일). 월~토 엿새 일하고 일요일 휴무. 한 사이클('달') = 2주 = 영업일 12일.** 런 길이는 12턴 × 12사이클 = 144턴으로 기존(120턴)과 비슷하고, 기한·통관·입금이 전부 **하루 단위로 1씩** 줄어 읽기가 단순해졌다.

- **날짜(1~31일)는 쓰지 않는다.** 화면에는 `3월 6일차 토`처럼 **몇 일차 + 요일**만 나온다. 한 사이클이 2주라는 사실이 날짜와 충돌하지 않고, 요일이 있어 "토요일까지 일하고 일요일에 쉰다"는 리듬이 그대로 읽힌다.
- `D.TURNS_PER_MONTH 12` · `TURNS_PER_WEEK 6` · `WEEKEND_AFTER [6, 12]` · `DAYS_PER_TURN 1`.
- `Game.yearOf/weekOf/dowOf/dowName/dateLabel/dateDowLabel`. **연도**는 런 시작 시 `new Date().getFullYear()`를 찍어 세이브에 고정(3월 시작이라 1·2월은 +1년).
- 문구는 전부 날짜 단위로(ko 122곳, en 동일). 기간은 i18n 자리표시자 `{n#d}`로 렌더 — 지금은 1배지만 단위를 다시 바꿀 때를 위해 남겼다. "다음 턴"류는 "다음 영업일", 턴 번호는 `{n}일차`.
- 달력 이벤트도 12턴 기준으로 재배치: 명절 폭주 3~5일차, **연휴 6~7일차 = 토·일·월** (일요일을 사이에 끼워 진짜 연휴 모양), 5월 선물 주간 6~9일차, 11월 행사 주간 8~12일차.

**일요일 (`phase: 'weekend'`)**
턴이 아니다. 입고·호출·기한 진행 없음(기한은 영업일 기준), 보관 기간도 멈춘다. **마당에 둔 것만 도난 판정을 한 번 더** 받는다 — `_endTurn`의 도난 블록을 `_theftRoll(reasons)`로 빼서 일요일이 재사용한다. 둘째 일요일(12일차 뒤) 다음은 바로 월말 정산이다. 선택지 하나:

| 선택 | 효과 |
|---|---|
| 휴식 | 스트레스 −1 |
| 야근 | 다음 영업일 직접 배송 +2, 스트레스 +1 (`weekendBonus`, `_endTurn` 에서 소멸) |
| 주말 알바 | 50c × 물가 · 이번 일요일 도난 판정 없음 (마당이 비어 있으면 선택 불가) |

스트레스 회복이 월말 −2 한 번뿐이라 봇이 한계(20)에 붙어 죽던 문제에 밸브가 생겼다.

**명절 연휴 — 현실대로**
`holiday_off`에서 `noArrivals`를 걷어냈다. 현실에서 연휴에 멈추는 건 **배송**이지 창고가 아니다(노동절이 법정 공휴일이 돼도 택배는 정상 배송, 업계가 통째로 쉬는 건 연 1회 '택배 없는 날'과 설·추석 연휴 정도). 이제 연휴에도 입고는 그대로 들어오고 차만 못 부른다 — 연휴 전 폭주와 합쳐져 압박이 제대로 걸린다.

**튜토리얼** 대본(부록 I)을 12턴으로 늘렸다(각 달 뒤에 이틀씩). 1개월차 3일차 첫 호출 · 6일차 둘째 차 · 8일차 두 대 동시는 자리가 그대로다. 일요일 비트 둘: `weekend`(왜 못 부르는지 + 선택 게이트), `weekendYard`(마당에 물건이 나가 있을 때 알바를 짚어 준다).

**UI** HUD `🏠2026 3월·1개월차 6일차 토`, 입고 예정·날씨 칩은 `{n}일차`, 이번 영업일이 끝나면 `🛌 일요일` 칩이 붙어 마당을 비우라는 신호가 된다. 일요일 카드는 모달(선택지 3개).

**검증** 단위 68개, 봇 40시드 전 시나리오(표준 평균 6.1개월), Playwright 스모크 `test/smoke.js` 37항목 + 인수인계 3개월차, 콘솔 에러 0.

**남은 것** 사람 플레이테스트. 스트레스를 '평판/유명세' 같은 **키우는 지표**로 뒤집는 안은 별도 과제(유저 제안).

## 부록 K. 스트레스 → 평판 (v1.8)

유저 지적: **"안정이 차면 게임이 끝난다는 건데, 그럼 왜 끝나는 걸까."** 지표 이름(`안정`)은 단계 이름이고 화면 어디에도 '스트레스'라는 말이 없었다. 게다가 창고 사장이 스트레스로 쓰러진다는 건 이 게임이 하는 이야기도 아니다. 방향을 뒤집는다.

**평판 — 키우는 지표, 0이면 끝**

- 시작 = 난이도별 1단계 상한(수습 24 · 정규 20 · 베테랑 16). 전임 창고장이 물려준 평판에서 출발한다.
- **깎는 것**(기존 스트레스가 오르던 자리 그대로): 반송 −2 · 도난 −2 · 파손/부패 폐기 −2 · 창고 초과 −1~−2 · 기한 초과(시나리오) −1.
- **올리는 것**(새로): 차를 80% 이상 채워 보내면 +1 · 고객 신뢰 단계 상승 +2 · **사고 없이 정산을 넘기면 +2**.
- **0 이하 → 런 종료.** "평판이 바닥났습니다 — 이제 아무도 여기에 맡기지 않습니다." 이제 끝나는 이유가 게임의 이야기 안에 있다.
- **등급과 상한**: 상한까지 채운 채로 정산을 넘기면 등급이 오르고 상한도 오른다 — 무명 20 → 동네 소문 35 → 구내 유명 50 → 시내 최고 70(`D.REP_TIERS`). 이게 '키우는 맛'이다.
- **개인 고객 물량 연동**: `repArrivalMult() = 0.55 + 0.45 × (rep / cap)` 을 그 달 추가 입고량에 곱한다. 상한이면 1.0배(기준), 바닥이면 0.55배 — 평판이 낮으면 일감부터 마른다. (처음엔 익명 고객 *가중치*를 건드렸는데, 그러면 품목 구성까지 흔들려 보상이 58% 뛰었다. 양만 건드린다.)
- `Game.rep / repTier / repCap() / repTierId() / repTierName() / addRep(n, why) / repArrivalMult()`. 규칙 키 이름(`gameoverStress`, `monthlyStress`)은 그대로 두되 의미가 바뀌었다 — 전자는 1단계 상한, 후자는 정산마다의 평판 증감.

**UI** 오른쪽 위 게이지가 `평판 ▓▓▓ 20/20`. 높을수록 좋고 낮을수록 빨개진다(색 반전). 등급 이름은 1단계를 넘겼을 때만 라벨에 붙여 HUD 한 줄을 지킨다. 정산에 `평판 19/20 무명 · −1` 줄. HUD 왼쪽에서 `·N개월차`를 뺐다 — 연도·월·일차·요일이 한 줄에 들어가야 해서다(개월차는 달력 화면과 정산에 있다).

**튜토리얼** 비트 `rep` 2페이지 — 평판이 처음 깎인 순간에 게이지를 짚어 "무엇이 깎고 무엇이 올리는지, 0이면 끝난다"를 말한다.

**밸런스(봇 40시드)** 표준 생존 0% → 5%, 평균 6.1개월 → **9.9개월**. 상한이 70까지 자라니 중반 이후 버틸 여지가 생겼다. 반면 자금이 12,000c까지 쌓여 후반 경제가 헐거워졌다 — **사람 플레이테스트 뒤 재조정 대상**.

**검증** 단위 68개, 봇 40시드 전 시나리오, Playwright 스모크 39항목(콘솔 에러 0).

**남은 것** 평판 등급으로 여는 언락 — 새 고객사 계약이 마켓에 뜨는 조건, ❆ 냉동·🛃 통관 품목이 들어오기 시작하는 조건을 지금의 "N개월차부터"에서 평판 등급으로 옮기는 작업. 2주 단위(바이위클리) 사이클 재편과 함께 간다.

## 부록 L. 바이위클리 사이클 · 런 길이 세 가지 · 평판 스케일 (v1.9)

유저 방향 둘. ① **"월단위가 아니라 바이위클리로 가자"** — 사이클은 2주다. ② **"돈이 확 늘어나는 건 괜찮은데 그만큼 스케일러블하게 가는 것 같지 않다"** — 후반에 자금만 쌓이고 판은 그대로였다.

**진짜 달력 · 반월 정산**

- `D.CYCLES_PER_MONTH = 2`. `Game.month` 은 **사이클** 번호이고, 사이클은 **반월**이다 — 전반 1~15일, 후반 16~말일(`D.HALF_SPLIT`).
- **날짜가 진짜다.** 달마다 길이가 다르고(2월 28 / 4·6·9·11월 30 / 나머지 31), 일요일은 턴이 아니라서 **사이클 길이가 12~14턴으로 달라진다**(`Game.turns(c)`, `businessDays(c)`). 화면에는 `2026 3월 6일 토`처럼 실제 날짜와 요일이 나온다.
- **요일도 진짜다.** 처음엔 "런 첫날이 언제나 월요일"로 고정했는데(연도별 밸런스 흔들림을 막으려고), 유저 지적 — **로그라이크니까 그 변동은 받아도 된다**. 대신 시나리오가 **연도와 지역(달력)** 을 줄 수 있게 하고, 대본이 걸린 **인수인계만 해를 고정**한다(`TUT.YEAR = 2027` — 그 해 3월 1일이 월요일이라 전반 13영업일이 대본과 딱 맞는다).
  - `monthLen` · `dowOfDate` 는 `Date.UTC` 로 실제 달력을 읽는다 — **윤년 2월 29일**도 그대로.
  - `rules.year` / `cfg.year` 로 해를 지정한다. 없으면 런을 시작한 해.
  - 시나리오 카드에 `📅 2026년 · 한국 달력` 을 띄워 어느 달력으로 도는지 보여 준다.
  - 연도별 편차(봇 15시드, 표준): 평균 사이클 8.9~9.5, 현금 15,878~20,211 — 로그라이크 변동 범위 안이다.
- 인플레·가격표·품목 비율표는 **개월차** 기준이라 두 사이클마다 한 칸 움직인다. 달력 이벤트에는 `half` 를 달았다 — 추석 연휴·쇼핑 행사는 후반, 5월 선물 주간은 전반. 그래야 한 달에 한 번만 온다.
- `season.sms`(계절 문자)는 전반 첫날에만. 정산·마켓 제목은 `3월 후반 정산`.
- 대본(`tutorial.js`)·날씨 배열이 사이클보다 짧으면 뒤를 비우고 채운다(`_makeSchedule`/`_genWeather`).

**런 길이 세 가지** — `months` 는 이제 사이클 수다.

| 시나리오 | 사이클 | 실제 | 해금 |
|---|---:|---|---|
| 분기 | 6 | 3개월 (72턴) | 처음부터 |
| 반기 | 12 | 6개월 (144턴) | 첫 클리어 |
| 한 해 | 24 | 12개월 (288턴) | 반기 클리어 |

**인수인계(튜토리얼)는 분기 런**이다 — 3개월을 박 반장과 끝까지 가고 그대로 런이 끝난다. 작별 비트는 3개월차 마지막 정산으로 옮겼다(`kind: 'summary'`). 데모 컷도 여기에 맞춰 6사이클. 대본(`www/js/tutorial.js`)은 **여섯 사이클 전부**를 덮는다(부록 M).

**평판이 난이도 스케일러**

돈이 느는데 판이 안 커지는 문제의 답을 달력이 아니라 평판에 걸었다. 등급이 오르면:

| 등급 | 상한 | 입고 | 운영비 | 열리는 품목 |
|---|---:|---:|---:|---|
| 무명 | 20 | ×1.0 | ×1.0 | — |
| 동네 소문 | 35 | ×1.15 | ×1.10 | 🛃 통관 |
| 구내 유명 | 50 | ×1.35 | ×1.25 | ❆ 냉동 |
| 시내 최고 | 70 | ×1.60 | ×1.45 | — |

품목 해금이 **"4개월차부터 냉동"에서 "구내 유명부터 냉동"으로** 옮겨졌다. 달력이 열어 주던 걸 내가 키워서 연다. (시나리오가 그 품목을 주제로 삼은 경우 `typeOverride` 는 건드리지 않는다.)

**밸런스(봇 30시드)**

| | 생존 | 평균 사이클 | 현금 |
|---|---:|---:|---:|
| 분기 | 97% | 6.0 | 5,116 |
| 반기 | 10% | 10.0 | 17,988 |
| 한 해 | 0% | 10.1 | 18,564 |

세 길이가 난이도 사다리로 깔끔하게 갈린다. 봇은 10사이클(5개월)쯤에서 평판이 바닥나 죽는데, **등급이 올라 물량·운영비가 커지는 걸 감당하지 못해서**다 — 스케일러가 작동한다는 뜻이다. 다만 죽을 때 현금 18,000c 가 남는 건 봇이 창고·계약을 안 키우기 때문이라 사람 플레이테스트로 확인이 필요하다.

**검증** 단위 68개, 봇 30시드 전 시나리오(분기 100% · 반기 7% · 한 해 0%), Playwright 스모크 39항목, 인수인계 봇 런 `phase win`(모든 비트 등장), 콘솔 에러 0.

**남은 것** 인수인계 대본을 6사이클 전부로, 평판 등급으로 여는 신규 고객사 계약, 사람 플레이테스트.


## 부록 M. 인수인계 대본 6사이클 (v1.9.1)

부록 L에서 대본이 각 달 전반(1·3·5)에만 붙어 있었다. 나머지 세 사이클이 무작위라 봇 런에서 반송이 7건씩 나왔다 — 첫 3개월에 사고를 겪게 할 이유가 없다. 여섯 사이클을 전부 대본으로 덮는다.

| 사이클 | 달력 | 영업일 | 부피 | 가르치는 것 |
|---|---|---:|---:|---|
| 1 | 3월 전반 | 13 | 38 | 쌓았다가 꽉 채워 보낸다. 3일차 첫 호출(7칸) · 6일차 둘째 차 · 8일차 두 대 동시 |
| 2 | 3월 후반 | 14 | 47 | 혼자 해 보기. 창고가 처음 4분의 3까지 찬다(`usage76`) |
| 3 | 4월 전반 | 13 | 57 | 고객과 ⚠ 파손, 배차 소진(`callsOut`), 이사철 보관 제안 |
| 4 | 4월 후반 | 13 | 56 | ❄ 신선과 냉장 구역 — 6칸이 금방 찬다 |
| 5 | 5월 전반 | 13 | 70 | 6일차에 **14칸 폭주** → 창고(24칸)를 넘겨 마당으로. 다음 날 비, 그대로 일요일(`usage91`·`rain`) |
| 6 | 5월 후반 | 13 | 40 | 마무리. 물량이 완만해 사고 없이 끝난다 — 평판 등급이 오르는 맛 |

- **창고 확장은 5사이클 마켓에서만** 판다. 1·2사이클 마켓에 확장이 있으면 사서 넓혀 버려 5사이클의 초과가 안 온다 — **넘쳐 본 다음에 파는 것**이 순서다.
- 해가 `TUT.YEAR = 2027` 로 고정이라 사이클 길이(13·14·13·13·13·13)가 매번 같고, 대본 행 수가 그대로 맞는다.

**봇 런(사람처럼 굴리는 `test/tutorial-run.js`) 결과**

| 사이클 | 순익 | 자금 | 평판 |
|---|---:|---:|---|
| 1 | +510 | 960 | 24/35 |
| 2 | +775 | 1,705 | 35/50 |
| 3 | +1,192 | 2,867 | 50/70 |
| 4 | +1,181 | 3,740 | 65/70 |
| 5 | +1,440 | 4,921 | 69/70 |
| 6 | +957 | 5,645 | 70/70 |

`phase win`, 폐기·도난·파손 0, 반송 1(5사이클 폭주 뒤). 등급이 무명 → 시내 최고까지 올라가는 곡선을 3개월 안에 한 번 보여 준다. 비트는 intro · usage · callReady · firstCall · handOff · weekend · rep · callsOut · m2 · special · deadline1 · offer · marketRefill · marketLimit · marketSwitch · m3 · usage76 · usage91 · rain · win · summary1~3 · farewell 이 전부 나온다.

반송 1건은 봇이 '가장 찬 계약'만 보고 급한 걸 먼저 보내지 않아서다 — 사람은 호출 팝업의 '급한 순 자동 선택'을 쓰므로 더 낫다. 여기서 더 깎으면 봇에 과적합이다.

## 부록 N. 평판 등급이 고객을 데려온다 (v1.10)

부록 K에서 평판을 지표로 세우고, 부록 L에서 등급을 난이도 스케일러(물량·운영비·품목 해금)로 썼다. 남은 절반이 이것 — **누가 나한테 맡기러 오는가**.

**고객마다 `repTier`**

| 등급 | 찾아오는 고객 |
|---|---|
| 무명 (0) | 🛒 큰마트 · 🏺 유리공방 — 동네 단골 |
| 동네 소문 (1) | 🌾 농협 직송 · 🚚 이사센터 · 🌙 새벽마켓 |
| 구내 유명 (2) | 🛃 수입상 · 🪑 가구공장 · ❆ 냉동창고 |
| 시내 최고 (3) | 💎 명품관 |

- 마켓의 고객 카드는 **지금 등급에서 열린 고객 중에서만** 뽑는다(`Game.openCustomers()`), 등장 확률도 등급을 탄다(0.3 → 0.66).
- 품목 해금(🛃 통관 = 동네 소문, ❆ 냉동 = 구내 유명)과 줄을 맞췄다 — 냉동 택배가 오기 시작하는 등급에 냉동창고 고객도 열린다.
- 실측(등급 고정, 60회 마켓): 무명 0회 · 동네 소문 27회(농협·이사센터) · 구내 유명 33회(+수입상·가구공장·냉동창고) · 시내 최고 42회(+명품관).

**보여 주기**

- **고객 화면 아래에 "아직 오지 않은 고객 — 소문이 나면 찾아옵니다"** 목록. 열린 고객은 `지금 마켓에 뜰 수 있음`, 아직이면 `구내 유명 등급부터`. 평판을 키울 이유가 화면에 있어야 한다.
- 정산에 등급이 오른 사이클이면 `⭐ 등급이 올랐습니다 — 새 고객이 찾아옵니다`, 로그에 새로 눈독 들이는 고객 이름.
- 박 반장 비트 `repUp`(첫 등급 상승 정산, 2페이지): 상한이 같이 오른다는 것, 물량·임대도 같이 오른다는 것, 그리고 **전에는 안 오던 고객이 마켓에 얼굴을 내민다**는 것.

**봇** `marketBot` 이 고객 카드도 사게 했다(자리·자금 여유가 있을 때). 20런에서 이사센터 9 · 농협 5 · 수입상 2 · 냉동창고 2 · 명품관 1 · 가구공장 1, 종료 시 등급은 구내 유명 5 / 시내 최고 15. 시나리오 밸런스는 그대로(표준 평균 9.7사이클, 분기 100%).

**검증** 단위 68개, 봇 30시드 전 시나리오, Playwright 스모크 **44항목**(고객 화면·등급 상승 2개 추가), 인수인계 봇 런 `phase win`(repUp 포함 비트 전부), 콘솔 에러 0.

## 부록 O. 실을 차가 없는 택배 · 계약이 못 받는 것 (v1.11)

플레이테스트에서 4사이클(4월 전반)에 **4칸짜리 대형**이 들어왔다. 그때 가진 계약은 대량(1~2칸)·냉장(1~4칸)·파손(1~4칸) 셋 — 어디에도 안 실리고 직접 배송은 크기 2까지라 막힌다. 화면 어디에도 **왜** 안 되는지, **뭘 사면** 되는지가 없었다. 반송 말고 길이 없는데 플레이어는 그걸 반송이 난 뒤에야 안다.

뿌리는 **계열이 강하게 전문화되어 있다**는 설계다(부록 D). 대량은 속성 없는 1~2칸만, 냉장은 ❄🌾만, 대형은 4칸 이상만. 좋은 규칙이지만 **화면에 한 번도 쓰여 있지 않았다** — 도움말 표조차 ❄⚠🌾 칸에 "아무 업체"라고 적혀 있어 정반대를 가르치고 있었다.

### 1. 못 싣는 택배는 붉게 말한다

- `Game.familiesFor(p)` — 이 택배를 **파손 위험 없이** 받아 주는 계열들. 계열의 기본 센터(`D.centerFor(fam, 0)`)로 판정하니 "일단 이 계열을 사면 된다"는 답이 된다.
- `Game.unhandled()` — 지금 어떤 계약으로도 못 싣고(배차 잔량과 무관) 직접 배송도 안 되는 택배들.
- 택배 줄: <b style="color:red">실을 차 없음</b> (`ps.noCarrier`). 통관 대기 중인 건 제외한다 — 그건 기다리면 되는 것이라 붉을 이유가 없다.
- 택배 상세: `지금 계약으로는 못 보냅니다 — 대형 · 철도 · 해상 계열 계약이 있어야 합니다 (마켓에서 구할 수 있어요)` (`pd.needFamily`). 계열조차 없으면(냉동 구역이 없는 ❆ 등) `창고 시설이나 강화가 필요합니다`(`pd.needNothing`).
- 박 반장 코치줄도 이걸 최우선으로 바꾼다(`coach.stuck`) — 창고가 90% 차 있어도 **반송 확정인 택배가 먼저**다.

### 2. 마켓이 반드시 답을 낸다

일반 마켓은 이미 `blockedTypes()` 로 막힌 품목의 해결 계약을 우선 뽑고 있었다. 구멍은 **대본 마켓**이었다 — `_scriptedMarketItems` 가 대본에 적힌 계약만 내놓고 조기 반환해서, 인수인계 중에 실을 차가 없어도 마켓에 답이 없을 수 있었다. 대본 목록 **앞에** 막힌 품목의 계열 센터를 하나 밀어 넣는다.

    [일반 마켓] sea0 (창고의 대형 1개 처리 가능) | large1
    [대본 마켓] large0 (창고의 대형 1개 처리 가능) | fragile1 (…갈아타기…)

힌트 문구도 `market.hint` = `창고의 {short} {count}개 처리 가능` 으로 **지금 창고에 있는 것**을 가리킨다. 추상적인 능력 설명보다 "저기 저거"가 빠르다.

### 3. 새 계약이 뭐가 다른지

마켓 계약 카드에 한 줄 추가(`mk.newlyHandles`): **＋ 지금은 못 하던 것: ❄냉장 · 크기 4칸**. 내가 가진 계약들(갈아타기면 그 대상은 빼고)과 비교해서 **새로 열리는 속성**과 **새로 열리는 첫 크기**만 적는다. 능력 아이콘 나열은 이미 아래에 있으니 여기는 차이만.

### 4. 내 계약이 못 받는 것 (튜토리얼과 무관하게)

`Game.contractBlocks(c)` 가 게이팅 속성 4종(❄⚠🛃❆)을 의사 택배로 하나씩 찔러 보고 거절당한 것과 크기 범위를 돌려준다. 강화(특약)·신뢰 특성으로 넓어진 실제 범위를 쓰므로, 완충 특약을 붙이면 ⚠가 목록에서 사라진다.

계약 상세와 마켓 오퍼 상세에 같은 줄이 붙는다:

    취급 못 함: ❄냉장 🛃통관 ❆냉동 · 크기 1~4칸만

계약을 사기 **전에도** 보인다 — 오퍼 상세는 아직 계약이 아니라 `{ carrier, enh: {} }` 의사 계약으로 판정한다.

### 5. 말로도 한 번

- `cantHandle` 비트(2페이지): 실을 차가 없는 택배가 생긴 날. "저대로 두면 기한 넘기고 반송이야" → "택배를 눌러 보면 어떤 계열이 필요한지 나와. 계열마다 받는 게 정해져 있어."
- `bigParcel` 비트: 4칸 이상이 들어왔는데 그만한 계약이 없을 때. 대형은 **직접 배송도 안 된다**는 것까지.
- `special` 비트에서 `months` 제한을 뗐다 — 4사이클에 처음 보는 🌾 농산물을 아무도 설명해 주지 않던 이유가 이것이었다. `gating` 에 `'produce'` 를 넣고 `story.special.produce` 문구를 새로 썼다.
- 도움말 속성 표의 "보낼 수 있는 업체" 칸을 실제 계열 규칙으로 전부 고쳤다(❄→냉장·항공·철도·해상 등). 대형(4~7칸) 줄을 추가하고, 표 앞에 "계열마다 받는 것이 정해져 있다 / 못 보내면 **실을 차 없음** 이 뜬다"는 문단을 놓았다.

### 6. 검증

단위 68개, Playwright 스모크 **46항목**(합성 4칸 대형 택배를 창고에 넣고 줄 경고와 상세 안내를 둘 다 확인하는 5.5단계 추가), 마켓 보장은 일반·대본 양쪽에서 확인, 마켓 오퍼 상세 전 카드 열어 콘솔 에러 0.

## 부록 P. '승리'가 아니라 인수인계 · 사이클을 개월로 말하기 (v1.11.1)

플레이테스트 지적 셋. 전부 **부록 L에서 달을 사이클로 쪼개고도 문구가 따라오지 않은 자리**다.

### 1. "내년 5월" — 사이클을 개월로 안 읽던 문구들

인수인계는 분기(6사이클) 런이라 2027년 **3월~5월**, 같은 해 안에서 끝난다. 그런데 박 반장은 "1년 단위로 돌아 … 내년 5월까지"라고 했다. `story.win.1` 이 `lastCal`(달 숫자)만 쓰고 **해는 '내년'으로 박아** 뒀기 때문이다 — 한 해(24사이클) 런에서만 맞는 말이었다.

- 파라미터를 셋 추가했다: `runCycles`(사이클 수) · `runMonths`(개월로 환산) · `lastLabel`.
- `lastLabel` 은 `yearOf(마지막 사이클) === yearOf(1)` 로 갈라 `fmt.lastSameYear`(`5월`) / `fmt.lastNextYear`(`내년 2월`) 를 고른다. 런 길이가 바뀌어도 따라온다.

| 런 | 박 반장 |
|---|---|
| 분기 6사이클 | "자네가 맡은 건 **6사이클, 3개월**이야. 3월에 시작해서 **5월까지** 굴리면…" |
| 반기 12사이클 | "12사이클, 6개월 … 8월까지" |
| 한 해 24사이클 | "24사이클, 12개월 … **내년 2월**까지" |

같은 뿌리의 다른 자리도 같이 고쳤다:

- `over.win` — `{months}개월 생존 성공!` 에 **사이클 수**를 넣고 있었다(분기 클리어가 "6개월 생존"). `Game.runMonths()` 로 환산해 `6사이클(3개월)을 끝까지 굴렸습니다`.
- `log.monthStart` — `{m}개월차 시작` 에 사이클 번호가 들어가 5월 후반이 "6개월차"였다. `── 2027년 5월 후반 시작 ──` 로.
- `demo.*` — `BUILD.demoMonths` 는 이름과 달리 사이클 수로 쓰이고 있었다(주석은 개월). 주석을 바로잡고 기본값을 6(=3개월)으로, 종료 문구는 `monthIndex()` 로.
- `how.3` · `title.goal` — "3월에 인수해서 내년 2월까지 · 1년 버티기". 런이 세 가지가 됐고, **끝나는 조건도 창고 포화가 아니라 평판**이다(부록 K). 둘 다 고쳤다.

### 2. "승리"가 아니다

플레이어는 이기러 온 게 아니라 **넘겨받으러** 왔다. 박 반장이 "버티면 자네가 이긴 거야"라고 하고 끝 화면이 「런 성공!」이면 이 게임이 무슨 이야기인지가 흐려진다.

- 인수인계 런은 결과 화면 제목이 **「인수인계 완료」**, 문구가 **"인수인계 끝 — 이제 여기 사장은 자네야"**(`res.winStory` · `over.winStory`, 결과에 `story` 플래그를 실어 가른다).
- 일반 런은 「완주!」. 시나리오 카드의 라벨 `prep.win` 도 **승리 → 완주 조건**.
- `story.intro.1` / `story.win.2` 도 "이긴 거야" → "인수인계 끝이고, 여기는 자네 거야" / "이기고 지고가 아니라, 넘겨받는 거지".

### 3. 버튼 설명은 버튼을 보면서

`callModal` 첫 장이 **"버튼에 실을 것 7/7칸이라 적혀 있었지?"** 였다. 그 버튼은 방금 눌러서 **화면을 떠난** 뒤다 — 없는 것을 기억으로 가리키고 있었다.

순서를 뒤집었다.

- `callReady` 1페이지에서 **계약 버튼을 강조한 채로** 숫자를 읽는다: "저 버튼에 **실을 것 7/7칸 · 28c** 라고 적혀 있지? 차 한 대가 7칸인데 지금 이 계약으로 실을 게 7칸 있다는 뜻이야." (비트의 1페이지에도 같은 `hl` 을 달았다. 값은 새 파라미터 `readyVol` · `readyFee`.)
- 2페이지가 "꽉 채울수록 개당이 싸진다 → 눌러 봐"로 이어진다.
- 팝업 안 `callModal.1` 은 **같은 값을 여기서도 볼 수 있다**로 바뀐다: "방금 버튼에서 본 그 숫자, 여기서는 **게이지**로 보여 줘. 7/7칸 — 버튼에서 한 번, 여기서 한 번. 굳이 밖에서 계산 안 해도 여기 들어와서 보면 돼."

### 4. 검증

단위 68개(`log.monthStart` 전반/후반 케이스로 교체), 스모크 **47항목**(게임 방법 카드의 런 길이·종료 조건 검사 추가), 인수인계 봇 런 `phase win` · 비트 전부 · 반송 1, 브라우저에서 intro → usage → callReady → callModal → callGo 순서 실제 확인, 콘솔 에러 0.

## 부록 Q. 마켓이 미리 말한다 · 평판의 눈금 · 80% 똔똔 (v1.12)

플레이테스트 한 판에서 나온 지적 여덟 개. 화면 문제 넷, 안내 문제 둘, 밸런스 둘.

### 1. 값이 라벨 옆에서 어긋난다

`.kv` 가 `1fr auto` 그리드인데 라벨이 **낱말 가운데서** 접혔다 — `처리한 택 / 배`, `창고 사용 / 률`. 한 줄 고치면 된다.

    .kv { … align-items: baseline; word-break: keep-all; }

`keep-all` 이면 한국어는 띄어쓰기에서만 접히고, `baseline` 이면 라벨이 두 줄이 돼도 값이 첫 줄에 붙는다.

### 2. '다음 달 예상' 을 가리키는데 화면엔 '2개월차 예상'

또 사이클↔개월이다(부록 P). `mk.forecastSum` 이 사이클 번호를 개월차로 찍고 있었다. `Game.cycleLabel()` 을 써서 **`다음 사이클(3월 후반) 예상 물량 28~37개`** 로, 준비 마켓은 `이번 사이클(…)` 로.

### 3. 예상 물량은 접혀 있는데 말로 설명하고 있었다

박 반장이 "저 줄 보이지? 어떤 택배가 몇 개쯤 올지야"라고 하는데 그 줄은 **접힌 요약 한 줄**이었다. 순서를 바꿨다 — `marketFc` 가 줄을 강조하고 **누르게**(gate) 한 뒤, `marketFcOpen` 이 펼쳐진 고객별 내역을 보면서 설명한다. 부록 P의 호출 버튼과 같은 원칙: **보면서 말한다.**

### 4. 처리 못 하는 게 오는 걸 미리 말해 준다 (튜토리얼 밖에서도)

부록 O는 **이미 창고에 있는** 택배를 다뤘다. 진짜 해결은 그게 **오기 전**이다.

`Game.forecastBlocked(m)` — 다음 사이클 예상 중 지금 계약(과 직접 배송)으로 못 받는 종류. 마켓 예상 줄 아래에 붉게:

    ⚠ 통관은 지금 계약으로 못 싣습니다 — 아래에서 받아 주는 계약을 구하세요

두 가지 판정을 쓴다.

- **대본 런**은 다음 사이클에 뭐가 오는지 정확히 안다 — `script(m)` 의 입고를 그대로 본다.
- **무작위 런**은 추정이라, 예상 범위의 합이 3 이상(대략 기대 1.5개 이상)인 종류만 경고한다. 범위가 ±1이라 거의 안 오는 것도 최대 2로 찍히는데, 그걸로 경고하면 **경고가 배경음**이 된다. 실측: 무명 등급에서는 한 번도 안 뜨고(그 등급엔 🛃❆가 안 온다), 구내 유명에서 냉장·파손만 가진 채로 가면 🛃통관이 뜬다.

비트 `marketBlocked` 가 같은 줄을 짚는다 — 달 제한 없이, 실제로 그럴 때만.

**(v1.12.1 정정)** 첫 판에서 바로 어긋났다. 화면엔 `📦 개인 고객 … 🛃통관 0~2 · 대형 0~2` 가 떠 있는데 붉은 줄은 없었다. 원인이 둘이었다.

1. 경고는 **대본**을 보고, 화면은 **추정**을 보고 있었다. 대본 런의 예상 물량도 `customerForecast` 로 그렸는데 그건 통계 추정이라 대본에 없는 🛃·대형까지 뽑아 놓고, `forecastBlocked` 는 대본만 봐서 "막힌 것 없음"이라고 답했다. → `_scriptForecast(m)` 을 만들어 **대본 런은 예상 물량도 대본에서** 뽑는다. 인수인계에서는 이제 `🛒 큰마트 12 · 일반 12` 처럼 정확한 수가 뜨고(min=max라 한 숫자로 표기), 오지도 않는 🛃·대형을 권하지 않는다.
2. 기대 1.5개 미만은 경고하지 않는 문턱이 있었다. 화면에 떠 있는데 아무 표시가 없으면 **어느 쪽도 못 믿는다.** 문턱을 없애고 판정을 `화면에 뜬 예상 그대로`로 바꿨다 — 뜬 종류 중 못 받는 것은 전부 표시한다.

표시도 요약 한 줄에서 **줄 단위**로 내렸다. 못 받는 종류는 그 자리에서 붉은 글씨 + `✖`(`.fcwarn`), 요약 줄은 그대로 두고, 박 반장 비트를 **충전·강화보다 먼저** 오도록 순서를 올렸다. 실제 흐름:

    (예상 물량 줄 강조) 눌러 봐 — 다음 사이클에 뭐가 올지 보여 줘
    (펼쳐진 내역)      고객별로 몇 개, 특수는 뭐가 몇 개인지까지 나와
    (붉은 줄 강조)     봐, 붉은 줄. 대형이 오는데 받아 줄 데가 없어. 오고 나서 사면 늦어

### 5. 프리미엄을 말로만 권했다

`market1.3` 이 "프리미엄 등급은 비싸"라고 하는데 **화면에 프리미엄 매물이 없어도** 떴다. 매물이 있을 때만 뜨는 `marketPremium` 비트로 떼어내고, 카드를 강조하고, 계열 표준 대비 수치를 읽는다: "{premCap0}칸 {premTrucks0}대가 {premCap1}칸 {premTrucks1}대가 돼."

마켓 계약 카드의 `＋ 지금은 못 하던 것` 도 고쳤다 — 갈아타기 매물을 **갈아탈 대상을 뺀 채로** 비교해서, 원래 되던 걸 새로 된다고 말하고 있었다(빠른손 익스프레스에 "크기 1칸").

### 6. "방금 깎였지" — 안 깎였다

`rep` 비트가 `rep < repCap()` 이면 떴다. 등급이 올라 **상한이 늘어난 것뿐**인데(20/20 → 20/35) 깎였다고 했다. `Game.repDropped`(addRep 에 음수가 들어온 적이 있는지)로 문구를 가른다 — 진짜 깎였으면 `story.rep.1drop`, 아니면 소개만 하는 `story.rep.1`.

### 7. 20/35 의 20 은 시작값이라 뭘 채우는지 안 보인다

게이지는 **0이 되면 끝**이라는 위험 신호이기도 해서 절대값을 버릴 수 없다. 그래서 두 가지를 더했다.

- 게이지에 **이 등급이 시작된 지점**(이전 등급 상한)에 눈금(`.gauge > u`). 20/35 의 20 이 "이미 반쯤 찬 것"이 아니라 "이 등급의 바닥"이라는 게 보인다.
- HUD 게이지를 누르면 **평판 상세**: 등급 구간 안에서의 진행 막대, `다음 등급 동네 소문까지 3`(상한이면 `상한을 채웠습니다 — 사고 없이 이번 정산을 넘기면 …`), 오르는 법·깎이는 법, 그리고 **다음 등급이 되면** 물량 +15% · 운영비 +10% · 새 고객 · 새 품목까지.
- 정산의 평판 줄에도 `다음 등급까지 3`.

### 8. 돈이 남아돈다 → 배차비를 규칙으로 바꿨다 (기준선은 절반)

인수인계 세 달 끝에 자금 4,378c, 봇의 한 해 런은 17,000c. 어떤 적재율로 불러도 남으니 **"꽉 채워 보내라"가 문구일 뿐 수지가 아니었다.**

배차비를 손으로 정한 값에서 **규칙**으로 바꿨다.

    fee = FILL_BREAKEVEN × cap × (그 계열이 싣는 종류들의 칸당 보상 평균, 전문 보너스 포함)

처음엔 `FILL_BREAKEVEN = 0.8` 로 잡았다 — 튜토리얼이 줄곧 "80%가 손해 안 보는 선"이라 말해 왔으니 그 말을 수지로 만들자는 생각이었다. 실제로 돌려 보니 **너무 셌다**: 첫 사이클 순익이 +10c까지 떨어져서, 무난하게 굴린 한 판이 본전 언저리로 끝난다. 그래서 **0.5** 로 내렸다.

대신 **본전 선과 보너스 선을 갈라 놓았다.**

- **절반(50%)** = 똔똔. 그 아래로 부르면 부를수록 손해다.
- **80%** = '적재 효율' — 업체 신뢰 +1, 평판 +1. 이제 80%는 **살아남는 선이 아니라 잘하는 선**이다.

| 계열 | 용량 | 원래 | 0.8 시도 | **최종(0.5)** |
|---|---:|---:|---:|---:|
| 대량 | 6 | 35 | 102 | **64** |
| 냉장 | 5→**4** | 48 | 92 | **58** |
| 파손 | 4 | 48 | 110 | **69** |
| 냉동 | 4 | 55 | 128 | **80** |
| 통관 | 8 | 70 | 188 | **118** |
| 대형 | 10 | 68 | 227 | **142** |
| 항공 | 4 | 83 | 77 | **48** |
| 철도 | 16 | 95 | 310 | **194** |
| 해상 | 14 | 83 | 165 | **165** |

냉장은 **5칸 → 4칸**도 같이 줄였다. 신선은 2칸·4칸으로 오는데 5칸 차는 2+2=4/5(80%)가 최선이고 4칸 하나를 실으면 80%에 못 미쳤다 — 4칸이면 2+2도 4도 딱 맞는다.

실측(무작위 25런의 모든 호출을 적재율 10% 단위로):

| 적재율 | 10% | 30% | 40% | **50%** | 70% | 80% | 100% |
|---|---:|---:|---:|---:|---:|---:|---:|
| 평균 순익(수익−배차비) | −39 | −12 | +14 | **+6** | +19 | +40 | +52 |

부호가 뒤집히는 자리가 30~40% 사이 — 의도한 절반 근처다. 이제 **반도 못 채우고 부르면 부를수록 손해**고, 대기가 진짜 선택지가 된다.

밸런스 영향(봇 30시드, 최종 자금):

| 시나리오 | 원래 | 0.8 시도 | **최종(0.5)** |
|---|---:|---:|---:|
| 분기 (생존 100%→97%) | 5,510 | 2,325 | **4,149** |
| 반기 | 16,997 | 7,822 | **11,617** |
| 한 해 | 17,227 | 8,018 | **11,617** |
| 성수기 (생존 100%) | 5,127 | 3,133 | **4,254** |

인수인계 봇 런은 `phase win` 유지, 최종 자금 5,725 → **4,743**, 반송 1. 0.8이 너무 조였던 것에 비해 0.5는 **원래와 0.8의 중간**에 앉는다 — 돈이 남아돌지는 않되 한 판이 본전으로 끝나지도 않는다. 어려운 시나리오(파업·항만·폭염)는 사람 플레이테스트에서 다시 봐야 한다.

`test/unit.js` 에 규칙 자체를 검사하는 테스트를 넣었다. 보상표나 용량을 건드리면 배차비가 어긋난 걸 그 줄이 잡는다.

### 9. 그 밖

- 결과 화면의 **「인수인계 완료」는 대본 런에만**. 안내만 켠 일반 런은 「완주」다 — 박 반장이 석 달 만에 떠나는 12개월 런을 '인수인계 완료'라 부를 순 없다.
- 박 반장의 마지막 말에 **끝났다는 것과 넘긴다는 것**을 넣었다: "여기까지가 인수인계야. … <b>이 창고, 이제 자네 거야. 잘 부탁하네.</b>"
- `how.2.d` 의 하드코딩 수치(개당 16c / 4c)를 새 배차비로: 시작 트럭 7칸에 두 개면 개당 51c, 꽉 채우면 26c.
- 인수인계 **시작 화면**이 "한 해를 돕니다"라고 했다 — 인수인계는 분기(6사이클·석 달)다. `story.startBody` 를 "봄 석 달(분기 · 6사이클)을 돕니다 … 여기서 끝까지 가면 반기·한 해 같은 더 긴 런이 열립니다"로. 데모 안내의 "한 해를 끝까지"도 "분기·반기·한 해 중에 골라"로. 이걸로 '1년'이라는 말이 문구에서 전부 사라졌다.

### 10. 검증

단위 **69개**(배차비 규칙 테스트 추가, 기존 요금 상수 테스트는 `D.FAMILIES` 참조로 교체), 스모크 47항목, 인수인계 봇 `phase win` · 비트 전부, 적재율-순익 실측 25런, 브라우저에서 정산 얼라인·평판 팝업·마켓 예상 줄 확인, 콘솔 에러 0.

## 부록 R. 캠페인 레벨 — 안 보여 주는 것으로 가르친다 (v1.13, 레벨 1)

유저 방향: **"일단 많은 걸 노출 안 시키는 게 중요한 것 같다."** 처음에는 계약도 하나, 고객도 하나, 날씨도 맑음뿐이고 그 정보를 전부 숨긴다. 창고에도 냉장·냉동 없이 실내/야외 둘뿐. 첫 메뉴도 시작·옵션·언어만. 레벨이 오르면 기능과 스토리가 하나씩 열린다.

지금까지 튜토리얼은 **완성된 화면을 말로 덮는** 방식이었다. 1일차 화면에 계약 3개·냉장/냉동 바·날씨 칩·평판 게이지·고객·배차 잔량이 한꺼번에 있으니 박 반장이 설명할 것이 많아지고, 그래서 글이 많아진다. 순서를 뒤집는다 — **화면에서 빼면 설명할 것이 없어진다.** 텍스트를 줄이는 가장 확실한 방법은 기능을 늦게 여는 것이다.

### 구조

캠페인은 **첫 해(24사이클)를 여섯 레벨로 쪼갠 것**이다. 레벨마다 4사이클(2개월)이고, 한국 달력이 그대로 흐르므로 언락이 계절과 저절로 맞는다. 판은 이어진다 — 자금·계약·창고·고객·상호가 다음 레벨로 넘어간다.

| 레벨 | 달력 | 열리는 것 |
|---|---|---|
| 1 | 3~4월 | (없음) 쌓기 · 꽉 채워 호출 · 기한 |
| 2 | 5~6월 | 마켓 · 배차 소모품 · 창고 확장 · 2대 동시 |
| 3 | 7~8월 | 날씨 · 야외 적재 · 직접 배송 |
| 4 | 9~10월 | ❄ 특수 품목 · 냉장 구역 · 고객과 신뢰 |
| 5 | 11~12월 | 평판과 등급 · 사고와 보험 · 보관 계약 |
| 6 | 1~2월 | ❆ 냉동 · 🛃 통관 · 주말 선택 · 퍽 |
| 끝 | — | 자유 런(분기·반기·한 해) · 도감 · 기록 |

**레벨 1만 구현했다**(`LEVELS.IMPLEMENTED = 1`). 나머지는 `www/js/levels.js` 에 커리큘럼만 선언해 뒀고, 레벨 1을 끝내면 지금은 인수인계와 자유 런이 열린다(임시 다리).

### 한 곳에서 켜고 끈다

`www/js/levels.js` 의 `FLAGS` 16개(`market · calls · simul · weather · theft · self · cold · frozen · attrs · customers · rep · storage · insurance · weekendChoice · perks · codex`)가 전부다. 레벨은 **누적**이고, `Game.shows(k)` 하나가 규칙과 화면 양쪽의 유일한 판정이다. 캠페인 밖(자유 런)은 언제나 전부 켜짐 — 기존 런은 한 줄도 달라지지 않는다.

- **규칙**(game.js): 배차가 안 줄고(`calls`), 동시 1대(`simul`), 날씨는 전부 맑음(`weather`), 평판은 움직이지 않고(`rep`), **정산 다음이 마켓이 아니라 바로 다음 사이클**(`market`), 보관 제안·보험·직접 배송·주말 선택지 없음. 레벨 1은 `noBankrupt` — 배우는 자리에서 부도로 끊지 않는다.
- **화면**(ui.js): 평판 게이지·회사 줄·냉장 바·날씨 칩·날씨 줄·고객 아이콘·고객 버튼·기록 버튼·빈 계약 슬롯·배차 잔량·정산의 평판 줄과 고객 섹션이 전부 사라진다. 대기 버튼도 「대기 · 직접 배송」이 아니라 「대기」.
- **비트**(story.js): 레벨 1은 **전용 비트 12개**(`BEATS_L1`)를 쓴다 — 대부분 한 장, 길어야 세 장. 기존 비트에는 `needs` 를 달아(예: `callsOut: 'calls'`) 아직 안 열린 기능을 말하지 않게 했다.

### 레벨 1

시작 판: 자금 300 · 창고 16칸(실내·야외만) · 계약 하나(한길 물류) · 고객 하나(큰마트) · 임대 정액 200.
4사이클 전부 대본이고 **일반 택배만** 온다(부피 27 → 34 → 40 → 55칸으로 완만히).

| 사이클 | 달력 | 자리 |
|---|---|---|
| 1 | 3월 전반 | 3일차에 **정확히 7칸** = 첫 차 한 대(그 달 첫 차는 +1칸). 6·9·12일차에 6칸 |
| 2 | 3월 후반 | 혼자 해 보기. 하루 2~3칸이라 이틀에 한 번 부르는 리듬 |
| 3 | 4월 전반 | 하루 4칸 — 매일 판단해야 한다 |
| 4 | 4월 후반 | 하루 5~6칸. 창고(16칸)가 처음 빡빡해진다 |

봇 런(`test/level-run.js`, 사람처럼 굴림): 사이클별 순익 +400 → +700 → +970 → +1,434, 끝 자금 3,506c, **반송 0 · 폐기 0 · 도난 0**, 처리 88개.

### 레벨 1을 끝내면 상호를 묻는다

「레벨 1 완료」 → **「이제 이름을 붙일 때야」** → 입력 → 프로필 `campaign { level, cleared, name }` 에 저장. 이때가 플레이어가 "내 창고"라고 처음 느끼는 지점이라 타이밍이 맞다. 이후 HUD 회사 줄이 그 이름으로 바뀐다(`Game.companyName()`).

타이틀도 캠페인 중에는 **시작하기 · 소리 · 음악 · 언어**뿐이다. 자유 런·인수인계·도감·기록은 캠페인을 끝내야 나온다.

### 검증

- 단위 **74개**(레벨 5개 추가): 플래그 누적, 시작 판, 숨긴 기능이 규칙에서도 꺼짐, 정산→다음 사이클, 결과에 level.
- `test/level-run.js`(신규): 레벨 1 봇 4사이클 완주.
- `test/level-ui.js`(신규, Playwright 390×800 ko): 타이틀 최소 메뉴 → 레벨 1 → **화면에 없어야 할 것 7가지 확인** → 4사이클 → 레벨 완료 → 상호 입력 → 타이틀에 자유 런이 열림. 콘솔 에러 0.
- 자유 런 회귀: 봇 20시드 전 시나리오 수치가 그대로(레벨 밖은 `shows()` 가 항상 참).

### 남은 것

- 레벨 2~6 (대본·비트·언락 연결). 레벨 사이 이어받기(`campaign.carry`)는 아직 저장만 하고 쓰지 않는다 — 레벨 2를 만들 때 붙인다.
- 레벨 1의 사이클 4가 봇에게는 여유로웠다(창고 최대 11/16). 사람 플레이테스트에서 더 조일지 결정.
- 레벨 1을 끝낸 자금 3,500c 가 레벨 2 첫 마켓에 적당한지.

## 부록 S. 착탈식 창고 모델 (v1.13.1)

유저 지적: **"새 런을 시작해도 (3D 뷰가) 아무것도 안 바뀌네. 카메라 위치만 바꿨는데, 모델을 직접 바꿔줬으면 좋겠어 — 착탈식으로."**

맞는 지적이었다. `scene3d.js` 의 `_buildWorld()` 는 창고를 **고정 모델 하나**로 짓고 있었다 — 바닥 9.8×6.2, 냉장실 바닥·낮은 벽·표시등, 기둥 3개, 지붕이 전부 상수였고, `warehouse` 를 보는 것은 바닥 **타일**뿐이었다. 그래서 레벨 1(16칸·냉장 0칸)에서도 화면에는 24칸짜리 창고에 냉장실이 붙어 있었고, 달라지는 건 카메라뿐이었다.

### 부품으로 쪼갰다

`_buildWorld` 를 **지형(`_buildTerrain`)** 과 **건물(`_syncBuilding`)** 으로 나눴다. 지형(잔디·도로·나무·배송 트럭)은 한 번만 짓고, 건물은 `warehouse` 가 바뀔 때마다 바뀐 모듈만 다시 짓는다.

| 모듈 | 언제 붙나 | 무엇이 달라지나 |
|---|---|---|
| `shell` 본동 | 언제나 | **깊이가 칸수를 따라 자란다** — `depth = clamp(ceil(cap / 8), 2, 7)`. 16칸이면 두 줄, 24칸이면 세 줄, 42칸이면 여섯 줄. 바닥·뒷벽·옆벽·지붕·기둥·도크가 그 발자국에 맞춰진다 |
| `cold` 냉장실 | `cold + frozen > 0` | 냉장 구역이 0칸이면 **아예 안 붙는다**(바닥·벽·표시등·간판까지). 있으면 칸수만큼 깊어진다 |
| `yard` 마당 | 언제나 | 본동 앞에 붙는다 — 본동이 얕으면 마당도 앞으로 당겨 온다 |
| `van` 밴 | 차량 시설(냉장/완충/대형 밴) 보유 | 마당 왼쪽에 그 차가 선다. 색은 종류별 |

- 구역 좌표(`MAIN`/`COLD`/`YARD`)가 상수에서 **`_zonesFor(wh)` 가 매번 계산하는 값**으로 바뀌었다. 바닥 타일·택배 패킹·구역 간판이 전부 이 값을 쓰므로, 건물이 자라면 택배가 쌓이는 자리도 같이 자란다.
- 게임 중에 붙는 모듈(확장·냉장실·밴)은 **위에서 내려앉는다**(`_dropIn`, bounce). 런을 처음 그릴 때는 조용히 짓는다 — 세이브를 열 때마다 건물이 떨어지면 곤란하다.
- 도로는 건물 오른쪽 끝에서 시작하도록 밀었다. 창고가 좁아지면 아스팔트가 마당 앞을 덮고 있었다.
- 카메라는 이제 **모델을 따라갈 뿐이다**: 냉장실이 없으면 그만큼 오른쪽으로, 건물이 작으면 조금 가깝게(배율 0.9~1.0). 보는 높이는 그대로 둬서 작은 창고가 작게 보이는 건 그대로다 — 자라는 게 보여야 하니까.

### 검증

`test/scene-shot.js`(신규): 창고 상태 다섯 가지(레벨 1 · 확장 한 번 · 냉장실 · 만재 · 후반+밴)로 3D 뷰만 찍는다. 콘솔 에러 0, 단위 74개, 레벨 1 봇 런·UI 런 그대로 통과.

### 남은 것

- 초대형(xl) 구역은 아직 모델에 표시가 없다.
- 마켓에서 확장을 산 직후가 아니라 **마켓을 닫을 때** 건물이 붙는다(그때 `scene.sync` 가 돈다). 마켓 안에서 바로 보여 주려면 구매 시점에 sync 를 한 번 더 불러야 한다.

## 부록 T. 정산 뒤 화면이 멈추던 버그 (v1.13.2)

레벨 1을 Playwright 로 **처음부터 끝까지 UI 로만** 굴려 보다가 잡았다(`test/play-ui.js`). 2사이클 1일차에서 화면이 얼어붙는다 — 날짜는 `3월 15일`(1사이클 마지막 날) 그대로, 대기 버튼은 `월말 정산`인 채 비활성, 아무것도 눌리지 않는다. 게임 상태는 이미 2사이클 1일차였다.

원인은 **마켓이 없는 런에는 화면을 다시 그릴 사람이 없다**는 것이었다. 정산 모달의 버튼은

    closeModal(); game.closeSummary(); saveGame(); checkPhase();

인데, 보통은 `closeSummary()` 가 `phase = 'market'` 으로 가고 `showMarket()` 이 화면을 맡았다가 마켓을 닫을 때 `scene.sync` + `renderAll()` 을 해 줬다. 레벨 1은 마켓이 없어서(부록 R) `closeSummary()` 가 바로 다음 사이클로 넘어가는데, `checkPhase()` 는 `phase === 'play'` 면 아무것도 하지 않는다 → 아무도 다시 그리지 않는다.

정산 버튼이 마켓 화면이 대신 해 주던 일을 직접 하게 고쳤다.

    closeModal(); game.closeSummary(); game.takeEvents(); saveGame();
    if (game.phase === 'play') { scene.sync(game, { animate: true }); renderAll(); }
    checkPhase();
    if (game.phase === 'play') { storyCheck({ kind: 'turn' }); showSms(); }

버튼 문구도 갈랐다 — 마켓이 있으면 「마켓으로」, 없으면 「3월 후반 시작」(`sum.toNext`).

**교훈**: 빨리 감기(게임 API 직접 호출)로 끝까지 도는 테스트는 이 버그를 못 잡는다. 화면을 갱신하는 것은 UI 쪽 코드이기 때문이다. `test/play-ui.js` 는 51일을 **버튼만 눌러서** 간다(약 3분).

**검증** `test/play-ui.js`: 타이틀(최소 메뉴) → 레벨 1 4사이클 완주(호출 22·대기 29) → 반송·폐기 0 → 레벨 완료 → 상호 입력 → 해금 확인 → 자유 런에서 3D 건물이 `24/6/4` 로 다시 지어지고 평판·날씨가 돌아오는 것까지. 콘솔 에러 0.

## 부록 U. 레벨 1에서 마지막까지 남아 있던 것들 (v1.13.3)

유저 지적 둘. **"냉장·냉동을 맨 처음 모델링에선 빼 줘. 평판 관련 내용도 레벨 1에선 빼면 좋겠어."**

### 첫 프레임의 냉장실

부록 S에서 건물을 착탈식으로 바꿨지만, **생성자가 쓰는 기본값이 `{ cap: 24, cold: 6, frozen: 4 }`** 였다. 그래서 타이틀 화면과 런이 그려지기 전 첫 프레임에는 냉장실이 붙은 창고가 서 있었다 — 레벨 1에 들어가면 곧 바뀌지만, 게임을 켜서 **처음 보는 모델**이 그것이었다.

기본값을 `BARE = { cap: 16, cold: 0, frozen: 0 }` 로 바꿨다. 이제 아무것도 안 붙은 맨 창고로 시작하고, 자유 런을 열면 그때 냉장실이 붙는다.

### 레벨 1에 남아 있던 평판

HUD 게이지와 정산 줄은 부록 R에서 껐지만, 두 군데가 남아 있었다.

- **도움말**(`help.body`, 5,700자)에 평판 10회 · 냉장 9 · 마켓 9 · 고객 12 · 보험 10회가 나온다. 레벨 1에서 도움말을 누르면 아직 없는 기능의 설명서가 통째로 열렸다 — 숨긴 보람이 없다. 마켓이 아직 안 열린 런에서는 **`help.l1`(그 레벨에 있는 것만, 7줄)** 을 보여 주고, 「게임 방법」 버튼(카드 3장이 런 길이와 평판을 말한다)도 그때는 빼둔다. 「창고장 노트」는 남는다.
- **메뉴** 머리말이 `분기 · 🏠 동네 택배 · ＋첫 호출 +1칸 · －…` 로 시나리오와 회사 특성을 늘어놓고, 「계절 문자」 토글도 있었다. 퍽이 안 열린 런에서는 **상호 한 줄**만 남기고, 계절 문자 토글은 날씨가 열린 뒤에만 보인다.

### 검증

`test/play-ui.js` 에 검사 넷 추가 — 첫 화면 모델이 `16/0/0/0`, 도움말에 평판·냉장·마켓·보험·고객·통관이 한 번도 안 나옴, 「게임 방법」 버튼 없음, 메뉴에 계절 문자·시나리오·평판 없음. 51일 UI 플레이 전부 통과, 콘솔 에러 0. 단위 74개(도움말 문구 검사 포함).

## 부록 V. '레벨'이 아니라 '장' (v1.13.4)

유저 지적: **"레벨 1. 쌓았다가, 꽉 채워 보낸다 — 이거 너무 이상해. 그냥 타이틀 이름처럼 해 줘. 레벨 1도 이상해… 그냥 서장. 이런 게 나을 것 같아."**

맞다. 「레벨 1 · 쌓았다가, 꽉 채워 보낸다」는 **제목이 아니라 설명문**이었고, '레벨'은 이 게임의 언어가 아니다(이 게임에는 경험치도 레벨업도 없다). 이야기로 부른다.

| 코드 | 화면 (ko) | 화면 (en) |
|---|---|---|
| level 1 | **서장** | Prologue |
| level 2~6 | 1장 ~ 5장 | Chapter 1 ~ 5 |

- 타이틀 버튼: 「시작하기」 아래 작은 글씨가 **「서장」** 뿐이다. 설명 문장(`lv.l1.title`)은 지웠다.
- 완료 화면: 「레벨 1 완료」 → **「서장 완료」**.
- 코드 안에서는 그대로 `level 1~6` 이다(`levels.js`·`cfg.level`·`profile.campaign.level`). 바뀐 것은 **부르는 이름**뿐이고, 화면 문구는 전부 `lv.ch.<n>` 하나를 거친다 — 나중에 장마다 부제를 붙이고 싶으면 그 키만 늘리면 된다.

**검증** `test/play-ui.js`(51일 UI 플레이) 전부 통과, 단위 74개, 타이틀 한/영 확인(「시작하기 서장」 / 「Start Prologue」).

## 부록 W. 서장 오프닝 — 왜 내가 여기 서 있는가 (v1.13.5)

유저 지적: **"박 반장 말이 너무 아무런 그리팅도 없고 설명도 없어. 왜 주인공이 갑자기 일을 돕게 됐고, 왜 인수인계를 하게 되었고, 좀 다양한 캐릭터가 나와서 말을 시작하면 좋을 것 같아."**

맞다. 이전 오프닝은 첫 문장이 곧장 **"쌓였다 싶으면 차를 불러"** 였다. 인사도, 이름도, 상황도 없이 규칙부터 나왔다. 튜토리얼이 막히는 이유는 규칙이 어려워서가 아니라 **왜 이걸 하고 있는지 모르는 채로 규칙을 받기 때문**이다.

### 무엇을 세우는가 — 세 가지 질문

1. **저 사람은 누구인가** → 박 반장, 이 창고에서 스무 해, 이번 달이 정년.
2. **나는 왜 여기 있는가** → 주인은 문을 닫자고 했고, 내가 해보겠다고 했고, 박 반장이 우겨서 자리가 남았다.
3. **왜 기한이 있는가** → 영감님이 말한 "한 해만 굴려 봐"가 캠페인 전체(6장 × 2개월 = 12개월)의 기한이 된다. 게임 길이를 시스템이 아니라 사람이 말한다. *(부록 Y·Z 에서 설정을 다시 세웠다 — 박 반장은 그 한 해를 내내 함께한다)*

### 등장인물 — 첫날에 세 사람

| 화자 | 언제 | 무엇을 위해 |
|---|---|---|
| **박 반장** | 1턴 · 이후 전부 | 안내자. 인사 → 사정 → 기한 → 규칙 |
| **여 실장** (한길 물류) | 1턴 4쪽 뒤 | *계약*이 사람이라는 걸 보여 준다. "지금은 저희가 유일한 계약이라" — 계약이 하나뿐인 이유를 대사가 대신 설명한다 |
| **김 대리** (큰마트) | 2턴 (`l1mart`) | *고객*이 사람이라는 걸 보여 준다. 고객 UI는 서장에서 잠겨 있지만(부록 R), 물건이 어디서 오는지는 알려 준다 |

첫 배송 성공(`l1first`)에도 여 실장이 다시 들어온다 — "잘 받았습니다! 다음에도 꽉 채워 주시면 저희도 좋고요." 규칙(적재율)이 사람의 입에서 두 번째로 나온다.

### 마지막 대사가 다음 화면을 연다

서장 마지막 보름(`l1last`) 2쪽:

> 이 창고, 아직 이름이 없어. 전 주인 간판은 떼 버렸거든.
> 이번 달만 넘기면 하나 붙여 봐.

서장 완료 직후의 **상호 이름 입력**(부록 R)이 시스템 팝업이 아니라 박 반장이 꺼낸 이야기의 답이 된다.

### 구현

- `story.js` `build()` 에 **페이지별 `nameKey`** 를 추가했다. 지금까지 화자 이름은 `speaker` 하나로 결정됐는데(`park` = 창고장, 그 외 = 계약 담당), 김 대리처럼 계약도 창고장도 아닌 인물이 필요했다.
  ```js
  name: pg.nameKey ? root.I18n.t(pg.nameKey) : speaker === 'park' ? root.I18n.t(CHARACTER.nameKey) : repName(speaker)
  ```
- `l1intro` 3쪽 → **6쪽**(인사 · 사정 · 기한 · 창고 가리키기 · 여 실장 · 규칙+게이트).
- `l1mart` 비트 신설 — 2턴, `l1intro` 를 본 뒤에만.
- `l1first` 1쪽 → 3쪽, `l1last` 1쪽 → 2쪽.
- 문구는 전부 `story.l1*` 키로 ko/en 양쪽에 넣었다. 새 이름 키는 `story.name.mart`.

### 검증

`test/play-ui.js` 51일 UI 플레이 전부 통과(호출 22 · 대기 29 · 반송 0 · 폐기 0 · 콘솔 에러 0), 단위 74개. 오프닝 6쪽이 순서대로 나오는 것과 2턴 김 대리 등장을 브라우저에서 직접 읽어 확인했다.

## 부록 X. 오프닝 씬 — 카메라가 먼저 말한다 (v1.13.6)

부록 W 로 첫 대사에 인사와 사정이 생겼다. 그래도 게임을 켜면 **첫 그림이 곧장 '창고 + 버튼'** 이다. 플레이어는 자기가 어디에 서 있는지 보기 전에 조작을 본다.

그래서 서장을 처음 여는 순간 **11초짜리 카메라 연출**을 한 번 재생한다. 새 에셋은 없다 — 이미 있는 창고·탑차·나무·도로를 카메라가 다르게 볼 뿐이다.

### 샷

| # | 길이 | 카메라 | 자막 |
|---|---|---|---|
| A | 3.6초 | 길 건너 높은 자리에서 창고 전경으로 천천히 다가간다. 도크 밖에 탑차가 서 있다 | 2027년 3월 2일, 월요일. / 스무 해 굴러온 창고 하나가 이번 달로 문을 닫는다. |
| B | 3.6초 | 컷 — 창고 앞으로 바짝 붙어 도크를 낮게 훑는다 | 주인은 접자고 했고, 나는 해보겠다고 했다. |
| C | 3.8초 | 컷 없이 그대로 **게임 카메라로 떠오른다**. 동시에 창고 뷰가 화면 전체에서 평소 크기로 줄고, HUD·패널이 밝아진다 | 오늘이 첫날이다. |

C 가 이 연출의 핵심이다. 오프닝과 플레이 화면 사이에 **이음매가 없다** — 페이드 아웃도, 로딩도, 「시작」 버튼도 없이 카메라가 착지하는 자리가 곧 1턴 화면이고, 거기서 박 반장이 말을 건다(부록 W).

### 화면 크기가 카메라의 일부다

처음엔 창고 뷰를 화면 전체(100%)로 키웠는데 **벽만 찍혔다**. 세로 화면(390×800)에서 세로 화각 38도는 가로로 치면 20도 남짓 — 망원 렌즈다. 그래서 오프닝 동안에는

- 창고 뷰를 **62%** 만 쓰고(가로세로비 0.8 → 가로 화각 37도), 남는 아래쪽은 검은 띠가 덮는다.
- 화각도 46도로 벌렸다가 마지막 샷에서 게임 화각 38도로 건너간다.
- 착지점은 **줄어든 뒤의 화면**에서 미리 재 둔다. 줄어드는 동안 실시간으로 따라가면 가로세로비가 0.9 를 넘는 순간 카메라 공식이 다른 가지로 갈아타면서 카메라가 튄다.
- 평소 창고 뷰 높이는 화면에 따라 다르다(짧은 화면은 25%). 상수로 박지 않고 `getComputedStyle` 로 읽는다.

### 깜빡임 — rAF 를 두 개 돌리면 안 된다

처음엔 `intro.js` 가 자기 `requestAnimationFrame` 을 돌렸다. 그랬더니 중간중간 **하늘과 잔디만 찍힌 프레임**이 나왔다. 원인: 매 프레임 `renderer.setSize()` 로 캔버스를 다시 잡는데, scene3d 의 렌더 루프가 그보다 먼저 돌면 그 프레임은 **빈 캔버스**가 된다(알파 캔버스라 `#scene` 의 하늘/잔디 CSS 배경이 그대로 비친다).

고침: 오프닝은 자기 루프를 갖지 않는다. `scene.cineStep` 에 함수를 걸어 두면 scene3d 가 **`render()` 직전에** 불러 준다. 크기 변경 → 카메라 → 렌더가 한 프레임 안에서 순서대로 일어난다.

### 규칙

- **서장을 시작할 때마다.** 처음 한 번만 틀까 했지만(`introSeen`) 유저가 매번 보자고 했다 — 서장은 이 게임의 오프닝이고, 새로 시작한다는 건 이 이야기를 처음부터 다시 본다는 뜻이다. 대신 **이어하기에는 안 나온다**(중간부터 이어 붙이는 자리에 오프닝이 끼면 어색하다). 판단은 `startLevel` 한 줄 — `startPlay({ intro: n === 1 })`.
- **아무 데나 누르면 끝난다.** 오른쪽 위 「건너뛰기」 버튼도 있지만 화면 전체가 버튼이다.
- 오프닝 동안 모달·대화창·SMS 는 하나도 뜨지 않는다. `startPlay({ intro: true })` 가 `checkPhase()`·`storyCheck()` 를 오프닝 뒤로 미룬다.
- `?nointro` 로 끌 수 있다(테스트·반복 확인용).
- 탑차는 오프닝 동안만 도크 밖(x 7.6)에 세워 두고, 끝나면 평소 대기 자리(x 12)로 돌려놓는다.

### 곁다리로 잡은 버그 — 서장 이어하기가 깨져 있었다

오프닝 재생 조건을 시험하다 `이어하기` 에서 `this._shows.has is not a function` 이 떴다.

`_shows` 는 "이 레벨에서 열려 있는 기능" **Set** 인데(부록 R), `toJSON` 이 인스턴스를 통째로 펼치는 바람에 세이브에 같이 나갔다. Set 은 JSON 으로 나가면 `{}` 다. 불러오면 `{}` 는 truthy 라서 `shows()` 가 `.has` 를 부르고 터진다 — **서장을 저장하고 이어하면 화면이 죽었다.**

고침: `level`·`_shows` 는 저장하지 않는다(둘 다 `levels.js` 의 표에서 나오는 값이다). `fromJSON` 이 `cfg.level` 로 다시 만든다.

```js
const lvn = g.cfg && g.cfg.level;
g.level = lvn && LV ? LV.get(lvn) : null;
g._shows = g.level ? LV.showsAt(lvn) : null;
```

단위 테스트 둘을 박아 뒀다 — 서장 세이브 왕복 뒤 `level`·`shows('market')`·대본이 살아 있는지, 자유 런은 그대로 전부 열려 있는지.

### 검증

`test/intro-ui.js`(새 파일) — 첫 시작·이어하기·**두 번째 서장**·`?nointro` 네 경우를 돌려 본다. `test/play-ui.js` 에 오프닝 절 추가 — 재생 여부, 오프닝 중 대화창·패널이 안 보이는 것, 자막, 건너뛰기 뒤 원상복구(`cine` 해제 · 인라인 높이 제거 · 탑차 x12 · 화각 38 · `introSeen`). 51일 UI 플레이 전부 통과, 콘솔 에러 0, 단위 76개. 가로(900×620)·세로(360×780)도 따로 확인했다.

## 부록 Y. 서장의 이야기 — 한 사장님, 그리고 한 해의 시험 (v1.14)

유저 지적 셋을 한 번에 받았다.

1. **"창고가 저렇게 훤히 드러나면 안 되잖아."** 밖에서 보는 건물은 벽이 다 있어야 하고, 카메라가 안으로 들어올 때 벽이 사라져야 한다.
2. **"야외가 건물 내에 있는데, 야외는 건물 바깥이어야 하고."**
3. **"스토리가 너무 빈약하다. 지금은 너무 급격하게 돌아가는 느낌이야."** 내가 이 일을 맡기까지의 과정이 필요하다.

### 1. 건물은 '완성'과 '단면' 두 모습을 가진다

`scene3d.js` 의 창고를 둘로 쪼갰다.

| 부품 | 무엇 | 언제 보이나 |
|---|---|---|
| `shell` | 바닥 · 뒷벽 · 양옆 벽 · **뒤쪽** 지붕 · 기둥 · 도크 | 항상 |
| `front` | **앞지붕 + 앞벽 + 셔터** | 완성 건물일 때만 |

`scene.close(true)` 면 앞면이 붙어 **밖에서 보는 완성된 창고**, `close(false, true)` 면 앞면이 **들려 올라가며 투명해진다** — 오프닝 마지막 샷에서 카메라가 안으로 들어오는 그 순간에 맞춰 벗겨진다. 플레이 화면은 언제나 벗겨진 단면이다.

바닥 표시(구역 타일·「야외」 간판)는 `depthTest: false` 스프라이트라 벽을 뚫고 보인다. 그래서 `closed` 동안에는 타일 그룹째 숨긴다.

**그림자**: 지붕과 기둥은 이제 그림자를 **던지지 않는다**(`castShadow = false`). 앞지붕까지 덮고 나니 실내가 통째로 어두워져 택배 상자가 안 보였다. 받기는 그대로 받는다 — 건물이 땅에 던지는 그림자는 벽이 만든다.

### 2. 야외는 건물 밖이다

전에는 바닥 슬래브가 마당까지 깔려 있었고 `yardZ0` 가 앞벽 안쪽이었다. 고쳤다.

```
yardZ0 = frontZ + 0.35     // 앞벽 '바깥'
roadZ  = yardZ0 + 마당깊이 + 1.0    // 도로는 마당 너머 (창고가 깊어지면 같이 밀린다)
```

바닥 슬래브는 `BACK_Z ~ frontZ` 실내만 덮고, 마당은 흙바닥 패드다. 탑차의 z 도 도로를 따라간다. 창고가 앞뒤로 0.5 쯤 깊어졌으므로 카메라도 조금 물러섰다(`k` 하한 0.9 → 0.96, 보는 점 z 0.3 → 0.55).

### 3. 이야기 — 나는 왜 여기 있는가

설정을 세웠다.

- **한종수(한 사장님 · 영감님).** 이 창고의 주인, 노인이다. 나이는 숫자로 못 박지 않는다 — 지팡이와 말투가 이미 말한다. 마흔에 리어카 한 대로 시작했다.
- 주인공은 **열여섯에 아버지 심부름으로 처음 왔고**, 그 뒤로 손이 모자란 날마다 와서 일을 도왔다.
- 작년 겨울, 영감님이 말했다. *"내가 이제 못 들겠어. 자네가 한 해만 굴려 봐. 할 만하면 싸게 넘겨주지."*
- 그래서 **한 해(24사이클 = 12개월) 시험 운영**이 시작된다. 이게 캠페인 전체다 — 서장~5장 × 2개월 = 12개월.
- **박 반장**은 이 창고에서 물건을 받고 내보내는 사람, 스무 해째. 영감님이 부탁해서 **한 해 내내** 옆에 붙는다(서장만이 아니다). 시험이 끝난 뒤에도 조언자로 남을 수 있다.
- 한 해를 다 굴리면 **상호를 짓고 매매계약서에 도장을 찍는다.** 거기서부터가 자유 런이다.

캠페인 길이가 곧 이야기의 기한이라는 게 핵심이다. "2개월 × 6장"은 시스템 숫자였는데, 이제 영감님이 말한 "한 해만 굴려 봐"의 한 해다.

### 4. 오프닝 — 회상을 낀 5샷 (약 20초)

| # | 길이 | 화면 | 자막 |
|---|---|---|---|
| 1 | 4.0초 | 오늘 아침, 길 건너 전경 | 2027년 3월 2일, 월요일. / 이 창고에는 주인이 있다. 한종수. 여든둘. |
| 2 | 4.2초 | **회상** · 창고 옆구리 | 아버지 심부름으로 처음 온 게 열여섯이었다. |
| 3 | 4.2초 | **회상** · 셔터 앞 | 그 뒤로는 손이 모자란 날마다 내가 왔다… / "내가 이제 못 들겠어. 자네가 한 해만 굴려 봐." |
| 4 | 3.6초 | 오늘로 — 색이 돌아온다 | "할 만하면 그때 싸게 넘겨주지." 그래서 오늘부터 열두 달이다. |
| 5 | 4.4초 | 착지 + **앞면이 들린다** | 혼자 하는 건 아니다. 오늘이 첫날이다. *(→ 부록 Z 에서 6샷 29.4초로 다시 짬)* |

**회상 연출**은 3D 를 새로 만들지 않는다 — `#app.recall` 클래스가 창고 뷰에 `filter: sepia(.8) contrast(.92)` 를 걸고, 자막 상자도 낡은 종이색으로 바뀐다. 컷마다 짧은 암전이 들어가므로 색이 도는 순간이 전환으로 읽힌다.

박 반장의 첫 대사(부록 W)도 새 설정에 맞춰 고쳤다 — *"영감님한테 얘기 들었어. 오늘부터 자네가 굴린다고."*

### 5. 장 마무리: 리포트 → 편지 → (마지막 장) 상호 → 계약서

장을 끝내면 화면이 셋 이어진다.

1. **박 반장 리포트** — 처리 · 정시율 · 반송/폐기 · 남은 자금을 표로. 아래에 한 줄 총평이 붙는데 성적에 따라 셋 중 하나다(`good` / `ok` / `bad`). 이건 *영감님께 보내는 보고서*다.
2. **한 사장님 편지** — 낡은 종이 모달. 장 번호와 성적(`letter.<n>.<good|bad>`, 없으면 `letter.generic.*`)으로 문구가 갈린다. 잘했을 때와 흘렸을 때의 편지가 다르다. 흘렸을 때도 야단치지 않는다 — *"나도 첫해엔 남의 이삿짐을 통째로 비 맞혔네."*
3. 마지막 장을 끝냈으면 **상호 입력** → **매매계약서 + 도장**. 매도인 한종수 / 매수인 〈내 상호〉 / 목적물 동네 창고 1동 · 부속 마당. 도장(`韓`)이 크게 찍히며 내려앉는다.

지금은 `LEVELS.IMPLEMENTED === 1` 이라 서장이 곧 마지막 장이고, 그래서 서장을 끝내면 바로 계약서까지 간다. 2~5장이 붙으면 그 장들은 리포트 + 편지로 끝나고(`lv.nextCh`), 계약서는 마지막 장으로 밀린다 — `afterChapter()` 한 줄(`r.level >= LEVELS.IMPLEMENTED`)이 그 분기다.

이를 위해 `_makeResult` 가 `onTimeCount` · `deliveredCount` · `returned` · `broken` 을 결과에 싣는다.

### 6. 한 사장님 스프라이트

`tools/sprites.py` 에 `char_E` 추가 — 80대, **성긴 백발**(정수리는 비었고 옆머리만), 깊은 주름, 흰 눈썹, 갈색 카디건 + 셔츠, 지팡이. 박 반장(안경 · 파란 셔츠 · 작업 조끼 · 머그)과 한눈에 구분된다. 표정은 `neutral / neutral_talk / smile / smile_talk / worry / laugh` 여섯 — 조연 담당자(3종)보다 많다. 이야기 인물이라 편지·회상에서 표정을 더 쓴다.

`story.js` 의 `sprite()` 도 고쳤다 — 전에는 말할 때 무조건 `<id>_neutral_talk` 로 떨어졌는데, 이제 `<id>_<expr>_talk` 를 먼저 찾는다.

### 검증

단위 76개, `test/intro-ui.js`(오프닝 4경우 + 완성/단면 확인), `test/play-ui.js` 51일 UI 플레이(장 마무리 네 화면 전부 검사 — 리포트 · 편지 · 계약서 · 도장), `test/level-ui.js`, `test/scene-shot.js`(창고 5단계). 콘솔 에러 0. UI 테스트는 오프닝을 매번 볼 필요가 없으므로 `?nointro=1` 로 연다(`play-ui` 만 오프닝을 직접 검사한다).

## 부록 Z. 오프닝 2차 — 30초, 인물 소개, 그리고 들어오는 탑차 (v1.14.1)

유저 지적 다섯.

### 1. "세피아가 대사 중간에 적용되고 있어"

맞다. 회상 플래그가 **샷**에 붙어 있었다. 컷 시각과 자막 시각이 어긋나면 한 줄을 읽는 도중에 화면 색이 변한다 — 읽는 사람 입장에서는 이유 없이 화면이 흔들린 것이다.

고침: 회상은 이제 **자막의 속성**이다(`LINES[].recall`). 전환은 자막이 거의 안 보일 때만 일어난다.

```js
const want = cur ? !!cur.recall : recall;
if (want !== recall && (!cur || alpha < 0.08)) { recall = want; ... }
```

그리고 샷 경계(암전)와 줄 사이 공백이 맞물리도록 타임라인을 다시 짰다 — 한 줄이 컷을 넘어가지 않는다. `test/intro-ui.js` 가 0.2초마다 찍어서 **"같은 자막이 떠 있는 동안 recall 이 바뀌면 위반"** 으로 센다. 위반 0.

### 2. "회상에도 효과음이 들리고 나왔으면"

`sfx.js` 에 오프닝 전용 넷을 더했다(전부 합성, 에셋 없음).

| | 언제 | 소리 |
|---|---|---|
| `recallIn` | 회상 진입 | 523Hz 가 220Hz 로 미끄러지는 한 숨 + 낮은 노이즈 |
| `recallOut` | 오늘로 복귀 | 반대로 올라간다 |
| `card` | 인물 소개가 뜰 때 | 짧은 두 음 |
| `shutter` | 앞면이 들릴 때 | 노이즈 + 낮은 톱니 + 끝의 걸쇠 소리 |

여기에 기존 `truck()` · `horn()` 이 탑차 입장에 붙는다.

### 3. "오프닝이 너무 짧아. 30초는 가져가도 돼"

**6샷 29.4초**로 늘렸다. 회상이 셋(옆구리 → 셔터 앞 → 로우앵글)이라 "여러 해 동안"이 한 컷으로 뭉개지지 않는다. 회상 세 샷의 구도를 일부러 다 다르게 잡았다 — 각도가 같으면 세피아 필터 혼자 일하는 셈이다.

| # | 길이 | 화면 | 회상 |
|---|---|---|---|
| S1 | 4.6초 | 오늘 아침 · 길 건너. **마당이 비어 있다** | |
| S2 | 5.0초 | 창고 옆구리, 오래 드나든 자리 | ● |
| S3 | 5.0초 | 셔터 앞, 짐을 같이 내리던 자리 | ● |
| S4 | 5.0초 | 로우앵글로 지붕선을 올려다본다 — 영감님이 말한 그날 | ● |
| S5 | 5.2초 | 색이 돌아오고 **탑차가 들어온다** | |
| S6 | 4.6초 | 착지 + 앞면이 들린다 | |

### 4. "한 사장님 포트레잇을 흘러가듯 보여주면 좋겠다"

대화창이 아니라 **영화 인물 소개 자막**이다. 왼쪽에서 밀려 들어왔다 4초 뒤 빠진다 — 초상(32px 스프라이트) + 이름 + 한 줄.

- 5.6초 · 회상 진입 직후 → **한종수** / 이 창고의 주인
- 21.2초 · 탑차가 들어온 뒤 → **박 반장** / 이 창고 반장

회상 중에는 카드도 낡은 종이색으로 바뀐다(`#intro.recall .icard`). 초상은 `Story.sprite()` 를 그대로 쓰므로 새 에셋이 없다.

### 5. "트럭이 처음부터 나와 있는데, 카메라가 변하면서 입장하는 느낌이면"

오프닝은 이제 **빈 마당**에서 시작한다. 탑차는 화면 밖(x 24)에 있다가 S5 가 시작될 때 도로를 따라 들어와 도크 앞(x 7.6)에 선다(2.6초). 엔진 소리가 먼저, 2.7초 뒤 경적. 끝나면 평소 대기 자리(x 12)로 돌려놓는다.

`scene3d` 에 `truckTo(x, dur)` 와 `dropTween(tag)` 추가. 태그를 쓰는 이유는 건너뛰기로 중간에 끊겼을 때 트윈이 남아 탑차를 계속 움직이면 안 되기 때문이다.

### 그 밖에

자막이 `textContent` 라 `<br>` 이 글자로 보였다 → `innerHTML`(문구는 우리 locale 파일이라 안전).

### 검증

`test/intro-ui.js` 에 절 하나 추가 — 30초 길이, **대사 도중 세피아 전환 0건**, 회상/오늘 구간 공존, 인물 카드 2장(한종수·박 반장), 탑차 x24 → x7.6. 단위 76개, `play-ui` 51일, 콘솔 에러 0.

### 덧 — 박 반장은 한 해 내내 (v1.14.2)

유저 정정: **"박 반장은 일 년 내내 함께할 거야. 그 후에도 어드바이저로 일할 수 있고. 두 달만 함께하는 건 아냐."**

부록 W 에서 박 반장을 "두 달만 옆에 붙는" 사람으로 썼는데 틀렸다. 그는 이 창고 반장이고, **한 해 시험 내내 함께 간다.** 서장(첫 두 달)은 그가 규칙을 하나씩 짚어 주는 구간일 뿐, 그 뒤로도 자리를 지킨다. 시험이 끝나 창고가 내 것이 된 뒤에도 조언자로 남을 수 있다.

고친 문구:

| 키 | 전 | 후 |
|---|---|---|
| `card.park.desc` | 전 창고장 · 두 달만 옆에 | 이 창고 반장 |
| `story.l1intro.3` | "…자네 옆에 좀 붙어 있으라대. 두 달만. 그 담부턴 자네가 알아서." | "…자네 옆에 좀 붙어 있으라대. 나야 갈 데도 없고 — 올해는 계속 같이 하는 거야." |
| `intro.10` | 혼자 하는 건 아니다. | 혼자 하는 건 아니다. 박 반장님이 올해 내내 붙는다. |

자막이 길어져 타임라인도 다시 맞췄다(L10 23.4초 2.3초 / L11 26.1초 2.9초).

## 부록 AA. 나레이션 — 쳐지는 자막과 전용 음색 (v1.14.3)

유저 지적 셋.

### 1. "대사가 여전히 너무 빨라"

자막 시간을 손으로 박아 놓은 게 문제였다. 문구를 고칠 때마다 시간을 다시 재야 했고, 그래서 늘 짧은 쪽으로 남았다.

이제 **타임라인을 문구에서 계산한다.**

```
한 줄 = LEAD(0.32초, 떠오름) + 글자수 × CPS + HOLD(1.35초, 다 친 뒤 머무름)
줄 사이 = GAP(0.38초)
샷 길이 = 그 샷에 실린 줄들의 합 (최소값 min 이 있으면 그것보다는 길게)
```

`CPS` 는 한국어 0.052초/글자, 그 외 0.034 — 한글은 한 글자에 담긴 정보가 많아 라틴보다 천천히 친다. 문구를 고치면 샷 길이가 따라 늘어나므로 손으로 맞출 게 없다.

결과: **6샷 약 38초**(한국어 기준). 전보다 8초 길지만 한 줄당 머무는 시간이 두 배다.

### 2. "한 번에 나오는 게 아니라 대사 치듯이"

자막이 **한 글자씩** 쳐진다. `<br>` 은 한 글자로 세어 태그가 반쯤 잘려 나오지 않게 한다.

```js
const tokens = html => html.split(/(<br\s*\/?>)/i)
  .reduce((a, part) => (/^<br/i.test(part) ? (a.push('<br>'), a) : a.concat(part.split(''))), []);
```

치는 동안 끝에 **깜빡이는 블록 커서**가 붙고, 다 치면 사라진다.

### 3. "박 반장과는 다른 나레이션 전용 음색"

이 글은 누가 말하는 게 아니라 **주인공이 속으로 읽는 글**이다. 그래서 대화창의 `blip`(300Hz 사각파, 딱딱하고 높다)과 정반대로 잡았다.

```js
narrate(n = 0) {
  const f = 176 + [0, 14, 26, 8, 20][n % 5];
  tone(f, 0.06, 'triangle', 0.14, 0, 26);   // 낮고 둥근 삼각파
  tone(f * 2, 0.03, 'sine', 0.045);          // 아주 옅은 배음
}
```

한 옥타브 아래, 삼각파, 음량 절반. 공백과 `<br>` 에서는 울리지 않는다(최소 간격 0.05초).

### 검증

`test/intro-ui.js` 에 검사 셋 추가 — 길이(32~50초), **"같은 줄 안에서 글자가 늘어나는 중간 상태 프레임"** 30개 이상(= 한 번에 안 뜬다), 타자 중 커서 존재. 기존 검사(세피아 위반 0 · 인물 카드 2 · 탑차 x24→x7.6)와 `play-ui` 51일, 단위 76개 전부 통과.

## 부록 AB. 첫날을 '시키는 대로' 대신 '숫자로' (v1.15)

플레이테스트 피드백 둘.

1. **"처음에 여전히 텍스트가 많다."**
2. **"하라는 대로 따라 하라는데 그게 무슨 의미인지 잘 모르겠다."**

두 번째가 진짜 문제다. 첫날 박 반장이 *"오늘은 기다려"* 라고 하는데, **왜** 기다리는지는 말로만 있었다("한 번 부를 때마다 돈이 나가"). 플레이어는 규칙을 배운 게 아니라 지시를 따랐을 뿐이고, 그러면 다음에 혼자 판단할 수가 없다.

### 여 실장이 계산해 준다

첫날 **여 실장이 인사차 들른다.** 그리고 지금 이 순간의 숫자를 실제로 읽어 준다 — 보내지는 않는다.

> **여 실장** — 한길 물류 여 실장입니다. 인사차 들렀어요. 오늘 한 대 부르시게요?
> **여 실장** — 저희 차는 **7칸**, 한 번 부르면 **51c**예요. 지금 창고에 있는 건 2칸 — 받는 값이 35c고요.
> **여 실장** — 그러니까 지금 보내시면 **16c 손해**예요. 배차비는 한 칸을 싣든 7칸을 싣든 똑같이 51c라서.
> **박 반장** — 봤지? 그래서 오늘은 **기다려**.

전부 `params()` 가 게임 상태에서 뽑은 실제 값이다. 하드코딩이 아니라서 밸런스를 바꾸면 대사의 숫자도 따라 바뀐다.

```js
// 지금 이 순간 차를 부르면 실제로 얼마인가
const nowCap = g.vehicleCap(nowC), nowFee = g.truckFee(nowC);
let nowVol = 0, nowRev = 0;
for (const p of g.eligibleParcels(nowC).sort(급한순)) { if (nowVol + p.size > nowCap) continue; nowVol += p.size; nowRev += p.reward; }
→ { nowCap, nowFee, nowVol, nowRev, nowNet: nowRev - nowFee, nowLoss: Math.max(0, nowFee - nowRev) }
```

**그리고 3일차에 그 약속이 지켜진다.** 창고가 7칸이 되는 날(대본이 정확히 그렇게 넣는다) 박 반장이 같은 계산을 뒤집어 보여 준다.

> **박 반장** — 이제 부를 만해. 저 버튼에 **7/7칸**.
> **박 반장** — 지금 부르면 130c 받고 배차비 51c — **79c 남아.** 첫날이랑 같은 51c야. 눌러 봐.

**16c 손해 → 79c 남음.** 같은 51c로. 이게 이 게임의 핵심 규칙 전부이고, 플레이어는 그걸 두 번의 실제 숫자로 본다. 첫날 대사는 6장에서 **5장**으로 줄었고, 그중 3장이 숫자다.

### 대기 팝업은 자체 배송이 열릴 때까지 없앤다

「대기」를 누르면 뜨던 팝업에는 서장에서 고를 것이 하나도 없었다 — 직접 배송 목록은 잠겨 있고(`self`), 야외 재배치도, 밴도 없다. 남는 건 부연 설명뿐이었다.

```js
if (!selfIds && !game.shows('self')) {
  const f = game.forecast(), w = [];
  if (f.overdue) w.push(T('wm.overdue', { n: f.overdue }));
  if (f.spoil) w.push(T('wm.spoil', { n: f.spoil }));
  if (w.length) toast(T('wm.ifWait') + ': ' + w.join(' · '), 2600);
  doWait([]); return;
}
```

누르면 그대로 하루가 넘어간다. 팝업이 알려 주던 **경고(반송·부패 임박)는 토스트로 남겼다** — 정보를 없앤 게 아니라 모달을 없앤 것이다. `self` 는 3장에서 열리므로 서장·1장·2장이 해당된다. 팝업 안에서 "여기를 눌러" 하던 `l1waitGo` 비트도 같이 지웠다.

부수 효과: 51일 UI 플레이가 229초 → 191초. 하루에 모달을 한 번씩 덜 여니까.

### 검증

`test/play-ui.js` 에 검사 셋 — 첫날 비트가 끝나는지, `l1waitGo` 가 더는 안 나오는지, **대기가 팝업 없이 하루를 넘기는지(25일)**. 단위 76개, `level-ui`, `intro-ui` 전부 통과. 첫날·3일차 대사는 브라우저에서 직접 읽어 숫자(7칸/51c/2칸/35c/16c → 7/7칸/130c/79c)를 확인했다.

## 부록 AC. 첫 사이클은 기한이 없다 (v1.17)

유저: **"맨 첫 사이클에서는 기한도 빼면 좋겠어. 무기한으로 놔두고 기한이 있는 택배가 나중에 오는 걸로."**

부록 AB 에서 첫날을 "숫자로" 고쳤는데, 그 숫자는 **배차비 하나**였다. 그런데 화면에는 처음부터 ⏳ 가 같이 떠 있었다. 배우는 자리에서 규칙이 둘이면 둘 다 흐려진다.

### 무기한

첫 사이클(반월 13영업일)에 들어오는 택배에는 **기한을 붙이지 않는다.**

```js
// _mkParcel
noDeadline: this.month <= (R.noDeadlineCycles || 0)
```

`levels.js` 의 서장 mods 에 `noDeadlineCycles: 1`. 값이 사이클 수라서 나중에 장마다 조절할 수 있다.

무기한 택배는

- 하루가 지나도 `deadline` 이 줄지 않는다 (폭염 감소도 안 받는다)
- 기한 초과도, 반송도 없다
- 화면에 ⏳ 대신 흐린 **「기한 없음」**
- 자동 선택·긴급도 정렬에서 맨 뒤 (`_urgencyKey` 에 +90) — 급할 게 없으니 급한 것부터 실린다

`p.deadline` 숫자 자체는 그대로 둔다. 정렬·저장·마이그레이션이 전부 그 숫자를 쓰고 있어서, 없애는 것보다 **안 쓰는 쪽**이 훨씬 안전하다.

**함정 하나.** 첫 시도에서 `if (!p.overdue && !p.noDeadline)` 로 막았더니 무기한 택배가 통째로 `else` 가지 — 즉 **기한 초과 반송 처리**로 쏟아졌다(반송 16). 조건을 덧붙이는 대신 `if (p.noDeadline) continue;` 로 아예 빠져나가야 한다. 분기 하나를 좁히면 반대쪽이 넓어진다.

### 기한은 사람이 들고 온다

2사이클 첫 기한부 택배가 들어온 날, **그걸 가져온 사람이 직접 말한다.**

> **큰마트 김 대리** — 이번 주부터는 급한 것도 같이 보냅니다. 날짜 적힌 건 그 안에 나가야 해요.
> **박 반장** — 보이지? 이제 ⏳ 가 붙어. 저 숫자가 남은 날이야. 지금까진 급한 게 없어서 안 붙었던 거고.

`l1due` 비트(`when: g.parcels.some(p => !p.noDeadline)`). 기존 `l1deadline`(「저건 내일까지야」)은 `l1due` 를 본 뒤에만 뜨도록 순서를 묶었다.

실제 진행 순서: `l1intro → l1mart → l1call → l1callPick → l1callGo → l1first → l1free → l1sunday → l1summary → l1due → l1deadline → l1last`. 1사이클은 **차를 꽉 채워 보낸다** 하나만 배우고, 정산을 한 번 본 뒤에 기한이 들어온다.

### 곁다리

똑같이 생긴 택배를 접는 묶음 줄(`parcelGroupRow`)에 고객 아이콘 게이트가 빠져 있었다. 1사이클에는 남은 날짜가 전부 같아져서 묶음이 훨씬 자주 생기는 바람에 드러났다 — 서장에서 잠가 둔 고객 아이콘이 묶음 줄로 새고 있었다.

### 검증

단위 77개(신규: 1사이클 전부 무기한 · 12영업일이 지나도 기한이 안 줄고 반송 0 · 2사이클 택배에는 기한이 붙는다), `play-ui` 51일(첫 사이클 ⏳ 없음 · `l1due` 가 실제로 나온다 · 반송·폐기 0 · 처리 88개 · 남은 자금 3506c — 기한을 빼기 전과 같은 숫자), `sim.js` 봇 결과도 이전과 같다.

## 부록 AD. 신뢰도도 나중에 (v1.19)

유저: **"초반엔 신뢰 관련된 내용도 빼고 튜토리얼에 나오기 전까지는 숨기는 게 좋을 것 같아."**

### 새 플래그 `trust`

`levels.js` FLAGS 에 `trust` 를 더하고 **3장**에서 연다(`weather` · `theft` · `self` 와 같이). 신뢰는 계약을 여러 번 불러야 쌓이는 것이니 마켓(2장)이 열린 다음 장이 맞다.

꺼져 있어도 **쌓이기는 한다** — 화면에만 안 나온다. 3장에서 열리는 순간 그동안의 신뢰가 그대로 보인다.

가리는 곳을 일일이 찾는 대신 **원천 둘을 막았다.**

```js
function trustBar(g, carrier) { if (g && !g.shows('trust')) return ''; … }
function trustTrack(carrier, xp) { if (game && !game.shows('trust')) return ''; … }
```

이 둘이 계약 카드 · 마켓 현재 계약 · 상세 팝업 · 내 계약 목록 · 슬롯 고르기 · 도감을 전부 먹는다. 나머지는 네 군데만 손댔다 — 호출 팝업의 신뢰 줄(`trustInfo`), 계약 상세의 `신뢰` 표 행(라벨만 남으면 안 되니까), 등급 상승 토스트(`announceTrust`), 그리고 빈 문자열이 되면 구분점이나 `<br>` 이 남는 두 자리.

### 같이 새던 것들

신뢰를 막고 나서 서장 화면을 다시 열어 보니 같은 결의 누수가 더 있었다.

| 자리 | 전 | 후 |
|---|---|---|
| 택배 상세 · 고객 줄 | 「고객 🛒 큰마트 · 신뢰 1단계 (개당 +5)」 + 고객 규칙 | `customers` 가 열릴 때만 |
| 택배 상세 · 첫 줄 고객 아이콘 | 🛒 | 택배 줄과 같은 게이트 |
| 택배 상세 · 직접 배송 행 | 「직접 배송 불가 —」 | `self` 가 열릴 때만 (잠긴 기능을 '불가'로 보여 줄 이유가 없다) |
| 호출 팝업 · 머리글 | `car.desc` 「속성 없는 택배(크기 1~2)를 탑차에 실어 보낸다. 일반 특화의 축 · 표준 계약」 | 삭제 (상세 팝업과 같은 이유 — 부록 v1.18) |
| 호출 팝업 · 보너스 | 「보너스: 동네 단골: 이달 첫 호출 차량 +1칸」 | `perks` 가 열릴 때만 |
| 호출 팝업 · 능력 줄 | 「🚚 능력 없음 · 크기 1~2」 | `attrs` 가 열릴 때만 (속성이 없는 장에서 「능력 없음」은 아무것도 안 알려 준다) |
| 호출 팝업 · 잔여 배차 | 「잔여 7대 → 6대」 | `calls` 가 열릴 때만 |
| 호출 팝업 · 동시 한도 | 「동시 최대 1대」 | `simul` 이 열릴 때만 |

### 결과

서장의 호출 팝업은 이렇게 된다.

```
한길 물류
탑차 7칸 · 대당 51c
차량 1대 · 적재 0/7칸
+0c − 51c = 순익 -51c
선택 0/5개
[택배 줄들]
급한 순 자동 선택 · 선택 해제
```

택배 상세는 네 줄이다 — 택배 한 줄, 「보낼 수 있는 곳」, 한길 7회, 닫기.

### 검증

단위 77개, `play-ui` 51일(신규: 서장 화면에 `.trust` 가 0개 · 택배 상세가 실제로 열리고 그 안에도 신뢰·고객·직접 배송이 없다), `level-ui`. 자유 런에서는 전부 그대로 나오는 것도 브라우저에서 확인했다(`shows('trust') === true` · 신뢰 막대 1개 · 보너스·능력·잔여·동시 모두 표시).

### 덧 — 소개 카드에서 숫자를 뺀다 (v1.19.1)

유저: **"박 반장 옆에 스무 해는 뭐야? 그리고 주인의 정확한 나이는 없어도 될 것 같아."**

맞다. 인물 소개 카드는 **이름 아래 한 줄**이고, 그 한 줄은 *이 사람이 여기서 뭐 하는 사람인가*에 답해야 한다. 「· 스무 해」는 그 자리에서 무엇의 스무 해인지 알 수 없는 숫자였고(경력이라는 건 카드만 봐서는 모른다), 「· 여든둘」은 알 필요가 없는 숫자였다.

| 키 | 전 | 후 |
|---|---|---|
| `card.han.desc` | 이 창고의 주인 · 여든둘 | **이 창고의 주인** |
| `card.park.desc` | 이 창고 반장 · 스무 해 | **이 창고 반장** |
| `intro.3` | 한종수. **여든둘.** 마흔에 리어카 한 대로 시작했다고 했다. | 한종수. 마흔에 리어카 한 대로 시작했다고 했다. |

경력과 나이는 대사에 남아 있다 — 박 반장의 「스무 해 했고」는 문장 안이라 무엇의 스무 해인지 알 수 있고, 영감님이 노인이라는 건 지팡이와 말투와 「내가 이제 못 들겠어」가 이미 말한다. 숫자로 못 박을 이유가 없다.

## 부록 AE. 서장 끝은 양도가 아니라 가계약이다 (v1.20)

### 무엇이 이상했나

서장(두 달)을 끝내면 **창고 매매 계약서**가 나오고 도장을 찍으면 창고가 그냥 넘어왔다.
두 가지가 어긋났다.

1. 계약서 본문이 "값은 이미 **한 해 동안** 몸으로 치렀다" 였다. 실제로는 두 달만 굴렸다.
   6장까지 붙으면 맞는 문장이지만, 지금은 서장이 마지막 구현 장이라 두 달 만에 한 해가 끝난 것처럼 보인다.
2. 값이 **무상**이었다. 끝내면 주는 것이라 "이번 달에 얼마를 남겨야 하나"를 계산할 이유가 없다.

### 고친 것 — 값을 부르고, 계약금을 걸고, 잔금을 채운다

`levels.js` 에 매매 조건을 둔다.

```js
const DEAL = { price: 8000, downRate: 0.6 };
```

- **매매 대금** `price` — 한 해에 걸쳐 채울 목표 금액.
- **계약금** — 서장을 끝낸 시점 자금의 `downRate` 만큼. 잘 벌었으면 많이 걸고 잔금이 줄어든다.
- **잔금** `price − 계약금` — 남은 열 달(2~6장) 안에 채운다. 이자는 없다.

서장 끝 화면은 **가계약서**가 되고, 값·계약금·잔금 세 줄이 계약서 위에 적힌다.
도장을 찍으면 계약금이 실제로 빠져나가고 `profile.campaign` 에 남는다.

```js
P.campaign.deal  = { price, paid, rest };   // 목표 금액과 지금까지 낸 돈
P.campaign.carry = { cash: left };          // 다음 장은 계약금 뺀 돈으로 시작한다
```

서장 종료 자금 1,476c 기준 → 계약금 885c · 잔금 7,115c · 이월 591c.
서장이 두 달에 1,176c 를 남겼으니 열 달에 7,115c 는 마켓·확장이 열리는 2장부터 수입이 커지는 것을 전제한 값이다.
**`price` 는 2장이 붙으면 다시 잡는다** — 지금은 서장만 돌려 본 임시 값이다.

### 문구

| 키 | 전 | 후 |
| --- | --- | --- |
| `ct.title` | 창 고 매 매 계 약 서 | 창 고 매 매 **가** 계 약 서 |
| `ct.body` | 값은 이미 한 해 동안 몸으로 치렀다 | 값을 {price}c 로 한다 · 오늘 계약금 {down}c · 잔금 {rest}c 는 남은 열 달 안에 |
| `lv.nameBody` | 한 해를 다 굴렸네 | 두 달 굴려 봤으니 이제 간판을 달아야지 |

`ct.price` · `ct.down` · `ct.rest` · `ct.after` 를 새로 넣었다 (ko/en 동시).
편지(`letter.1.*`)는 원래부터 "남은 열 달, 계속 해 보게" 였다 — 이제 계약서와 말이 맞는다.

### 검증

- `test/play-ui.js`: `값 = 계약금 + 잔금`, `계약금이 빠지고 잔금 목표가 남는다` 두 검사 추가.
- `test/level-ui.js`: 프로필 덤프에 `deal`·`carry` 가 찍힌다.
- 단위 78 · play-ui · level-ui · intro-ui · tutorial-ui 전부 통과.

### 덧 — 같이 고친 것 (v1.20)

- **한 해 달력의 '이번 달'** — `x.m === g.month` 로 비교하고 있었다. 달력 칸은 개월차인데 `g.month` 는
  사이클(반월)이라, 3월 후반(사이클 2)에 4월 칸이 켜졌다. `g.monthIndex()` 와 비교하도록 고치고 단위 테스트를 붙였다.
- **호출 팝업의 코치 문구** — "적재 효율 ✓ 신뢰 +1" 과 "적재 50% · 2칸 더 채우면 80%" 를 `shows('trust')` 뒤로 숨겼다.
  신뢰를 안 가르친 서장에서 신뢰 보상을 말하고 있었고, 적재율 잔소리도 서장에는 과한 친절이었다. 차 게이지에 `3/6칸` 이 이미 있다.
- **기한 없음** — 무기한 택배의 `기한 없음` 표시를 아예 지웠다. 빈 칸이 곧 '급할 일 없음'이다. `ps.noDeadline` 키는 ko/en 에서 삭제.

## 부록 AF. 장 카드 — 각 장의 처음과 끝 (v1.21)

### 왜

서장이 끝나면 곧장 타이틀 화면으로 떨어졌다. 장이 **책의 장처럼** 느껴지지 않고
"판이 하나 끝났다"로 읽혔다. 시작도 마찬가지로, 38초짜리 컷씬이 끝나면 바로 박 반장이 말을 걸어서
**지금 무슨 장을 시작하는지**가 화면에 한 번도 안 나왔다.

영화 챕터 카드를 넣는다. 규칙은 한 줄도 말하지 않는다 — 제목·부제·달력, 그것뿐이다.

### 시작 카드 — 컷씬이 끝난 뒤

```
        서 장
       빈 창고
   2027년 3월 — 4월

  화면을 누르면 시작합니다
```

`startPlay({ intro, chapter })` 가 컷씬(`Intro.play`)이 끝난 자리에 `showChapterStart(n)` 을 끼운다.
**이어하기는 카드가 없다** — `chapter` 를 안 넘기기 때문이다. 컷씬을 꺼도(`?nointro`) 카드는 뜬다.
달력 줄은 하드코딩이 아니라 `LEVELS.startCycle(n)` 과 그 장의 `cycles` 에서 계산한다.

### 끝 카드 — 편지 뒤, 다음 장 예고까지

```
      서장 · 끝
 셔터는 이제 내 손으로 올린다.
 ─────────────────
        다음
   1장 · 두 번째 트럭  준비 중

  화면을 누르면 이어집니다
```

`리포트 → 편지 → 끝 카드 → (다음 장 또는 상호·계약서)`.
**다음 장이 구현돼 있으면 타이틀을 거치지 않고 바로 그 장으로 들어간다** — 장은 이어지는 것이지
매번 메인으로 나갔다 들어오는 것이 아니다.

```js
function afterChapter(r) {
  const next = r.level + 1, hasNext = next <= LEVELS.IMPLEMENTED;
  showChapterEnd(r.level, Math.min(next, LEVELS.LAST), () => {
    if (!hasNext) return askCompanyName(r);           // 마지막 구현 장 → 상호·가계약서
    P.campaign = { …cleared, level: next, carry: { cash: r.cash } };
    startLevel(next);                                  // 타이틀을 거치지 않는다
  });
}
```

지금은 `IMPLEMENTED: 1` 이라 서장 끝 카드가 `1장 · 두 번째 트럭 (준비 중)` 을 예고하고 상호 입력으로 간다.
1장이 붙는 순간 같은 코드가 곧바로 이어 붙는다.

### 부제 — 영화 챕터처럼

게임 규칙을 요약하지 않는다("쌓았다가, 보낸다" 같은 것). 그 장의 **그림 한 장**을 짧은 명사구로.

| 장 | 달력 | 부제 (ko) | (en) | 맺음말 |
| --- | --- | --- | --- | --- |
| 서장 | 3~4월 | 빈 창고 | The Empty Depot | 셔터는 이제 내 손으로 올린다. |
| 1장 | 5~6월 | 두 번째 트럭 | The Second Truck | 마당에 차가 한 대 더 늘었다. |
| 2장 | 7~8월 | 장마 | Monsoon | 비가 그치고, 마당이 말랐다. |
| 3장 | 9~10월 | 차가운 짐 | Cold Freight | 찬 것들이 제 자리를 찾았다. |
| 4장 | 11~12월 | 소문 | Word Gets Around | 이름이 동네를 넘었다. |
| 5장 | 1~2월 | 마지막 겨울 | Last Winter | 설 물량이 마지막으로 빠져나갔다. |

키는 `lv.sub.<n>` · `ch.end.<n>` · `ch.when` · `ch.tap` / `ch.tapNext` · `ch.endTitle` · `ch.nextLabel` · `ch.soon` (ko/en).

### 구현 메모

- `chapterCard(cls, inner, onTap)` 하나가 두 카드를 다 만든다. 타이틀과 같은 `.titlecard` 틀(모달 테두리 없음)을 쓰되
  `.chcard` 로 가운데 정렬·페이드인. **카드 전체가 탭 대상**이고 한 번만 먹는다(`used` 플래그).
- `.modal h2` 가 라벨 배경(`var(--line)`)에 `display: flex` 라서 제목이 왼쪽 정렬된 띠로 나왔다 —
  `.chcard h2` 에서 `display: block; background: none; text-align: center` 로 되돌린다.
- 카드가 뜨는 동안 대화창·모달이 안 뜬다. `go()`(첫 비트·문자)는 카드를 탭한 뒤에 돈다.

### 검증

단위 78 · play-ui(장 시작 카드 2검사 + 끝 카드 1검사 추가) · level-ui(2검사 추가) · intro-ui(2검사 추가) 전부 통과.
`shots/P-00c-chapter.png` · `shots/P-42b-chapter-end.png` 로 화면 확인.

## 부록 AG. 1장 — 배차가 소모품이라는 것 (v1.22, 레벨 2)

서장 하나로 멈춰 있던 캠페인에 두 번째 장을 붙였다. 장마다 필요한 것이 셋이다 —
**이어받기**, **대본**, **전용 비트**. 이어받기는 한 번만 만들면 되고, 나머지는 장마다 쓴다.

### 1. 이어받기 (`campaign.carry`) — 한 런이 이어지는 것처럼

`levels.js` 주석에만 있고 아무도 안 쓰던 자리를 채웠다.

```js
// game.js
carryState() {
  return { cash, warehouse: { cap, cold, frozen, xl },
           contracts: [{ carrier, grade, calls, enh }],
           customers: [[id, level]], trust: { …xp },
           seen: story.seen, notes: story.notes };
}
```

- `_initCompany` / `_initCustomers` 가 `cfg.carry` 를 **레벨의 시작 판보다 먼저** 본다.
- 계약은 강화(`enh`)와 남은 배차(`calls`)까지 그대로 간다.
- **신뢰도 xp 도 넘어간다** — 3장 전까지 화면에 안 보이지만 쌓이고 있었고, 1장에서 두 대가 붙는 것도 서장에서 쌓은 신뢰 1단계 덕이다.
- **`story.seen` 도 넘어간다.** 이게 없으면 박 반장이 장마다 "차는 매일 부르는 게 아니야"를 다시 말한다.
- `level.minCash` — 서장을 망쳐도 다음 장이 막히지 않게 바닥값을 보장한다(1장 400c).

### 2. 안 연 품목은 아예 오지 않는다 (규칙 쪽 문)

앞 두 사이클만 대본이고 뒤 두 사이클은 무작위로 풀기로 했는데, 그대로 돌리자 **반송 11 · 폐기 6** 이 났다.
무작위 입고가 ⚠ 파손 · 🌾 농산 · 🛃 통관 · ❄ 신선 · 4칸 대형을 뱉었기 때문이다 —
1장에는 그걸 받아 줄 계약도, 둘 냉장 구역도 없다. **반송 말고는 길이 없는 상황**이다.

화면에서 숨기는 것(`shows`)만으로는 부족하고, 문이 규칙 쪽에도 있어야 한다.
`_typeRatio` 가 이미 🛃·❆ 를 평판 등급으로 막고 있었으므로 같은 자리에 레벨 문을 달았다.

```js
const gate = { fragile: 'attrs', produce: 'attrs', intl: 'attrs', large: 'attrs', fresh: 'cold', frozen: 'frozen' };
for (const t in gate) if (base[t] && !this.shows(gate[t])) { base.normal += base[t]; base[t] = 0; }
```

그 몫은 일반으로 간다. 이걸 넣고 **반송 0 · 폐기 0**. 크기 문제(4·7칸)도 같이 사라진다 — 일반은 1~2칸뿐이다.

### 3. 1장 (5~6월) — `LEVELS[1]`

| | |
| --- | --- |
| 달력 | 2027년 5~6월 · `startMonth: 5` · `monthOffset: 2` |
| 여는 것 | `market` · `calls` · `simul` |
| 시작 판 | 서장에서 그대로 (계약금 885c 뺀 591c · 16칸 · 한길 물류 하나) |
| 대본 | 1~2사이클만. 3~4사이클(6월)은 무작위 |

`monthOffset: 2` 가 없으면 물량·가격·등급 표를 개월차 1~2로 읽어서 **6월인데 3월만큼만 온다**.
장마다 한 해의 어디인지를 표에 알려 줘야 한다 (2장 +4 · 3장 +6 · 4장 +8 · 5장 +10).

**`callsDelta: +3`** — 계약이 하나뿐인 장이라 배차가 사이클 중간에 마르면 남은 열흘을 통째로 못 보낸다.
마켓은 사이클 끝에만 열리니까, **한 번 충전하면 한 사이클이 도는 그릇**이어야 한다.
(실제로 이걸 넣기 전 봇이 4사이클 2일차에 배차가 떨어져 열하루를 대기만 하다 반송 13을 냈다.)
배차가 바닥나는 경험은 1사이클 대본이 **마지막 날에 한 번만** 만들어 준다.

대본이 만드는 자리:

| 사이클 | 달력 | 대본 | 마켓 |
| --- | --- | --- | --- |
| 1 | 5월 전반 | 하루 3~4칸씩 55칸 — 배차 7대(49칸)를 다 쓰고 마지막에 바닥난다 | 충전(상시) · 한도 강화 |
| 2 | 5월 후반 | 3일차에 몰아 줘 **14칸 = 두 대(7+7) 정확히**. 뒤로 갈수록 창고가 빡빡해진다 | 충전 · 한도 · **창고 확장** |
| 3~4 | 6월 | 무작위 (일반 택배만 온다) | 무작위 |

창고 확장은 **2사이클 마켓에서 처음 판다** — 넘쳐 본 다음에 파는 것이 순서다(부록 M 과 같은 원칙).

### 4. 1장 전용 비트 (`BEATS_L2`, 10개)

`check()` 가 `LEVEL_BEATS[level.n]` 로 장별 비트 표를 고른다(없으면 기존 `BEATS`).
새로 여는 셋만 말한다 — 서장에서 가르친 쌓기·적재율·기한은 한 번도 다시 꺼내지 않는다.

`l2intro`(배차 숫자가 새로 붙었다) → `l2calls`(호출하니 하나 줄었다) → `l2callsOut`(0이다, 월초에 안 채워진다)
→ `l2market`(여기가 마켓) → `l2refill`(충전은 정액 — 남은 게 있으면 버리는 것) → `l2limit`(그릇을 키운다)
→ `l2two`(두 대가 붙었다 · 배차도 배차비도 둘) → `l2cap`(넘쳐 봤으니 확장) → `l2usage` → `l2last`.

### 5. 상호·가계약서는 서장에 고정

지금까지 "**구현된 마지막 장**을 끝내면 상호를 묻는다"였다. 1장이 생기자 그 자리가 1장 끝으로 밀렸다 —
박 반장이 서장 마지막에 "이번 달만 넘기면 간판 하나 붙여 봐"라고 해 놓고 두 달 뒤에 묻는 꼴이다.

`afterChapter` 를 고쳤다: **`r.level === 1` 일 때만** 상호 → 가계약서를 거치고, 도장을 찍으면
계약금을 뺀 판(`carry.cash = left`)으로 그대로 1장이 이어진다. 본계약서(잔금 완납)는 5장 끝 자리다.

### 6. 검증

| | |
| --- | --- |
| 단위 | 78 |
| `level-run 1` | 반송 0 · 폐기 0 · 300 → 1,476c |
| `level-run 2` | 반송 0 · 폐기 0 · 기한초과 0 · 600 → 1,443c · 처리 103개 |
| `level-ui` | 서장 → 끝 카드 → 상호 → 가계약서 → **타이틀 없이 1장 카드** → 자금·계약·창고·본 비트가 넘어온 것 확인 |
| `play-ui` | 같은 흐름을 51일 UI 플레이로. `1장에 열린 것은 마켓·배차·동시뿐` · `1장 입고는 아직 일반뿐` |
| `intro-ui` · `tutorial-ui` | 회귀 통과 |

### 남은 것

- 2~5장(3장~5장 커리큘럼은 `levels.js` 에 선언만). 같은 틀을 그대로 쓰면 된다 — `startMonth`·`monthOffset`·대본 2사이클·`BEATS_Lx`.
- 1장 `callsDelta: +3` 은 계약이 하나뿐이라 필요한 값이다. 2장에서 계약이 둘이 되면 다시 본다.
- `DEAL.price 8000` 은 여전히 임시값. 1장까지 돌면 1,443c 가 남으니, 열 달에 7,115c 는 아직 빡빡한 편이다.

## 부록 AH. 2장 — 장마 (v1.23, 레벨 3)

여는 것 넷: **날씨·예보** · **야외 적재와 젖음·도난** · **직접 배송** · **신뢰도**.
앞의 셋은 새 규칙이고, 넷째는 새 규칙이 아니라 **줄곧 돌고 있던 것의 공개**다 —
두 대가 붙은 것도, 배차비가 싸진 것도 서장부터 쌓인 신뢰도였다. 그 영수증을 여기서 처음 보여 준다.

### 대본이 만드는 자리

| 사이클 | 달력 | 무엇이 오는가 | 가르치는 것 |
| --- | --- | --- | --- |
| 1 | 7월 전반 13일 | 하루 3~4칸으로 잔잔하다가 **11일차에 26칸** | 장마 전 밀어내기 → 창고가 넘쳐 마당으로 → 그날부터 비 |
| 2 | 7월 후반 14일 | 하루 3칸 (1사이클이 남긴 30칸을 같이 밀어내야 한다) | 배차가 마지막에 마른다 → 직접 배송 · 갈아타기 매물 |
| 3~4 | 8월 | 무작위 | 혼자 |

**11일차 26칸**이 이 장의 핵심 장치다. 차가 하루에 옮길 수 있는 건 많아야 두 대 14칸이라,
플레이어가 어떻게 굴려도 그날은 창고가 넘친다 — 그리고 그날부터 비다.
(처음에는 물량을 완만하게 늘려 봤는데, 봇이 매일 호출해 버려서 창고가 8칸을 안 넘었다.
**하루 최대 처리량보다 큰 하루**를 만들지 않으면 마당은 영영 안 쓰인다.)

2사이클 마켓에 **`bulk1` 갈아타기 매물**을 올렸다. 신뢰도를 배운 바로 다음에
"갈아타면 신뢰는 0부터, 남은 배차도 같이 날아간다"가 오도록 한 것이다.

폭염(`heat`)은 화면에 뜨지만 아직 할 일이 없다 — ❄ 신선이 오는 3장에서 다시 꺼낸다(`heatAlerts: 0`).

### 비트 `BEATS_L3` (12개)

`l3intro`(예보 줄) → `l3forecast`(비 표시를 읽는 법) → `l3yard`(넘친 건 마당으로) →
`l3rain`(젖으면 20% 깎인다 · 대기 게이트) → `l3reorder`(적재 정리) → `l3theft`(마당은 문이 없다) →
`l3self`(차가 없을 땐 내가 나른다) → `l3selfPick` → **`l3trust`**(3쪽: 정체 · 왜 두 대가 붙었는지 · 어떻게 오르는지) →
`l3switch`(갈아타면 0부터) → `l3yardBuy`(마당 울타리) → `l3last`.

### 두 군데서 판이 터졌다

**① 배차 0으로 끝낸 장은 다음 장 첫 사이클을 통째로 못 보낸다.**
1장을 배차 0으로 끝내자 2장이 0으로 시작했고, 마켓은 사이클 끝에만 열리니 **열세 날을 손도 못 썼다**(반송 28).
장 사이에는 두 달의 공백이 있으니 그동안 새로 끊어 둔 것으로 친다 — `D.CARRY_CALLS_FLOOR = 0.6`,
물려받은 계약은 `maxCalls` 의 60% 를 바닥으로 보장한다. `level.minCap` 도 같은 이유로 추가(창고 바닥값).

**② 뒤 사이클이 앞 사이클의 밀린 물량을 같이 져야 한다.**
2사이클 물량을 1사이클과 비슷하게 잡았더니 6일차에 배차가 말라 여드레를 서 있었다(반송 10).
1사이클이 30칸을 남기는 대본이므로 **2사이클은 그만큼 가볍게** 짜야 한다 — 76칸 → 42칸.

### `test/campaign-run.js` (신규)

장을 하나씩 검사하던 `level-run.js` 로는 위 두 문제를 못 잡는다. **이어서** 돌려야 나온다.
구현된 장을 처음부터 끝까지 `carryState()` 로 물려 가며 굴리고, 장마다 확인한다 —
완주 · 반송/폐기 0 · 흑자 · **열린 기능이 `grants` 누적과 일치** · **입고에 안 연 품목이 없다** · 계약과 본 대사가 이어졌다.
봇도 3장부터 직접 배송을 쓴다(`g.wait(selfIds)`).

```
서장   300 → 1,476c · 처리  87 · 반송 0
1장  1,476 → 2,504c · 처리 103 · 반송 0
2장  2,504 → 3,460c · 처리 121 · 반송 0 · 도난 0
```

### 검증

단위 78 · `campaign-run` 전부 통과 · `sim`(자유 런) 회귀 · `level-ui` · `play-ui` · `intro-ui` · `tutorial-ui`.

### 남은 것

- 3~5장. 3장(9~10월)은 ❄ 신선·냉장 구역·고객이라 **품목 문(`_typeRatio`)을 처음 여는 장**이다.
- 2장 2사이클의 기한 초과는 직접 배송을 쓰면 사라진다 — 사람 플레이테스트에서 그 유도가 먹히는지 확인.
- `DEAL.price 8000`: 2장까지 3,460c. 남은 여섯 달에 잔금 7,115c 는 아직 빡빡하다.

## 부록 AI. 3장 — 차가운 짐 (v1.24, 레벨 4)

다섯 달 내내 일반 택배만 왔다. **품목 문이 처음 열리는 장**이라 밸런스가 제일 크게 흔들렸고,
실제로 규칙 쪽 구멍이 셋 나왔다.

### 커리큘럼을 다시 나눴다

원래 4장(레벨 4)의 `attrs` 하나가 ⚠ 파손 · 🌾 농산 · 🛃 통관 · **4칸 이상 대형**을 한꺼번에 열었다.
한 장에 넷은 너무 많고, 통관은 원래 5장(레벨 6) 커리큘럼이다. 플래그를 둘 쪼갰다.

| 새 플래그 | 여는 것 | 어느 장 |
| --- | --- | --- |
| `bigsize` | 4칸 이상 대형 택배 | 4장 (레벨 5) |
| `customs` | 🛃 통관 품목 | 5장 (레벨 6) |

`_typeRatio` 의 문도 따라 바뀌었다 — `fragile·produce → attrs` · `fresh → cold` · `large → bigsize` · `intl → customs` · `frozen → frozen`.

### 3장 (9~10월)

| | |
| --- | --- |
| 달력 | 2027년 9~10월 · `startMonth: 9` · `monthOffset: 6` |
| 여는 것 | `attrs`(⚠🌾) · `cold`(❄ + 냉장 구역) · `customers` |
| 대본 | 1사이클 — 아직 일반뿐이지만 **이름 있는 화주가 붙는다**. 마켓이 냉장 계약 + 냉장고를 판다<br>2사이클 — ❄ 신선이 처음 온다. 3~4사이클은 무작위(⚠🌾 섞여서) |

**❄ 는 둘 곳과 보낼 곳이 둘 다 있어야 한다.** 그래서 오기 전에 마켓이 먼저 온다 —
1사이클 마켓이 `cold0` 계약과 `cold1` 냉장고를 팔고, 예보 줄이 "다음 보름에 ❄ 가 오는데 받을 데가 없다"고 붉게 알린다(부록 Q 의 `forecastBlocked`).

`level.addCustomers` 를 추가했다 — 장이 새 화주를 **물려받은 목록 위에 더한다**(덮어쓰지 않는다).
3장에서 🛒 큰마트 · 🏺 유리공방 · 🌾 농협이 처음 붙는다.

비트 `BEATS_L4` 13개: `l4intro` → `l4cust`(화주가 뭔지) → `l4warn`(오기 전에 사라) → `l4coldCar`/`l4coldFac`(보낼 곳과 둘 곳) →
`l4cold`(들어왔다) → `l4coldFull` → `l4fragile`/`l4risk`(확률과 배상) → `l4produce` → `l4break` → `l4custUp` → `l4last`.

### 규칙 쪽 구멍 셋

**① 마켓이 안 열린 기능의 해결책을 팔고 있었다.**
2장 마켓에 냉동고·설원 냉동물류·통관 대행 특약이 떴다. 그 품목은 오지도 않는데 돈만 태우는 함정이다.
`_marketAllowed(it)` 로 시설·보험·고객·계약·강화를 전부 플래그로 걸렀다.

**② 그런데 거르기만 하면 '막힌 품목 보장'이 깨진다.**
3장 3사이클에 ⚠ 파손이 들어왔는데 마켓에 파손 계약이 없었다. 원인: `guaranteeBlocked` 가
해결책으로 **철도·항공**을 골라 놓고(그 계열도 ⚠ 를 받는다), 내 필터가 그걸 지웠고, 대신 들어올 것이 없었다.
→ **거르기 전에 가중치에서 뺀다.** `_familyOpen(fam)` 을 만들어 `_carrierWeights()` 가 닫힌 계열을 아예 안 내놓게 했다.

**③ 품목은 막았는데 크기는 안 막았다.**
`large` 타입은 막았지만 `fresh 4칸`·`fragile 4칸` 이 그대로 왔다 — 그걸 실을 계열(대형·철도·해상)도 마켓에 없는데.
`_genParcelSpec` 에서 `!shows('bigsize')` 면 4칸 이상 가중치를 통째로 뺀다.

### 대본이 앞 장의 구매에 흔들린다

2장의 11일차 폭주를 26칸으로 잡았는데, 봇이 1장에서 창고 확장을 두 번 사서 36칸으로 들어오자
**넘치지 않아 `l3yard`·`l3rain` 이 아예 안 나왔다**. 34칸으로 올렸다.
장이 이어지는 구조에서는 **대본의 숫자가 앞 장의 씀씀이에 따라 의미가 달라진다** — 조건부 비트라 안 나와도 깨지진 않지만,
그 장의 핵심 교훈이면 여유 있게 잡아야 한다.

### 검증

`test/level-run.js` 의 마켓·대기 처리를 `campaign-run.js` 와 같은 것으로 맞췄다
(두 벌을 따로 두니 같은 장이 한쪽에서는 반송 0, 다른 쪽에서는 폐기 13이 나왔다).

```
서장   300 → 1,476c · 처리  87 · 반송 0
1장  1,476 → 2,069c · 처리 107 · 반송 0
2장  2,069 →   897c · 처리 127 · 반송 0   (봇이 창고 확장에 크게 씀)
3장    897 → 2,682c · 처리 139 · 반송 0 폐기 0
```

단위 78 · `campaign-run` 전부 통과 · `level-run 1~4` · `sim`(자유 런 회귀) · `level-ui` · `play-ui` · `intro-ui` · `tutorial-ui`.

### 남은 것

- 4장(11~12월: 평판·보험·보관·대형) · 5장(1~2월: 냉동·통관·주말 선택·퍽).
- 4장은 **평판이 열리는 장**이라 런 종료 조건이 처음 생긴다 — `noBankrupt` 를 언제 뗄지도 여기서 정해야 한다.
- `DEAL.price 8000`: 3장까지 2,682c. 남은 넉 달에 잔금 7,115c 는 지금 수입 곡선으로는 무리다 — 4~5장 수입이 커지는 것을 보고 다시 잡는다.

## 부록 AJ. 마켓도 안 열린 것은 안 보여 준다 (v1.24.1)

유저 지적: **"마켓이 좀 번잡해. 슬롯도 당장은 네 개까지 필요없고 실제로 특수물품 나오기 전까지는
슬롯 하나로 가는 게 좋고 내 보험 이런 건 다 숨겨야 해."**

부록 R 의 원칙(안 보여 주는 것으로 가르친다)을 플레이 화면에만 적용하고 **마켓은 빠뜨리고 있었다**.
1장 마켓에 `냉장 0/0` · `초대형 0/0` · 「보험」 · 「고객」 · 빈 슬롯 세 칸이 그대로 있었다.
전부 그 장에 **존재하지 않는 것**이고, 특히 빈 슬롯 세 칸은 "여기를 채워야 한다"는 잘못된 숙제처럼 보인다.

| 뺀 것 | 언제 다시 나오나 |
| --- | --- |
| 창고 줄의 `냉장 n/n` | `cold` (3장) |
| 창고 줄의 `냉동 n/n` | `frozen` (5장) |
| 창고 줄의 `초대형 n/n` | `bigsize` (4장) |
| 「보험」 버튼 | `insurance` (4장) |
| 「고객」 버튼 | `customers` (3장) |
| 「내 계약」 버튼 | 계약 슬롯이 둘 이상일 때 |
| 계절 설명 줄 (`선물(파손주의) ↑` 같은 품목 얘기) | `attrs` (3장) |
| 「마켓 새로고침」 | `attrs` (3장) — 그전에는 매물이 대본으로 고정이라 의미가 없다 |
| 가격 배수 (`가격 ×0.90`) | `attrs` (3장) |

### 계약 슬롯은 쓸 만큼만

```js
visibleSlots() {                       // game.js
  if (!this._shows) return D.CONTRACT_SLOTS;          // 자유 런은 넷 그대로
  const n = !this.shows('cold') ? 1 : !this.shows('bigsize') ? 3 : D.CONTRACT_SLOTS;
  return Math.max(n, this.contracts.filter(Boolean).length);
}
```

- 서장~2장: **한 칸**. 계약이 하나뿐이고 2장의 갈아타기도 교체라 칸이 더 필요 없다.
- 3장(냉장이 열림): 세 칸 — 한길 + 냉장 + 파손.
- 4장(대형이 열림)부터: 네 칸.

마켓의 현재 계약 목록, 플레이 화면의 계약 버튼, 구매할 때의 슬롯 고르기 화면이 전부 이 값을 쓴다.
이미 가진 계약보다 적게 보여 주는 일은 없다(`Math.max`).

### 결과

1장 1사이클 마켓이 **2.3화면 → 1화면**. 남은 것은 자금·구매 한도, 창고 한 줄, 현재 계약 한 장,
다음 사이클 예상 물량 한 줄, 강화 카드 한 장, 시작 버튼뿐이다.

`test/level-ui.js` 에 「1장 마켓이 번잡하지 않다」 다섯 검사를 넣었다 —
냉장·초대형 없음 · 보험/고객/내 계약 버튼 없음 · 새로고침·가격 배수 없음 · 빈 슬롯 0칸.

## 부록 AK. 지나온 장으로 돌아갈 수 있어야 한다 (v1.24.2)

유저 지적: **"다시하기를 눌러도 서장으로 가는 방법이 없네. 1장에 도달하면 서장으로는 어떻게 가?"**

맞다. 타이틀의 「처음부터」는 **지금 장**을 다시 하는 것이고, `campaign.level` 은 앞으로만 간다.
한 장을 넘기고 나면 앞 장을 다시 볼 길이 프로필을 지우는 것밖에 없었다.

### 장별 시작 판을 남긴다

되돌아가려면 **그 장을 시작할 때의 판**이 있어야 한다. 지금까지는 `campaign.carry` 하나에
"다음 장의 시작 판"만 덮어쓰고 있었다.

```js
// 장이 넘어갈 때
carryAt[next] = carry;        // N장을 시작할 때의 판 (자금·창고·계약·고객·신뢰·본 대사)
```

`startLevel(n)` 은 `carryAt[n]` → `carry` → `null` 순으로 찾는다. 서장(1장)은 언제나 시작 판이 없다.

### 타이틀의 장 고르기

```
        시작하기
          3장
  [서장] [1장] [2장] [3장]
```

- 고를 수 있는 것은 **깬 장 + 지금 장**(`cleared + 1`). 한 장뿐이면(서장만 해 본 프로필) 아예 안 그린다.
- 고르면 확인을 한 번 묻고(`lv.pickAsk` — 진행 중인 런이 사라진다), 그 장의 시작 판으로 **처음부터** 다시 한다.
- 되돌아가도 `cleared` 는 안 내려간다 — 다시 앞으로 갈 때 장 고르기가 그대로 남아 있다.

서장을 다시 고르면 자금 300c · 창고 16칸 · 계약 하나로 완전히 처음부터다(오프닝 컷씬도 다시 나온다).

`test/level-ui.js` 에 「1장에서 서장으로 돌아갈 수 있다」 세 검사를 넣었다 —
칩이 `서장,1장` 으로 뜬다 · 확인을 묻는다 · 서장이 300c/16칸으로 다시 시작된다.

## 부록 AL. 4장 — 소문 (v1.25, 레벨 5)

한 해에서 제일 바쁜 두 달(11월 김장·쇼핑 행사 / 12월 연말)에 **평판**이 열린다.
여기서 처음으로 **끝날 수 있는 판**이 된다. 같이 열리는 것: 사고와 보험 · 보관 계약 · 4칸 대형.

### 종료 조건은 평판 하나다

이 장의 가장 큰 결정. 지금까지 모든 장이 `noBankrupt: true` 였고, 4장도 **그대로 둔다**.

- 자금 부도와 평판 고갈을 같이 걸면, 플레이어는 자기가 **왜** 죽었는지 못 배운다.
- 평판은 지금까지 보이지 않게 움직이던 것들(반송·도난·파손·적재율·정산)의 **총합**이라,
  이 장의 교훈 자체가 "그동안 네가 한 일이 여기 쌓여 있었다"이다.
- 돈으로 죽는 것은 자유 런의 몫이다.

단위 테스트 둘을 박아 뒀다 — `2장에서는 addRep 이 평판을 안 움직인다` · `4장에서 평판 0이면 phase가 over` ·
`1~5장 전부 rules.noBankrupt`.

### 4장 (11~12월)

| | |
| --- | --- |
| 달력 | `startMonth: 11` · `monthOffset: 8` · 11월 후반 8~12일차가 **쇼핑 행사 폭주**(달력 이벤트, 입고 ×1.5) |
| 여는 것 | `rep` · `insurance` · `storage` · `bigsize` |
| 대본 | 1사이클 — 예보가 4칸 대형을 미리 경고하고 마켓이 `large0` 과 보험을 판다<br>2사이클 — 대형이 실제로 오고, 행사 폭주와 보관 제안이 겹친다. 3~4사이클(12월)은 무작위 |
| `addCustomers` | 🚚 이사센터 — 보관 계약을 들고 오는 화주 |

**대형은 2사이클에 온다.** 처음에는 1사이클 4일차에 넣었는데, 실을 차가 없는 채로 아흐레가 흘러 반송이 났다.
3장의 ❄ 와 같은 순서가 맞다 — **예보가 먼저 경고 → 마켓이 답을 판다 → 그다음 도착**.

`callsDelta: +5`. 12월은 한 해에서 물량이 제일 많은 달이라 배차 그릇이 앞 장들보다 커야 한다.

비트 `BEATS_L5` 12개: `l5intro` → **`l5rep`(3쪽: 정체 · 0이면 끝 · 어떻게 올리나)** → `l5repDrop` → `l5repUp` →
`l5bigWarn`/`l5bigCar`/`l5big` → `l5ins`/`l5claim` → `l5offer`(자리를 파는 것) → `l5rush`(행사 주간) → `l5last`.

### 봇이 사람보다 못 부르고 있었다

`level-run` · `campaign-run` 이 **한 턴에 계약 하나만** 불렀다. 사람은 부를 만한 계약을 다 부른다.
12월처럼 하루 일곱 개가 들어오는 달에서 이 차이가 반송 21로 나타났다. 둘 다 고쳤다(한 턴 최대 4회).

이어받기가 없을 때(장을 건너뛰어 고른 경우)의 `company` 시작 판도 3장을 무난히 끝낸 사람의 판에 맞췄다 —
`bulk1 · cold0 · fragile0 · large0` · 48칸 · 냉장 8.

### 검증

```
서장   300 → 1,476c · 처리  87 · 반송 0
1장  1,476 → 2,069c · 처리 107 · 반송 0
2장  2,069 →   897c · 처리 127 · 반송 0
3장    897 → 2,682c · 처리 139 · 반송 0
4장  2,682 → 9,520c · 처리 194 · 반송 0 폐기 0 도난 0
```

단위 **80** · `campaign-run` 전부 통과 · `level-run 1~5` 전부 반송 0 · `sim`(자유 런 회귀) ·
`level-ui` · `play-ui` · `intro-ui` · `tutorial-ui`.

### 남은 것

- 5장(1~2월: ❆ 냉동 · 🛃 통관 · 주말 선택 · 퍽) — 마지막 장이고, **잔금을 치르는 자리**다.
- `DEAL.price 8000`: 4장까지 9,520c 가 남는다. 잔금 7,115c 가 이제는 **너무 헐거워졌다** —
  5장을 붙이면서 값을 다시 잡거나, 중간 장마다 잔금을 조금씩 갚게 하는 편이 낫다.

## 부록 AM. 5장 — 마지막 겨울, 그리고 잔금을 나눠 낸다 (v1.26, 레벨 6)

캠페인이 완성됐다. 한 해(24사이클)가 여섯 장으로 다 이어지고, 마지막 장에서 잔금을 털고 본계약서에 도장을 찍는다.

### 잔금을 회차로 나눈다

부록 AE 에서 "값 8,000c, 마지막에 한 번에" 로 만들었더니 4장까지 9,520c 가 남아 **너무 헐거워졌다**.
더 큰 문제는 구조다 — 한 번에 갚는 구조면 **중간 장에서는 돈을 쌓기만 하면 돼서 계산할 것이 없다.**

```js
const DEAL = {
  price: 11000,
  downRate: 0.6,                                   // 서장 끝 계약금 = 그 시점 자금의 60%
  install: { 2: 500, 3: 700, 4: 1300, 5: 2600 },   // 그 레벨을 끝냈을 때 내는 회차
};
const dueAt = (level, rest) => (level >= LAST ? rest : Math.min(rest, DEAL.install[level] || 0));
```

장이 끝날 때마다 **리포트 → 편지 → 잔금 영수증 → 끝 카드** 순으로 한 회차를 낸다.
영수증에는 매매 대금 · 지금까지 낸 돈 · 이번 회차 · 오늘 낸 돈 · 남은 잔금 + 진행 막대가 있다.
**모자라면 낼 수 있는 만큼만 내고 밀린다** — 영감님은 "됐다, 다음에 같이 보내"라고만 한다(캠페인은 부도로 끊지 않는다).

봇 기준 한 해 흐름 (회차가 실제로 빠진 상태):

| | 끝 자금 | 회차 | 남은 잔금 | 다음 장 시작 |
| --- | ---: | ---: | ---: | ---: |
| 서장 | 1,476 | 계약금 885 | 10,115 | 591 |
| 1장 | 1,184 | 500 | 9,615 | 684 |
| 2장 | 1,126 | 700 | 8,915 | 426 |
| 3장 | 2,520 | 1,300 | 7,615 | 1,220 |
| 4장 | 7,212 | 2,600 | 5,015 | 4,612 |
| 5장 | 6,613 | **5,015 완납** | **0** | 1,598 |

매 장이 "이번 두 달에 회차만큼은 남겨야 한다"로 끝나고, 마지막에 딱 떨어진다.

### 5장 (2028년 1~2월)

| | |
| --- | --- |
| 여는 것 | `frozen`(❆ + 냉동고) · `weekendChoice`(일요일 선택) · `perks` · `codex` |
| 대본 | 1사이클 — 1월 비수기. 예보가 ❆ 를 경고하고 마켓이 냉동 계약·냉동고를 판다<br>2사이클 — ❆ 도착. 3~4사이클(2월)은 설 폭주와 연휴를 혼자 |

**🛃 통관은 캠페인에서 열지 않는다.** 계열이 여섯인데 슬롯이 다섯이라, 마지막 장에서 새 품목을 둘이나 열면
어느 하나는 **받을 길이 아예 없어진다**(실제로 그렇게 만들어 봤더니 ❆ 가 두 사이클 반을 마당에 서 있었다).
통관은 자유 런의 평판 등급이 연다.

**계약 슬롯을 4 → 5로 늘렸다.** 5장에 필요한 계열이 대량·냉장·파손·대형·냉동 다섯이다.
캠페인에서는 `cold` 전 1칸 · `bigsize` 전 3칸 · `frozen` 전 4칸 · 그 뒤 5칸으로 열린다.
(자유 런도 다섯 칸이 되어 봇 평균 현금이 13,300 → 18,000 으로 올랐다 — **자유 런 밸런스 재조정 대상**.)

**`noRepEnd`** — 마지막 장은 평판이 바닥나도 끝나지 않는다. 열 달을 굴려 온 판이 설 연휴 사흘에 끊기면
플레이어가 캠페인의 결말을 못 본다. 평판은 4장에서 가르쳤고, 여기서는 그 결과가 성적과
'잔금을 다 낼 수 있는가'로 남는다.

비트 `BEATS_L6` 9개: `l6intro` → `l6weekend`(일요일 선택) → `l6frozenWarn`/`l6frozenCar`/`l6freezer`/`l6frozen` →
**`l6holiday`(연휴 전에 창고를 비워 둬)** → `l6off` → `l6last`(3쪽, 박 반장의 마지막 말).

장별 편지 `letter.2~6.good/bad` 도 채웠다(지금까지 1장분만 있어 generic 으로 새고 있었다).

### 봇을 고친 것 셋

1. **계약을 꽉 찬 슬롯에 밀어 넣고 있었다** — 주력 계약이 통째로 날아갔다. 빈 슬롯에만 산다.
2. **한 턴에 계약 하나만 불렀다**(부록 AL) — 사람은 다 부른다.
3. **특약을 아무 계약에나 붙이려 했다** — 용달 계열은 특약이 안 붙고(`onlyPlain`), 냉장·파손 계열은
   `need` 때문에 다른 속성을 못 받는다. 붙을 때까지 계약을 돌며 시도한다.

`campaign-run.js` 의 사고 기준도 장마다 다르게 잡았다 — 앞 세 장은 0, 4장은 5 이하, 5장은 35 이하(봇 기준).
설 연휴는 비트가 알려 주는 대로 미리 비우면 사람이 훨씬 낫다.

### 검증

단위 80 · `campaign-run` 여섯 장 전부 통과(잔금 완납 포함) · `level-run 1~6` · `sim` ·
`level-ui`(잔금 화면 3검사 추가) · `play-ui` · `intro-ui` · `tutorial-ui`.

### 남은 것

- **자유 런 밸런스** — 계약 슬롯 5칸으로 현금이 35% 올랐다. 시나리오 승리 조건·가격을 다시 본다.
- 5장 설 구간은 봇이 반송 27을 낸다. 사람 플레이테스트에서 `l6holiday` 경고가 실제로 먹히는지 확인.
- 캠페인을 끝낸 뒤의 엔딩 화면(한 해 성적표)은 아직 리포트·편지·본계약서로만 끝난다.

## 부록 AN. 슬롯은 넷 그대로 — 자리가 없으면 '붙인다' (v1.26.1)

부록 AM 에서 5장을 돌리려고 계약 슬롯을 4 → 5로 늘렸는데, 자유 런 현금이 35% 오르는 부작용이 있었고
무엇보다 **문제를 회피한 것**이었다. 슬롯을 넷으로 되돌리고, 대신 **특약(강화)으로 푸는 법을 가르친다.**

### 특약이 실제로는 작동하지 않고 있었다

`optFrozen`(냉동 컨테이너 특약) 같은 것은 처음부터 있었지만 **어디에도 붙지 않았다.**

```js
// 전 — caps 만 넓혔다
contractCaps(c) { … if (c.enh.opt) caps.push(attr); return caps; }
canHandle(c, p) { return this._carrierAccepts(car, p, this.contractCaps(c), …); }
// _carrierAccepts 안
if (car.need && !car.need.some(a => attrs.includes(a))) return false;   // ← 여기서 걸린다
```

전문 계열은 `need` 가 있다 — 냉장은 `['cold','produce']`, 파손은 `['fragile']`, "이것만 받는다"는 뜻이다.
특약이 `caps` 만 넓히면 **그 계약은 여전히 ❆ 를 거절한다.** 붙일 수 있는 건 `need` 가 없고 `sizeMax ≤ 4` 인
항공 계열뿐이었고, 그건 사실상 없는 길이었다.

```js
// 후 — need 도 같이 넓힌다
contractNeed(c) { const need = car.need.slice(); if (c.enh.opt) need.push(attr); return need; }
canHandle(c, p) { return this._carrierAccepts(car, p, this.contractCaps(c), this.contractSizeMax(c), this.contractNeed(c)); }
```

이제 **냉장 계약에 냉동 특약을 붙이면 그 차가 ❆ 를 받는다.** 자리를 안 먹고, 계약 하나에 특약은 하나다.

### 마켓이 그 길을 반드시 연다

`guaranteeBlocked` 는 지금까지 **계약만** 보장했다. 슬롯이 다 찼으면 그 계약을 살 수가 없으니 무용지물이었다.

```js
if (R.guaranteeBlocked && this.contracts.filter(Boolean).length >= D.CONTRACT_SLOTS) {
  // 막힌 속성을 푸는 opt 강화를, 붙일 계약이 실제로 있을 때만(_canFitOpt) 반드시 매물에 넣는다
}
```

택배 상세의 안내도 갈린다 — 자리가 남았으면 `… 계열 계약이 있어야 합니다`,
**자리가 다 찼으면 `계약 자리가 다 찼습니다 — 마켓에서 <특약>을 사서 지금 계약에 붙이면 이것도 실립니다`**(`pd.needOpt`).

### 5장이 이걸 가르친다

5장 1사이클 마켓에서 냉동 **계약**을 빼고 **`optFrozen` 특약**을 판다. 계약 자리는 이미 넷이 다 차 있다.

새 비트 둘:
- **`l6full`**(3쪽) — "자리가 넷이 다 찼어 → 이럴 땐 계약 말고 <b>특약</b>이야 → 자리가 모자랄 땐 늘리는 게 아니라 **붙이는 거**다"
- `l6stuck` — 그래도 막힌 택배가 있으면 상세를 짚어 준다

### 봇도 예보를 보고 산다

봇이 `blockedTypes()`(이미 창고에 있는 것)만 보고 있어서, **다음 보름에 올 ❆** 를 위해 미리 사지 않았다.
`forecastBlocked()` 를 같이 본다 — 마켓 예보 줄이 붉게 알려 주는 바로 그 정보다.

### 검증

단위 **82**(특약이 need 를 넓힌다 / 자리가 찼으면 마켓이 특약을 낸다 — 2개 추가) ·
`campaign-run` 여섯 장 전부 + 잔금 완납 · `level-run 1~6` · `level-ui` · `play-ui` · `intro-ui` · `tutorial-ui`.

**자유 런 현금이 18,000 → 13,100 으로 제자리**. 슬롯 5칸이 만들던 밸런스 변화가 사라졌다.

## 부록 AO. 한 장 = 한 사이클 · 캠페인은 봄 석 달 (v1.27)

플레이테스트: **"튜토리얼이 너무 지겹다. 두 달도 길다."** 재 보니 정확한 지적이었다.

### 측정

서장(4사이클 · 53영업일)에서 **가르치는 대사가 어디에 있는지** 뽑아 봤다.

```
사이클 1 (13일): l1intro · l1call · l1usage · l1sunday · l1summary
사이클 2 (14일): l1due · l1deadline
사이클 3 (13일): (대사 없음)
사이클 4 (13일): (대사 없음)
```

**53영업일 중 27일까지만 가르치고, 나머지 26일은 같은 판단의 반복이었다.**
봇이 클릭만 해도 UI 플레이 213초, 사람은 10분을 훌쩍 넘는다.

업계 기준과 대조하면 더 분명하다 — 코어 게임플레이에 1분 안에 진입하고 "아하 모먼트"가
90초를 넘으면 상당수가 안 돌아온다, 첫 안내는 2스텝 이하, 튜토리얼 한 단계가 30초만 돼도 길다,
**스텝이 늘수록 이탈이 누적된다**([Playio](https://blog.playio.co/mobile-game-onboarding-retention) ·
[Mobile Free To Play](https://mobilefreetoplay.com/mobile-tutorials-storytelling/)).

### 새 구조 — 여섯 장 × 한 사이클 = 봄 석 달

| 장 | 달력 | 영업일 | 부제 | 여는 것 |
| --- | --- | ---: | --- | --- |
| 서장 | 3월 전반 | 13 | 빈 창고 | — (기한도 없다) |
| 1장 | 3월 후반 | 14 | 두 번째 트럭 | 기한 · market · calls · simul |
| 2장 | 4월 전반 | 13 | 봄비 | weather · theft · self · trust |
| 3장 | 4월 후반 | 13 | 차가운 짐 | attrs · cold · customers |
| 4장 | 5월 전반 | 13 | 소문 | rep · insurance · storage · bigsize |
| 5장 | 5월 후반 | 13 | 마지막 봄 | frozen · weekendChoice · perks · codex |

**서장 UI 플레이 213초 → 56초.** 한 장이 13~14영업일이고 매 장이 새로 가르칠 것을 갖는다.

### `cycleOffset` — 장은 새 런이지만 달력은 이어진다

장마다 `startMonth`·`monthOffset` 을 따로 맞추던 것을 **`cycleOffset` 하나로** 바꿨다.
"이 장이 캠페인의 몇 번째 사이클에서 시작하는가"만 주면 개월차·전후반·달·해·물량표가 전부 따라온다.

```js
cycleAbs(c)   { return (c || this.month || 1) + (this.rules.cycleOffset || 0); }
monthIndex(c) { return Math.ceil(this.cycleAbs(c) / D.CYCLES_PER_MONTH); }
half(c)       { return (this.cycleAbs(c) - 1) % D.CYCLES_PER_MONTH + 1; }
```

`calendarMonths()` 도 1..12 개월차를 가정하던 것을 실제 사이클을 훑도록 고쳤다
(3월 후반만 도는 장이 4월로 찍히고 있었다). 장 카드도 한 사이클이면 `2027년 3월 후반`으로 적는다.

### 장은 **준비 마켓**으로 연다

한 사이클이면 **정산 뒤 마켓이 아예 안 열린다** — 대본의 `market:` 블록이 통째로 죽어 있었다.
서장을 뺀 모든 장을 `prep: true` 로 열어, 마켓이 **장의 시작**에 온다.

이게 오히려 맞다. 이 게임의 핵심은 "오기 전에 준비한다"인데, 마켓이 끝에 있으면
그 장에서 배운 것을 그 장에서 쓸 수가 없다. 이제 **예보를 보고 사고, 그다음 13일을 굴린다.**

따라오는 규칙: **그 장의 대본이 보내는 것은 시작 판이 다 받아야 한다.** 마켓은 준비용이지 생존용이 아니다.
`minWarehouse: { cold, frozen }` 을 추가했다 — `minCap` 이 칸만 보장하고 냉장·냉동은 이월값 그대로라
앞 장에서 냉장고를 안 사면 ❄·❆ 가 갈 데가 없었다.

### 계절을 봄으로 다시 짰다

석 달이면 장마(6월)·연말(12월)·설(2월)이 달력에 없다. 3~5월의 사건으로 다시 엮었다 —
**봄비**(4월 전반) · **이사철**(4월 후반, 대형·보관) · **가정의 달 선물 주간**(5월, ⚠ 파손 +8%p).
부제·맺음말·박 반장 대사·영감님 편지·계약서 문구를 전부 손봤다(ko/en 38곳).
장마·설은 자유 런의 한 해 런에서 그대로 만난다.

### 경제 재조정

장이 1/4 길이가 됐으니 전부 다시 잡았다 — 임대료(`opCostFixed` 200~380 → 110~210),
배차 그릇(`callsDelta`, 사이클 중간 충전이 없으므로 넉넉히), 그리고 잔금.

| | 값 | 회차 |
| --- | ---: | --- |
| 전 | 11,000c | 500 · 700 · 1,300 · 2,600 |
| 후 | **1,200c** | 100 · 120 · 150 · 180 |

봇 기준 한 해(봄) 흐름: 계약금 267 → 회차 100·120·150·180 → 마지막 383 완납, **744c 남고 끝난다.**

### 검증

단위 **82** · `campaign-run` 여섯 장 + 잔금 완납 · `level-run 1~6` · `sim`(자유 런 회귀 이상 없음) ·
`level-ui`(준비 마켓 검사로 교체) · `play-ui`(**56초**) · `intro-ui` · `tutorial-ui`.

### 남은 것 — 밸런스 한 패스가 더 필요하다

3~5장에서 봇이 반송 6~9를 낸다. 품목이 여러 계열로 쪼개지면 어느 차도 80%를 못 채우는데
한 사이클 장에는 중간 충전도 없어서다. 봇은 "80% 찰 때만 부른다"라 사람보다 불리하지만,
사람에게도 같은 압박이 있으므로 **대본의 품목 분포와 계열별 배차 배분**을 한 번 더 잡아야 한다.
(품목을 날짜로 묶어 보는 안은 오히려 나빠졌다 — 처리량이 29 → 22로 떨어졌다.)

## 부록 AP. 서장은 보름 대타 · 블랙스크린을 걷어낸다 (v1.28)

유저 지적 넷.

1. **서장 시작 카드 · 서장 끝 카드 · 1장 시작 카드가 굳이 있나** — 장이 네 사이클이던 때의 격식이
   한 사이클(13영업일)에 그대로 남았다. 프롤로그는 맨 처음 한 번이면 된다.
2. **상점(매매) 계약서를 적는 게 뜬금없다.**
3. **"동네창고"라는 이상한 개념이 설명도 없이 바로 나온다.**
4. **보름만 하고 넘겨받는 것도 이상하다** — 개연성이 필요하다.

### 1. 장 카드를 없앤다

`chapterCard` · `showChapterStart` · `showChapterEnd` 를 지우고 `ch.*` 문구와 `.chcard` CSS 도 걷었다.

- 컷씬이 끝나면 **곧바로 첫날**이다(`startPlay` 가 카드를 끼우지 않는다).
- 장 사이에 남는 것은 **한 사장 편지 한 장**뿐: 마지막 날 → 정산 → (마켓) → 편지 → 바로 다음 장.
- 장 이름은 타이틀의 장 고르기(`lv.ch.*`)에만 남는다. 무엇이 새로 열렸는지는 박 반장이 게임 안에서
  말한다 — 카드가 하던 일이 아니다.

### 2. 서장은 '보름 대타'다

기간과 이유를 사람 입으로 말하게 고쳤다. 프롤로그 자막:

| 전 | 후 |
| --- | --- |
| 지난겨울. — "이제 자네가 올려 봐." | 지난주, 그 사람이 처음으로 셔터를 못 올렸다. |
| "잘 굴리면 싸게 넘겨주지." | — "보름만 자네가 맡아 줘. 하던 대로만 굴리면 돼." |
| 나한테는 여기 말고 갈 데가 없었다. | 할 만하면 그다음 얘기를 하지, 그렇게 말했다. |

이 한 줄이 **서장의 기능 게이팅을 서사로 설명한다** — 마켓도 새 계약도 없는 이유가
"새로 벌이지 말고 하던 대로만"이다(`story.l1free.1`). 박 반장의 첫 대사도
"한 사장한테 들었어 — 이 보름은 자네가 맡는다고"로 바뀐다.

### 3. 값을 부르는 자리는 1장 끝

- **서장 끝**: 편지 한 장. *"내가 아직 셔터를 못 올리네. 봄만 더 맡아 주게 — 여름 오기 전에 할 얘기가 있네."*
  서류도, 상호도, 계약금도 없다.
- **1장 끝(한 달)**: 편지가 값 얘기를 꺼내고 → **간판(상호) 입력** → **가계약서 + 도장**.
- `afterChapter`: level 1 → 그냥 다음 장 / level 2 → 상호·가계약 / 그 뒤 → 잔금 회차.
- `DEAL`: `downRate` 0.6 → **0.5**(자금이 더 쌓인 자리에서 걷는다),
  `install` `{2:100, 3:120, 4:150, 5:180}` → **`{3:120, 4:150, 5:180}`**.
  앞 회차를 가볍게 둬서 **마지막 장에 가장 큰 덩어리**가 남는다(봇 기준 379c).

봇 한 해 흐름(`campaign-run`): 서장 338 → 1장 742(계약금 371) → 2장 710(−120) → 3장 711(−150)
→ 4장 1126(−180) → 5장 1072(잔금 379 완납, 723c 남음).

### 4. 창고에 이름과 자리를 준다

- HUD 가 서장 내내 **자유 런 회사 데이터의 '동네 택배'** 를 걸고 있었다. 이제 한 사장 간판
  **「한성창고」**(`lv.ownerShop`)를 걸고, 상호를 지으면 내 이름으로 바뀐다 — **간판을 바꿔 다는 것**이
  곧 상호 입력이다(`lv.nameBody`).
- 계약서 목적물 `동네 창고 1동 · 부속 마당` → **`신흥동 17-3 창고 1동 · 부속 마당`**.
  프롤로그 첫 줄에도 「한성창고」가 나오므로, **법적 문서가 창고를 처음 명명하는 일**이 없어졌다.
- 상호 기본값 `한길 택배` 가 운송사 `한길 물류` 와 겹쳤다 → **`새봄택배`**.
- `ct.body` 의 "남은 두 달 반" → "남은 두 달"(가계약이 한 장 뒤로 밀렸다). en 의 "the remaining ten" 도 같이.

### 5. 검증

단위 88 · `sim`(자유 런 회귀) · `campaign-run`(여섯 장 · 잔금 완납) · `level-run 1~6`(반송 0~3) ·
`intro-ui` · `play-ui` · `level-ui` 전부 통과, 콘솔 에러 0.
`smoke.js` · `tutorial-ui.js` 는 **이 변경 전(HEAD)에도 같은 자리에서 실패한다** —
타이틀·게임 방법·시나리오 카드 검사 5건과 `#parcels` 클릭(가려짐). 이 작업과 무관한 선행 실패다.

### 남은 것

- 서장 끝 편지가 "봄만 더"를 부탁하는데, 2~5장 편지는 그 약속을 다시 짚지 않는다.
- 1장 끝에 자금이 0이면 계약금 0c 로 가계약이 맺어진다(잔금 1200c 전액). 규칙상 막히진 않지만
  "오늘 계약금 0c 를 받았다"는 문장은 손볼 여지가 있다.

## 부록 AQ. 이름 없는 기능들 — 보름 목표 · 연속 만차 · 일괄 출고 · 투자 (v1.29)

유저 지적 둘.

1. **"도크 러시가 뭔지 미션이 뭔지가 확실하지 않네."**
2. **"투자도 설명이 없는데 처음부터 쓸 수 있어. 결국 더 많은 물량을 받겠다는 거 아닌가?
   레벨을 올리기보단 캠페인을 활성화하면 물량이 쏟아지는 쪽이 좋겠어 — 창고가 남아도는 걸 막게."**

부록 R 의 원칙을 이 넷이 통째로 비껴가 있었다. 미션 미터는 **서장 1일차부터** 떠 있고 설명은
첫 호출 뒤에야 나왔다. 러시는 `shows('simul')` 에 묶여 1장부터 뜨는데 그 장의 주제가 아니었고,
투자 버튼은 1일차부터 눌렸다. **화면에서 빼면 설명할 것도 같이 사라진다** — 다시 그 순서로.

### 1. 노출을 장에 맞춘다

| 플래그 | 여는 장 | 왜 거기인가 |
| --- | --- | --- |
| `mission` · `chain` | 1장 | 마켓에서 돈을 쓰기 시작하는 장. 목표가 생겨야 쓸 이유가 생긴다 |
| `rush` | 2장 | 창고가 차야 의미가 있는 기능이다. 마당·비가 열리는 장과 한 몸 |
| `invest` | 2장 | 창고를 키워야겠다는 게 몸으로 느껴진 뒤. 캠페인과 일괄 출고가 같은 장에서 만난다 |

`rushState().unlocked` 을 `shows('simul')` 에서 `shows('rush')` 로 옮겼고,
미터 셋과 투자 버튼(창고 팝업의 '사업 확장' 포함)은 전부 `shows()` 뒤로 갔다.

### 2. 이름을 한국말로

| 전 | 후 |
| --- | --- |
| 도크 러시 / DOCK RUSH | **일괄 출고** (en: BATCH OUT) |
| 퍼펙트 로드 | **연속 만차** (en 은 Perfect Load 유지) |
| 이번 미션 | **이번 보름 목표** |

`rush.title` 키를 새로 두고 ui.js 의 하드코딩 라벨을 걷었다. 비트도 같이 옮겼다 —
`l1progress`·`l1chainHit` → `l2mission`·`l2chain`, `l2rushReady`·`l2rushHit` → `l3rushReady`·`l3rushHit`.

### 3. 홍보는 상시 물량이 아니라 캠페인이다

홍보 레벨이 **상시 입고 +3건/레벨**이었다. 숫자는 늘지만 플레이어가 하는 일이 없고,
창고가 비어 도는 보름을 메울 방법도 없었다. 레벨의 의미를 **한 번 열 때의 크기**로 바꿨다.

```js
marketing: { costs: [...], parcels: 3, campaign: { days: 3, per: 4, cost: 50 } }
// 캠페인 한 번 = 레벨 × 4건이 3영업일 안에 몰려 들어온다. 비용 = 레벨 × 50c. 보름에 한 번.
```

- `Game.campaignPlan()` / `runCampaign()`. 주입은 기존 `_injectGrowthDemand` 에 끝 경계(`end`)를
  더해 **다음 사흘에만** 몰아넣는다. 쓴 사이클은 `campaignCycle` 로 남아 세이브를 건너도 유지된다.
- 상시 주입은 세 군데에서 걷었다 — `_makeSchedule`(대본·무작위 양쪽), `customerForecast`,
  그리고 홍보를 사면 그 자리에서 물량이 들어오던 `investGrowth`.
- 화면: 투자 창 맨 위에 캠페인 칸(열 수 있으면 활성, 이번 보름에 이미 열었으면 회색).
  홍보 카드의 현재 효과도 "캠페인 한 번에 N건"으로.

**이게 일괄 출고와 한 몸인 이유**: 캠페인으로 창고를 채우고 → 72%를 넘기면 트럭 두 대가 더 붙고 →
한꺼번에 빼면 35%가 더 붙는다. 창고가 비어 있는 것이 손해가 되고, 채우는 수단이 플레이어 손에 있다.

### 4. 검증

단위 **90**(서장 게이팅 1 · 캠페인 1 추가, 홍보 즉시 주입 테스트는 캠페인 기준으로 교체),
`campaign-run` 여섯 장 완주(l2mission·l2chain·l3invest·l3rushReady 가 제 장에서 나온다),
`play-ui`(서장 화면에 미터 셋과 투자 버튼이 없다), `level-ui`, 콘솔 에러 0.

### 남은 것

- 1장에서 러시 트럭 두 대가 빠지면서 봇 처리량이 줄고, 그 돈이 뒤로 이어져 4·5장 반송이
  4→9 · 6→10 으로 늘었다. 임계값 안이지만 **사람 플레이테스트에서 다시 본다**.
  (1장 대본은 원래 배차 7대 × 6칸 = 42칸을 천장으로 짠 것이라 러시의 +2대는 설계 밖이었다.)
- 캠페인 비용·크기(레벨 × 50c / × 4건)는 아직 손으로 잡은 값이다.
- 나머지 다섯 축(차량·창고·자동화·브랜드·저온)은 여전히 설명 없는 레벨업 카드다.

### 덧 — 캐릭터는 '서장'을 모른다 (v1.29.1)

유저 지적: **"'서장 때는 안 보이는 거야'는 제4의 벽을 깨는 거야. 서장·1장 같은 건 캐릭터들이 알면 안 되지."**

맞다. 장 구분은 **우리가 판을 자르는 단위**지 그 사람들이 사는 세계의 말이 아니다.
안 보이던 이유는 판 안의 사정으로 말해야 한다.

| 키 | 전 | 후 |
| --- | --- | --- |
| `story.l2intro.2` | 배차 {trucks}대라고 붙었지? **서장 때는** 안 보이던 거야 | …붙어 있지?<br>**전에는 한 사장이 챙기던 거라** 자네가 볼 일이 없던 숫자야 |
| `story.l3invest.1` | 투자 버튼, **이제부터 쓸 수 있어** | 투자, **이제 자네가 쓸 때가 됐어** |

해금을 알리는 말투(`이제부터 쓸 수 있어`)도 같은 결이라 같이 고쳤다 — 기능이 열린 것이 아니라
**그럴 때가 된 것**으로 말한다. 대사·편지 전체를 훑어 `서장/N장/레벨/챕터`가 남지 않은 것을 확인했다
(화면 라벨과 장 고르기에는 그대로 쓴다 — 그건 캐릭터의 말이 아니다).

이름을 바꾸면서 자유 런 대사에 남아 있던 옛 용어도 같이 정리했다 —
`story.progressHud.*`·`story.chainHit.*` 의 "미션 등급 / 퍼펙트 로드" → "이번 보름 목표 / 연속 만차".
